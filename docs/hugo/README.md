# Docs

The documentation site is generated by Hugo with the Docsy theme. Start the development shell to install the required
tooling, then build and validate the site:

```sh
./dev.sh
task doc:build-site
```

Markdown files go under `/content/`, static files like images under `/static/`.
