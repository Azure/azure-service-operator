// Code generated by azure-service-operator-codegen. DO NOT EDIT.
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
package vCurrent

import "github.com/Azure/azure-service-operator/testing/Verification/vNext"

type Person struct {
	Items [][]int `json:"items"`
}

// AssignPropertiesFromPerson populates our Person from the provided source Person
func (person *Person) AssignPropertiesFromPerson(source *vNext.Person) error {

	// Items
	itemList := make([][]int, len(source.Items))
	for itemIndex, itemItem := range source.Items {
		// Shadow the loop variable to avoid aliasing
		itemItem := itemItem
		itemList1 := make([]int, len(itemItem))
		for itemIndex, itemItem := range itemItem {
			// Shadow the loop variable to avoid aliasing
			itemItem := itemItem
			itemList1[itemIndex] = itemItem
		}
		itemList[itemIndex] = itemList1
	}
	person.Items = itemList

	// No error
	return nil
}

// AssignPropertiesToPerson populates the provided destination Person from our Person
func (person *Person) AssignPropertiesToPerson(destination *vNext.Person) error {

	// Items
	itemList := make([][]int, len(person.Items))
	for itemIndex, itemItem := range person.Items {
		// Shadow the loop variable to avoid aliasing
		itemItem := itemItem
		itemList1 := make([]int, len(itemItem))
		for itemIndex, itemItem := range itemItem {
			// Shadow the loop variable to avoid aliasing
			itemItem := itemItem
			itemList1[itemIndex] = itemItem
		}
		itemList[itemIndex] = itemList1
	}
	destination.Items = itemList

	// No error
	return nil
}
