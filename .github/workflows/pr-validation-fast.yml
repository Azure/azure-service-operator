name: Validate Pull Request
on:
  pull_request: 
    branches:
      - main
    paths:
      - "docs/**"

  merge_group:
    branches:
      - main
    paths:
      - "docs/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
