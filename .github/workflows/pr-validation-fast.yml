name: Validate Pull Request
on:
  pull_request: 
    branches:
      - main
    paths:
      - "docs/**"

  merge_group:
    branches:
      - main
    paths:
      - "docs/**"

jobs:
  integration-tests:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'

  test-generator:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'

  integration-tests-fork:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
