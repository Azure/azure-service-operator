apiVersion: v1
kind: Service
metadata:
  name: azureoperator-webhook-service
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - port: 443
    targetPort: 443
  selector:
    control-plane: controller-manager
