{{- if and ( .Values.createNamespace ) ( not .Values.multipleInstances )}}
apiVersion: v1
kind: Namespace
metadata:
  labels:
    control-plane: controller-manager
  name: azureoperator-system
{{- end}}