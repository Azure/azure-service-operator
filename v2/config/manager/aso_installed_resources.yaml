apiVersion: serviceoperator.azure.com/v1api
kind: InstalledResourceDefinitions
metadata:
  name: installed-resources
  namespace: system
  annotations:
    "helm.sh/hook": post-install,post-upgrade  # TODO: This is a hack
spec:
  pattern:
    - "*"
