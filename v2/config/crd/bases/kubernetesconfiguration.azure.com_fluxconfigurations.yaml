---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: fluxconfigurations.kubernetesconfiguration.azure.com
spec:
  group: kubernetesconfiguration.azure.com
  names:
    kind: FluxConfiguration
    listKind: FluxConfigurationList
    plural: fluxconfigurations
    singular: fluxconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20230501
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /kubernetesconfiguration/resource-manager/Microsoft.KubernetesConfiguration/stable/2023-05-01/fluxconfiguration.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{clusterRp}/{clusterResourceName}/{clusterName}/providers/Microsoft.KubernetesConfiguration/fluxConfigurations/{fluxConfigurationName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              azureBlob:
                description: 'AzureBlob: Parameters to reconcile to the AzureBlob
                  source kind type.'
                properties:
                  accountKey:
                    description: 'AccountKey: The account key (shared key) to access
                      the storage account'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  containerName:
                    description: 'ContainerName: The Azure Blob container name to
                      sync from the url endpoint for the flux configuration.'
                    type: string
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  managedIdentity:
                    description: 'ManagedIdentity: Parameters to authenticate using
                      a Managed Identity.'
                    properties:
                      clientId:
                        description: 'ClientId: The client Id for authenticating a
                          Managed Identity.'
                        type: string
                    type: object
                  sasToken:
                    description: 'SasToken: The Shared Access token to access the
                      storage container'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  servicePrincipal:
                    description: 'ServicePrincipal: Parameters to authenticate using
                      Service Principal.'
                    properties:
                      clientCertificate:
                        description: 'ClientCertificate: Base64-encoded certificate
                          used to authenticate a Service Principal'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientCertificatePassword:
                        description: 'ClientCertificatePassword: The password for
                          the certificate used to authenticate a Service Principal'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientCertificateSendChain:
                        description: |-
                          ClientCertificateSendChain: Specifies whether to include x5c header in client claims when acquiring a token to enable
                          subject name / issuer based authentication for the Client Certificate
                        type: boolean
                      clientId:
                        description: 'ClientId: The client Id for authenticating a
                          Service Principal.'
                        type: string
                      clientIdFromConfig:
                        description: 'ClientIdFromConfig: The client Id for authenticating
                          a Service Principal.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientSecret:
                        description: 'ClientSecret: The client secret for authenticating
                          a Service Principal'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      tenantId:
                        description: 'TenantId: The tenant Id for authenticating a
                          Service Principal'
                        type: string
                      tenantIdFromConfig:
                        description: 'TenantIdFromConfig: The tenant Id for authenticating
                          a Service Principal'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster Azure Blob source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster Azure Blob source with the remote.'
                    type: integer
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      Azure Blob storage account.'
                    type: string
                type: object
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              bucket:
                description: 'Bucket: Parameters to reconcile to the Bucket source
                  kind type.'
                properties:
                  accessKey:
                    description: 'AccessKey: Plaintext access key used to securely
                      access the S3 bucket'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  bucketName:
                    description: 'BucketName: The bucket name to sync from the url
                      endpoint for the flux configuration.'
                    type: string
                  insecure:
                    description: 'Insecure: Specify whether to use insecure communication
                      when puling data from the S3 bucket.'
                    type: boolean
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster bucket source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster bucket source with the remote.'
                    type: integer
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      S3 bucket.'
                    type: string
                type: object
              configurationProtectedSettings:
                description: 'ConfigurationProtectedSettings: Key-value pairs of protected
                  configuration settings for the configuration'
                properties:
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - name
                type: object
              gitRepository:
                description: 'GitRepository: Parameters to reconcile to the GitRepository
                  source kind type.'
                properties:
                  httpsCACert:
                    description: 'HttpsCACert: Base64-encoded HTTPS certificate authority
                      contents used to access git private git repositories over HTTPS'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  httpsUser:
                    description: 'HttpsUser: Plaintext HTTPS username used to access
                      private git repositories over HTTPS'
                    type: string
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  repositoryRef:
                    description: 'RepositoryRef: The source reference for the GitRepository
                      object.'
                    properties:
                      branch:
                        description: 'Branch: The git repository branch name to checkout.'
                        type: string
                      commit:
                        description: |-
                          Commit: The commit SHA to checkout. This value must be combined with the branch name to be valid. This takes precedence
                          over semver.
                        type: string
                      semver:
                        description: 'Semver: The semver range used to match against
                          git repository tags. This takes precedence over tag.'
                        type: string
                      tag:
                        description: 'Tag: The git repository tag name to checkout.
                          This takes precedence over branch.'
                        type: string
                    type: object
                  sshKnownHosts:
                    description: |-
                      SshKnownHosts: Base64-encoded known_hosts value containing public SSH keys required to access private git repositories
                      over SSH
                    type: string
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster git repository source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster git repository source with the remote.'
                    type: integer
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      git repository.'
                    type: string
                type: object
              kustomizations:
                additionalProperties:
                  description: The Kustomization defining how to reconcile the artifact
                    pulled by the source type on the cluster.
                  properties:
                    dependsOn:
                      description: |-
                        DependsOn: Specifies other Kustomizations that this Kustomization depends on. This Kustomization will not reconcile
                        until all dependencies have completed their reconciliation.
                      items:
                        type: string
                      type: array
                    force:
                      description: |-
                        Force: Enable/disable re-creating Kubernetes resources on the cluster when patching fails due to an immutable field
                        change.
                      type: boolean
                    path:
                      description: 'Path: The path in the source reference to reconcile
                        on the cluster.'
                      type: string
                    postBuild:
                      description: 'PostBuild: Used for variable substitution for
                        this Kustomization after kustomize build.'
                      properties:
                        substitute:
                          additionalProperties:
                            type: string
                          description: 'Substitute: Key/value pairs holding the variables
                            to be substituted in this Kustomization.'
                          type: object
                        substituteFrom:
                          description: 'SubstituteFrom: Array of ConfigMaps/Secrets
                            from which the variables are substituted for this Kustomization.'
                          items:
                            description: Array of ConfigMaps/Secrets from which the
                              variables are substituted for this Kustomization.
                            properties:
                              kind:
                                description: 'Kind: Define whether it is ConfigMap
                                  or Secret that holds the variables to be used in
                                  substitution.'
                                type: string
                              name:
                                description: 'Name: Name of the ConfigMap/Secret that
                                  holds the variables to be used in substitution.'
                                type: string
                              optional:
                                description: 'Optional: Set to True to proceed without
                                  ConfigMap/Secret, if it is not present.'
                                type: boolean
                            type: object
                          type: array
                      type: object
                    prune:
                      description: 'Prune: Enable/disable garbage collections of Kubernetes
                        objects created by this Kustomization.'
                      type: boolean
                    retryIntervalInSeconds:
                      description: |-
                        RetryIntervalInSeconds: The interval at which to re-reconcile the Kustomization on the cluster in the event of failure
                        on reconciliation.
                      type: integer
                    syncIntervalInSeconds:
                      description: 'SyncIntervalInSeconds: The interval at which to
                        re-reconcile the Kustomization on the cluster.'
                      type: integer
                    timeoutInSeconds:
                      description: 'TimeoutInSeconds: The maximum time to attempt
                        to reconcile the Kustomization on the cluster.'
                      type: integer
                    wait:
                      description: 'Wait: Enable/disable health check for all Kubernetes
                        objects created by this Kustomization.'
                      type: boolean
                  type: object
                description: 'Kustomizations: Array of kustomizations used to reconcile
                  the artifact pulled by the source type on the cluster.'
                type: object
              namespace:
                description: |-
                  Namespace: The namespace to which this configuration is installed to. Maximum of 253 lower case alphanumeric characters,
                  hyphen and period only.
                type: string
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. This resource is an
                  extension resource, which means that any other Azure resource can be its owner.
                properties:
                  armId:
                    description: Ownership across namespaces is not supported.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              reconciliationWaitDuration:
                description: 'ReconciliationWaitDuration: Maximum duration to wait
                  for flux configuration reconciliation. E.g PT1H, PT5M, P1D'
                type: string
              scope:
                description: 'Scope: Scope at which the operator will be installed.'
                enum:
                - cluster
                - namespace
                type: string
              sourceKind:
                description: 'SourceKind: Source Kind to pull the configuration data
                  from.'
                enum:
                - AzureBlob
                - Bucket
                - GitRepository
                type: string
              suspend:
                description: 'Suspend: Whether this configuration should suspend its
                  reconciliation of its kustomizations and sources.'
                type: boolean
              waitForReconciliation:
                description: 'WaitForReconciliation: Whether flux configuration deployment
                  should wait for cluster to reconcile the kustomizations.'
                type: boolean
            required:
            - owner
            type: object
          status:
            description: The Flux Configuration object returned in Get & Put response.
            properties:
              azureBlob:
                description: 'AzureBlob: Parameters to reconcile to the AzureBlob
                  source kind type.'
                properties:
                  containerName:
                    description: 'ContainerName: The Azure Blob container name to
                      sync from the url endpoint for the flux configuration.'
                    type: string
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  managedIdentity:
                    description: 'ManagedIdentity: Parameters to authenticate using
                      a Managed Identity.'
                    properties:
                      clientId:
                        description: 'ClientId: The client Id for authenticating a
                          Managed Identity.'
                        type: string
                    type: object
                  servicePrincipal:
                    description: 'ServicePrincipal: Parameters to authenticate using
                      Service Principal.'
                    properties:
                      clientCertificateSendChain:
                        description: |-
                          ClientCertificateSendChain: Specifies whether to include x5c header in client claims when acquiring a token to enable
                          subject name / issuer based authentication for the Client Certificate
                        type: boolean
                      clientId:
                        description: 'ClientId: The client Id for authenticating a
                          Service Principal.'
                        type: string
                      tenantId:
                        description: 'TenantId: The tenant Id for authenticating a
                          Service Principal'
                        type: string
                    type: object
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster Azure Blob source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster Azure Blob source with the remote.'
                    type: integer
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      Azure Blob storage account.'
                    type: string
                type: object
              bucket:
                description: 'Bucket: Parameters to reconcile to the Bucket source
                  kind type.'
                properties:
                  bucketName:
                    description: 'BucketName: The bucket name to sync from the url
                      endpoint for the flux configuration.'
                    type: string
                  insecure:
                    description: 'Insecure: Specify whether to use insecure communication
                      when puling data from the S3 bucket.'
                    type: boolean
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster bucket source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster bucket source with the remote.'
                    type: integer
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      S3 bucket.'
                    type: string
                type: object
              complianceState:
                description: |-
                  ComplianceState: Combined status of the Flux Kubernetes resources created by the fluxConfiguration or created by the
                  managed objects.
                type: string
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configurationProtectedSettings:
                additionalProperties:
                  type: string
                description: 'ConfigurationProtectedSettings: Key-value pairs of protected
                  configuration settings for the configuration'
                type: object
              errorMessage:
                description: 'ErrorMessage: Error message returned to the user in
                  the case of provisioning failure.'
                type: string
              gitRepository:
                description: 'GitRepository: Parameters to reconcile to the GitRepository
                  source kind type.'
                properties:
                  httpsUser:
                    description: 'HttpsUser: Plaintext HTTPS username used to access
                      private git repositories over HTTPS'
                    type: string
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  repositoryRef:
                    description: 'RepositoryRef: The source reference for the GitRepository
                      object.'
                    properties:
                      branch:
                        description: 'Branch: The git repository branch name to checkout.'
                        type: string
                      commit:
                        description: |-
                          Commit: The commit SHA to checkout. This value must be combined with the branch name to be valid. This takes precedence
                          over semver.
                        type: string
                      semver:
                        description: 'Semver: The semver range used to match against
                          git repository tags. This takes precedence over tag.'
                        type: string
                      tag:
                        description: 'Tag: The git repository tag name to checkout.
                          This takes precedence over branch.'
                        type: string
                    type: object
                  sshKnownHosts:
                    description: |-
                      SshKnownHosts: Base64-encoded known_hosts value containing public SSH keys required to access private git repositories
                      over SSH
                    type: string
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster git repository source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster git repository source with the remote.'
                    type: integer
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      git repository.'
                    type: string
                type: object
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. Ex -
                  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                type: string
              kustomizations:
                additionalProperties:
                  description: The Kustomization defining how to reconcile the artifact
                    pulled by the source type on the cluster.
                  properties:
                    dependsOn:
                      description: |-
                        DependsOn: Specifies other Kustomizations that this Kustomization depends on. This Kustomization will not reconcile
                        until all dependencies have completed their reconciliation.
                      items:
                        type: string
                      type: array
                    force:
                      description: |-
                        Force: Enable/disable re-creating Kubernetes resources on the cluster when patching fails due to an immutable field
                        change.
                      type: boolean
                    name:
                      description: 'Name: Name of the Kustomization, matching the
                        key in the Kustomizations object map.'
                      type: string
                    path:
                      description: 'Path: The path in the source reference to reconcile
                        on the cluster.'
                      type: string
                    postBuild:
                      description: 'PostBuild: Used for variable substitution for
                        this Kustomization after kustomize build.'
                      properties:
                        substitute:
                          additionalProperties:
                            type: string
                          description: 'Substitute: Key/value pairs holding the variables
                            to be substituted in this Kustomization.'
                          type: object
                        substituteFrom:
                          description: 'SubstituteFrom: Array of ConfigMaps/Secrets
                            from which the variables are substituted for this Kustomization.'
                          items:
                            description: Array of ConfigMaps/Secrets from which the
                              variables are substituted for this Kustomization.
                            properties:
                              kind:
                                description: 'Kind: Define whether it is ConfigMap
                                  or Secret that holds the variables to be used in
                                  substitution.'
                                type: string
                              name:
                                description: 'Name: Name of the ConfigMap/Secret that
                                  holds the variables to be used in substitution.'
                                type: string
                              optional:
                                description: 'Optional: Set to True to proceed without
                                  ConfigMap/Secret, if it is not present.'
                                type: boolean
                            type: object
                          type: array
                      type: object
                    prune:
                      description: 'Prune: Enable/disable garbage collections of Kubernetes
                        objects created by this Kustomization.'
                      type: boolean
                    retryIntervalInSeconds:
                      description: |-
                        RetryIntervalInSeconds: The interval at which to re-reconcile the Kustomization on the cluster in the event of failure
                        on reconciliation.
                      type: integer
                    syncIntervalInSeconds:
                      description: 'SyncIntervalInSeconds: The interval at which to
                        re-reconcile the Kustomization on the cluster.'
                      type: integer
                    timeoutInSeconds:
                      description: 'TimeoutInSeconds: The maximum time to attempt
                        to reconcile the Kustomization on the cluster.'
                      type: integer
                    wait:
                      description: 'Wait: Enable/disable health check for all Kubernetes
                        objects created by this Kustomization.'
                      type: boolean
                  type: object
                description: 'Kustomizations: Array of kustomizations used to reconcile
                  the artifact pulled by the source type on the cluster.'
                type: object
              name:
                description: 'Name: The name of the resource'
                type: string
              namespace:
                description: |-
                  Namespace: The namespace to which this configuration is installed to. Maximum of 253 lower case alphanumeric characters,
                  hyphen and period only.
                type: string
              provisioningState:
                description: 'ProvisioningState: Status of the creation of the fluxConfiguration.'
                type: string
              reconciliationWaitDuration:
                description: 'ReconciliationWaitDuration: Maximum duration to wait
                  for flux configuration reconciliation. E.g PT1H, PT5M, P1D'
                type: string
              repositoryPublicKey:
                description: |-
                  RepositoryPublicKey: Public Key associated with this fluxConfiguration (either generated within the cluster or provided
                  by the user).
                type: string
              scope:
                description: 'Scope: Scope at which the operator will be installed.'
                type: string
              sourceKind:
                description: 'SourceKind: Source Kind to pull the configuration data
                  from.'
                type: string
              sourceSyncedCommitId:
                description: 'SourceSyncedCommitId: Branch and/or SHA of the source
                  commit synced with the cluster.'
                type: string
              sourceUpdatedAt:
                description: 'SourceUpdatedAt: Datetime the fluxConfiguration synced
                  its source on the cluster.'
                type: string
              statusUpdatedAt:
                description: 'StatusUpdatedAt: Datetime the fluxConfiguration synced
                  its status on the cluster with Azure.'
                type: string
              statuses:
                description: |-
                  Statuses: Statuses of the Flux Kubernetes resources created by the fluxConfiguration or created by the managed objects
                  provisioned by the fluxConfiguration.
                items:
                  description: Statuses of objects deployed by the user-specified
                    kustomizations from the git repository.
                  properties:
                    appliedBy:
                      description: 'AppliedBy: Object reference to the Kustomization
                        that applied this object'
                      properties:
                        name:
                          description: 'Name: Name of the object'
                          type: string
                        namespace:
                          description: 'Namespace: Namespace of the object'
                          type: string
                      type: object
                    complianceState:
                      description: |-
                        ComplianceState: Compliance state of the applied object showing whether the applied object has come into a ready state
                        on the cluster.
                      type: string
                    helmReleaseProperties:
                      description: 'HelmReleaseProperties: Additional properties that
                        are provided from objects of the HelmRelease kind'
                      properties:
                        failureCount:
                          description: 'FailureCount: Total number of times that the
                            HelmRelease failed to install or upgrade'
                          type: integer
                        helmChartRef:
                          description: 'HelmChartRef: The reference to the HelmChart
                            object used as the source to this HelmRelease'
                          properties:
                            name:
                              description: 'Name: Name of the object'
                              type: string
                            namespace:
                              description: 'Namespace: Namespace of the object'
                              type: string
                          type: object
                        installFailureCount:
                          description: 'InstallFailureCount: Number of times that
                            the HelmRelease failed to install'
                          type: integer
                        lastRevisionApplied:
                          description: 'LastRevisionApplied: The revision number of
                            the last released object change'
                          type: integer
                        upgradeFailureCount:
                          description: 'UpgradeFailureCount: Number of times that
                            the HelmRelease failed to upgrade'
                          type: integer
                      type: object
                    kind:
                      description: 'Kind: Kind of the applied object'
                      type: string
                    name:
                      description: 'Name: Name of the applied object'
                      type: string
                    namespace:
                      description: 'Namespace: Namespace of the applied object'
                      type: string
                    statusConditions:
                      description: 'StatusConditions: List of Kubernetes object status
                        conditions present on the cluster'
                      items:
                        description: Status condition of Kubernetes object
                        properties:
                          lastTransitionTime:
                            description: 'LastTransitionTime: Last time this status
                              condition has changed'
                            type: string
                          message:
                            description: 'Message: A more verbose description of the
                              object status condition'
                            type: string
                          reason:
                            description: 'Reason: Reason for the specified status
                              condition type status'
                            type: string
                          status:
                            description: 'Status: Status of the Kubernetes object
                              condition type'
                            type: string
                          type:
                            description: 'Type: Object status condition type for this
                              object'
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
              suspend:
                description: 'Suspend: Whether this configuration should suspend its
                  reconciliation of its kustomizations and sources.'
                type: boolean
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
              waitForReconciliation:
                description: 'WaitForReconciliation: Whether flux configuration deployment
                  should wait for cluster to reconcile the kustomizations.'
                type: boolean
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20230501storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20230501.FluxConfiguration
          Generator information:
          - Generated from: /kubernetesconfiguration/resource-manager/Microsoft.KubernetesConfiguration/stable/2023-05-01/fluxconfiguration.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{clusterRp}/{clusterResourceName}/{clusterName}/providers/Microsoft.KubernetesConfiguration/fluxConfigurations/{fluxConfigurationName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20230501.FluxConfiguration_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              azureBlob:
                description: |-
                  Storage version of v1api20230501.AzureBlobDefinition
                  Parameters to reconcile to the AzureBlob source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  accountKey:
                    description: |-
                      SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  containerName:
                    type: string
                  localAuthRef:
                    type: string
                  managedIdentity:
                    description: |-
                      Storage version of v1api20230501.ManagedIdentityDefinition
                      Parameters to authenticate using a Managed Identity.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      clientId:
                        type: string
                    type: object
                  sasToken:
                    description: |-
                      SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  servicePrincipal:
                    description: |-
                      Storage version of v1api20230501.ServicePrincipalDefinition
                      Parameters to authenticate using Service Principal.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      clientCertificate:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientCertificatePassword:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientCertificateSendChain:
                        type: boolean
                      clientId:
                        type: string
                      clientIdFromConfig:
                        description: |-
                          ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientSecret:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      tenantId:
                        type: string
                      tenantIdFromConfig:
                        description: |-
                          ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  url:
                    type: string
                type: object
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              bucket:
                description: |-
                  Storage version of v1api20230501.BucketDefinition
                  Parameters to reconcile to the Bucket source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  accessKey:
                    description: |-
                      SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  bucketName:
                    type: string
                  insecure:
                    type: boolean
                  localAuthRef:
                    type: string
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  url:
                    type: string
                type: object
              configurationProtectedSettings:
                description: |-
                  SecretMapReference is a reference to a Kubernetes secret in the same namespace as
                  the resource it is on.
                properties:
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - name
                type: object
              gitRepository:
                description: |-
                  Storage version of v1api20230501.GitRepositoryDefinition
                  Parameters to reconcile to the GitRepository source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  httpsCACert:
                    description: |-
                      SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  httpsUser:
                    type: string
                  localAuthRef:
                    type: string
                  repositoryRef:
                    description: |-
                      Storage version of v1api20230501.RepositoryRefDefinition
                      The source reference for the GitRepository object.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      branch:
                        type: string
                      commit:
                        type: string
                      semver:
                        type: string
                      tag:
                        type: string
                    type: object
                  sshKnownHosts:
                    type: string
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  url:
                    type: string
                type: object
              kustomizations:
                additionalProperties:
                  description: |-
                    Storage version of v1api20230501.KustomizationDefinition
                    The Kustomization defining how to reconcile the artifact pulled by the source type on the cluster.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    dependsOn:
                      items:
                        type: string
                      type: array
                    force:
                      type: boolean
                    path:
                      type: string
                    postBuild:
                      description: |-
                        Storage version of v1api20230501.PostBuildDefinition
                        The postBuild definitions defining variable substitutions for this Kustomization after kustomize build.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        substitute:
                          additionalProperties:
                            type: string
                          type: object
                        substituteFrom:
                          items:
                            description: |-
                              Storage version of v1api20230501.SubstituteFromDefinition
                              Array of ConfigMaps/Secrets from which the variables are substituted for this Kustomization.
                            properties:
                              $propertyBag:
                                additionalProperties:
                                  type: string
                                description: |-
                                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                  resources, allowing for full fidelity round trip conversions
                                type: object
                              kind:
                                type: string
                              name:
                                type: string
                              optional:
                                type: boolean
                            type: object
                          type: array
                      type: object
                    prune:
                      type: boolean
                    retryIntervalInSeconds:
                      type: integer
                    syncIntervalInSeconds:
                      type: integer
                    timeoutInSeconds:
                      type: integer
                    wait:
                      type: boolean
                  type: object
                type: object
              namespace:
                type: string
              operatorSpec:
                description: |-
                  Storage version of v1api20230501.FluxConfigurationOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. This resource is an
                  extension resource, which means that any other Azure resource can be its owner.
                properties:
                  armId:
                    description: Ownership across namespaces is not supported.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              reconciliationWaitDuration:
                type: string
              scope:
                type: string
              sourceKind:
                type: string
              suspend:
                type: boolean
              waitForReconciliation:
                type: boolean
            required:
            - owner
            type: object
          status:
            description: |-
              Storage version of v1api20230501.FluxConfiguration_STATUS
              The Flux Configuration object returned in Get & Put response.
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              azureBlob:
                description: |-
                  Storage version of v1api20230501.AzureBlobDefinition_STATUS
                  Parameters to reconcile to the AzureBlob source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  containerName:
                    type: string
                  localAuthRef:
                    type: string
                  managedIdentity:
                    description: |-
                      Storage version of v1api20230501.ManagedIdentityDefinition_STATUS
                      Parameters to authenticate using a Managed Identity.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      clientId:
                        type: string
                    type: object
                  servicePrincipal:
                    description: |-
                      Storage version of v1api20230501.ServicePrincipalDefinition_STATUS
                      Parameters to authenticate using Service Principal.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      clientCertificateSendChain:
                        type: boolean
                      clientId:
                        type: string
                      tenantId:
                        type: string
                    type: object
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  url:
                    type: string
                type: object
              bucket:
                description: |-
                  Storage version of v1api20230501.BucketDefinition_STATUS
                  Parameters to reconcile to the Bucket source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  bucketName:
                    type: string
                  insecure:
                    type: boolean
                  localAuthRef:
                    type: string
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  url:
                    type: string
                type: object
              complianceState:
                type: string
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configurationProtectedSettings:
                additionalProperties:
                  type: string
                type: object
              errorMessage:
                type: string
              gitRepository:
                description: |-
                  Storage version of v1api20230501.GitRepositoryDefinition_STATUS
                  Parameters to reconcile to the GitRepository source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  httpsUser:
                    type: string
                  localAuthRef:
                    type: string
                  repositoryRef:
                    description: |-
                      Storage version of v1api20230501.RepositoryRefDefinition_STATUS
                      The source reference for the GitRepository object.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      branch:
                        type: string
                      commit:
                        type: string
                      semver:
                        type: string
                      tag:
                        type: string
                    type: object
                  sshKnownHosts:
                    type: string
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  url:
                    type: string
                type: object
              id:
                type: string
              kustomizations:
                additionalProperties:
                  description: |-
                    Storage version of v1api20230501.KustomizationDefinition_STATUS
                    The Kustomization defining how to reconcile the artifact pulled by the source type on the cluster.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    dependsOn:
                      items:
                        type: string
                      type: array
                    force:
                      type: boolean
                    name:
                      type: string
                    path:
                      type: string
                    postBuild:
                      description: |-
                        Storage version of v1api20230501.PostBuildDefinition_STATUS
                        The postBuild definitions defining variable substitutions for this Kustomization after kustomize build.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        substitute:
                          additionalProperties:
                            type: string
                          type: object
                        substituteFrom:
                          items:
                            description: |-
                              Storage version of v1api20230501.SubstituteFromDefinition_STATUS
                              Array of ConfigMaps/Secrets from which the variables are substituted for this Kustomization.
                            properties:
                              $propertyBag:
                                additionalProperties:
                                  type: string
                                description: |-
                                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                  resources, allowing for full fidelity round trip conversions
                                type: object
                              kind:
                                type: string
                              name:
                                type: string
                              optional:
                                type: boolean
                            type: object
                          type: array
                      type: object
                    prune:
                      type: boolean
                    retryIntervalInSeconds:
                      type: integer
                    syncIntervalInSeconds:
                      type: integer
                    timeoutInSeconds:
                      type: integer
                    wait:
                      type: boolean
                  type: object
                type: object
              name:
                type: string
              namespace:
                type: string
              provisioningState:
                type: string
              reconciliationWaitDuration:
                type: string
              repositoryPublicKey:
                type: string
              scope:
                type: string
              sourceKind:
                type: string
              sourceSyncedCommitId:
                type: string
              sourceUpdatedAt:
                type: string
              statusUpdatedAt:
                type: string
              statuses:
                items:
                  description: |-
                    Storage version of v1api20230501.ObjectStatusDefinition_STATUS
                    Statuses of objects deployed by the user-specified kustomizations from the git repository.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    appliedBy:
                      description: |-
                        Storage version of v1api20230501.ObjectReferenceDefinition_STATUS
                        Object reference to a Kubernetes object on a cluster
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        name:
                          type: string
                        namespace:
                          type: string
                      type: object
                    complianceState:
                      type: string
                    helmReleaseProperties:
                      description: |-
                        Storage version of v1api20230501.HelmReleasePropertiesDefinition_STATUS
                        Properties for HelmRelease objects
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        failureCount:
                          type: integer
                        helmChartRef:
                          description: |-
                            Storage version of v1api20230501.ObjectReferenceDefinition_STATUS
                            Object reference to a Kubernetes object on a cluster
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                        installFailureCount:
                          type: integer
                        lastRevisionApplied:
                          type: integer
                        upgradeFailureCount:
                          type: integer
                      type: object
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    statusConditions:
                      items:
                        description: |-
                          Storage version of v1api20230501.ObjectStatusConditionDefinition_STATUS
                          Status condition of Kubernetes object
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          lastTransitionTime:
                            type: string
                          message:
                            type: string
                          reason:
                            type: string
                          status:
                            type: string
                          type:
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
              suspend:
                type: boolean
              type:
                type: string
              waitForReconciliation:
                type: boolean
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20241101
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /kubernetesconfiguration/resource-manager/Microsoft.KubernetesConfiguration/fluxConfigurations/stable/2024-11-01/fluxconfiguration.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{clusterRp}/{clusterResourceName}/{clusterName}/providers/Microsoft.KubernetesConfiguration/fluxConfigurations/{fluxConfigurationName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              azureBlob:
                description: 'AzureBlob: Parameters to reconcile to the AzureBlob
                  source kind type.'
                properties:
                  accountKey:
                    description: 'AccountKey: The account key (shared key) to access
                      the storage account'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  containerName:
                    description: 'ContainerName: The Azure Blob container name to
                      sync from the url endpoint for the flux configuration.'
                    type: string
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  managedIdentity:
                    description: 'ManagedIdentity: Parameters to authenticate using
                      a Managed Identity.'
                    properties:
                      clientId:
                        description: 'ClientId: The client Id for authenticating a
                          Managed Identity.'
                        type: string
                    type: object
                  sasToken:
                    description: 'SasToken: The Shared Access token to access the
                      storage container'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  servicePrincipal:
                    description: 'ServicePrincipal: Parameters to authenticate using
                      Service Principal.'
                    properties:
                      clientCertificate:
                        description: 'ClientCertificate: Base64-encoded certificate
                          used to authenticate a Service Principal'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientCertificatePassword:
                        description: 'ClientCertificatePassword: The password for
                          the certificate used to authenticate a Service Principal'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientCertificateSendChain:
                        description: |-
                          ClientCertificateSendChain: Specifies whether to include x5c header in client claims when acquiring a token to enable
                          subject name / issuer based authentication for the Client Certificate
                        type: boolean
                      clientId:
                        description: 'ClientId: The client Id for authenticating a
                          Service Principal.'
                        type: string
                      clientIdFromConfig:
                        description: 'ClientIdFromConfig: The client Id for authenticating
                          a Service Principal.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientSecret:
                        description: 'ClientSecret: The client secret for authenticating
                          a Service Principal'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      tenantId:
                        description: 'TenantId: The tenant Id for authenticating a
                          Service Principal'
                        type: string
                      tenantIdFromConfig:
                        description: 'TenantIdFromConfig: The tenant Id for authenticating
                          a Service Principal'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster Azure Blob source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster Azure Blob source with the remote.'
                    type: integer
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      Azure Blob storage account.'
                    type: string
                type: object
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              bucket:
                description: 'Bucket: Parameters to reconcile to the Bucket source
                  kind type.'
                properties:
                  accessKey:
                    description: 'AccessKey: Plaintext access key used to securely
                      access the S3 bucket'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  bucketName:
                    description: 'BucketName: The bucket name to sync from the url
                      endpoint for the flux configuration.'
                    type: string
                  insecure:
                    description: 'Insecure: Specify whether to use insecure communication
                      when puling data from the S3 bucket.'
                    type: boolean
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster bucket source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster bucket source with the remote.'
                    type: integer
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      S3 bucket.'
                    type: string
                type: object
              configurationProtectedSettings:
                description: 'ConfigurationProtectedSettings: Key-value pairs of protected
                  configuration settings for the configuration'
                properties:
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - name
                type: object
              gitRepository:
                description: 'GitRepository: Parameters to reconcile to the GitRepository
                  source kind type.'
                properties:
                  httpsCACert:
                    description: 'HttpsCACert: Base64-encoded HTTPS certificate authority
                      contents used to access git private git repositories over HTTPS'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  httpsUser:
                    description: 'HttpsUser: Plaintext HTTPS username used to access
                      private git repositories over HTTPS'
                    type: string
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  provider:
                    description: 'Provider: Name of the provider used for authentication.'
                    enum:
                    - Azure
                    - Generic
                    type: string
                  repositoryRef:
                    description: 'RepositoryRef: The source reference for the GitRepository
                      object.'
                    properties:
                      branch:
                        description: 'Branch: The git repository branch name to checkout.'
                        type: string
                      commit:
                        description: |-
                          Commit: The commit SHA to checkout. This value must be combined with the branch name to be valid. This takes precedence
                          over semver.
                        type: string
                      semver:
                        description: 'Semver: The semver range used to match against
                          git repository tags. This takes precedence over tag.'
                        type: string
                      tag:
                        description: 'Tag: The git repository tag name to checkout.
                          This takes precedence over branch.'
                        type: string
                    type: object
                  sshKnownHosts:
                    description: |-
                      SshKnownHosts: Base64-encoded known_hosts value containing public SSH keys required to access private git repositories
                      over SSH
                    type: string
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster git repository source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster git repository source with the remote.'
                    type: integer
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      git repository.'
                    type: string
                type: object
              kustomizations:
                additionalProperties:
                  description: The Kustomization defining how to reconcile the artifact
                    pulled by the source type on the cluster.
                  properties:
                    dependsOn:
                      description: |-
                        DependsOn: Specifies other Kustomizations that this Kustomization depends on. This Kustomization will not reconcile
                        until all dependencies have completed their reconciliation.
                      items:
                        type: string
                      type: array
                    force:
                      description: |-
                        Force: Enable/disable re-creating Kubernetes resources on the cluster when patching fails due to an immutable field
                        change.
                      type: boolean
                    path:
                      description: 'Path: The path in the source reference to reconcile
                        on the cluster.'
                      type: string
                    postBuild:
                      description: 'PostBuild: Used for variable substitution for
                        this Kustomization after kustomize build.'
                      properties:
                        substitute:
                          additionalProperties:
                            type: string
                          description: 'Substitute: Key/value pairs holding the variables
                            to be substituted in this Kustomization.'
                          type: object
                        substituteFrom:
                          description: 'SubstituteFrom: Array of ConfigMaps/Secrets
                            from which the variables are substituted for this Kustomization.'
                          items:
                            description: Array of ConfigMaps/Secrets from which the
                              variables are substituted for this Kustomization.
                            properties:
                              kind:
                                description: 'Kind: Define whether it is ConfigMap
                                  or Secret that holds the variables to be used in
                                  substitution.'
                                type: string
                              name:
                                description: 'Name: Name of the ConfigMap/Secret that
                                  holds the variables to be used in substitution.'
                                type: string
                              optional:
                                description: 'Optional: Set to True to proceed without
                                  ConfigMap/Secret, if it is not present.'
                                type: boolean
                            type: object
                          type: array
                      type: object
                    prune:
                      description: 'Prune: Enable/disable garbage collections of Kubernetes
                        objects created by this Kustomization.'
                      type: boolean
                    retryIntervalInSeconds:
                      description: |-
                        RetryIntervalInSeconds: The interval at which to re-reconcile the Kustomization on the cluster in the event of failure
                        on reconciliation.
                      type: integer
                    syncIntervalInSeconds:
                      description: 'SyncIntervalInSeconds: The interval at which to
                        re-reconcile the Kustomization on the cluster.'
                      type: integer
                    timeoutInSeconds:
                      description: 'TimeoutInSeconds: The maximum time to attempt
                        to reconcile the Kustomization on the cluster.'
                      type: integer
                    wait:
                      description: 'Wait: Enable/disable health check for all Kubernetes
                        objects created by this Kustomization.'
                      type: boolean
                  type: object
                description: 'Kustomizations: Array of kustomizations used to reconcile
                  the artifact pulled by the source type on the cluster.'
                type: object
              namespace:
                description: |-
                  Namespace: The namespace to which this configuration is installed to. Maximum of 253 lower case alphanumeric characters,
                  hyphen and period only.
                type: string
              ociRepository:
                description: 'OciRepository: Parameters to reconcile to the OCIRepository
                  source kind type.'
                properties:
                  insecure:
                    description: 'Insecure: Specify whether to allow connecting to
                      a non-TLS HTTP container registry.'
                    type: boolean
                  layerSelector:
                    description: 'LayerSelector: The layer to be pulled from the OCI
                      artifact.'
                    properties:
                      mediaType:
                        description: 'MediaType: The first layer matching the specified
                          media type will be used.'
                        type: string
                      operation:
                        description: |-
                          Operation: The operation to be performed on the selected layer. The default value is 'extract', but it can be set to
                          'copy'.
                        enum:
                        - copy
                        - extract
                        type: string
                    type: object
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  repositoryRef:
                    description: 'RepositoryRef: The source reference for the OCIRepository
                      object.'
                    properties:
                      digest:
                        description: |-
                          Digest: The image digest to pull from OCI repository, the value should be in the format ‘sha256:’. This takes
                          precedence over semver.
                        type: string
                      semver:
                        description: 'Semver: The semver range used to match against
                          OCI repository tags. This takes precedence over tag.'
                        type: string
                      tag:
                        description: 'Tag: The OCI repository image tag name to pull.
                          This defaults to ''latest''.'
                        type: string
                    type: object
                  serviceAccountName:
                    description: 'ServiceAccountName: The service account name to
                      authenticate with the OCI repository.'
                    type: string
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster OCI repository source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster OCI repository source with the remote.'
                    type: integer
                  tlsConfig:
                    description: 'TlsConfig: Parameters to authenticate using TLS
                      config for OCI repository.'
                    properties:
                      caCertificate:
                        description: 'CaCertificate: Base64-encoded CA certificate
                          used to verify the server.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientCertificate:
                        description: 'ClientCertificate: Base64-encoded certificate
                          used to authenticate a client with the OCI repository.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      privateKey:
                        description: 'PrivateKey: Base64-encoded private key used
                          to authenticate a client with the OCI repository.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      OCI repository.'
                    pattern: ^[a-zA-Z][a-zA-Z0-9+-.]*:[^\s]*$
                    type: string
                  useWorkloadIdentity:
                    description: 'UseWorkloadIdentity: Specifies whether to use Workload
                      Identity to authenticate with the OCI repository.'
                    type: boolean
                  verify:
                    description: 'Verify: Verification of the authenticity of an OCI
                      Artifact.'
                    properties:
                      matchOidcIdentity:
                        description: 'MatchOidcIdentity: Array defining the criteria
                          for matching the identity while verifying an OCI artifact.'
                        items:
                          description: MatchOIDCIdentity defines the criteria for
                            matching the identity while verifying an OCI artifact.
                          properties:
                            issuer:
                              description: 'Issuer: The regex pattern to match against
                                to verify the OIDC issuer.'
                              type: string
                            subject:
                              description: 'Subject: The regex pattern to match against
                                to verify the identity subject.'
                              type: string
                          type: object
                        type: array
                      provider:
                        description: 'Provider: Verification provider name.'
                        type: string
                      verificationConfig:
                        additionalProperties:
                          type: string
                        description: 'VerificationConfig: An object containing trusted
                          public keys of trusted authors.'
                        type: object
                    type: object
                type: object
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. This resource is an
                  extension resource, which means that any other Azure resource can be its owner.
                properties:
                  armId:
                    description: Ownership across namespaces is not supported.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              reconciliationWaitDuration:
                description: 'ReconciliationWaitDuration: Maximum duration to wait
                  for flux configuration reconciliation. E.g PT1H, PT5M, P1D'
                type: string
              scope:
                description: 'Scope: Scope at which the operator will be installed.'
                enum:
                - cluster
                - namespace
                type: string
              sourceKind:
                description: 'SourceKind: Source Kind to pull the configuration data
                  from.'
                enum:
                - AzureBlob
                - Bucket
                - GitRepository
                - OCIRepository
                type: string
              suspend:
                description: 'Suspend: Whether this configuration should suspend its
                  reconciliation of its kustomizations and sources.'
                type: boolean
              waitForReconciliation:
                description: 'WaitForReconciliation: Whether flux configuration deployment
                  should wait for cluster to reconcile the kustomizations.'
                type: boolean
            required:
            - owner
            type: object
          status:
            description: The Flux Configuration object returned in Get & Put response.
            properties:
              azureBlob:
                description: 'AzureBlob: Parameters to reconcile to the AzureBlob
                  source kind type.'
                properties:
                  containerName:
                    description: 'ContainerName: The Azure Blob container name to
                      sync from the url endpoint for the flux configuration.'
                    type: string
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  managedIdentity:
                    description: 'ManagedIdentity: Parameters to authenticate using
                      a Managed Identity.'
                    properties:
                      clientId:
                        description: 'ClientId: The client Id for authenticating a
                          Managed Identity.'
                        type: string
                    type: object
                  servicePrincipal:
                    description: 'ServicePrincipal: Parameters to authenticate using
                      Service Principal.'
                    properties:
                      clientCertificateSendChain:
                        description: |-
                          ClientCertificateSendChain: Specifies whether to include x5c header in client claims when acquiring a token to enable
                          subject name / issuer based authentication for the Client Certificate
                        type: boolean
                      clientId:
                        description: 'ClientId: The client Id for authenticating a
                          Service Principal.'
                        type: string
                      tenantId:
                        description: 'TenantId: The tenant Id for authenticating a
                          Service Principal'
                        type: string
                    type: object
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster Azure Blob source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster Azure Blob source with the remote.'
                    type: integer
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      Azure Blob storage account.'
                    type: string
                type: object
              bucket:
                description: 'Bucket: Parameters to reconcile to the Bucket source
                  kind type.'
                properties:
                  bucketName:
                    description: 'BucketName: The bucket name to sync from the url
                      endpoint for the flux configuration.'
                    type: string
                  insecure:
                    description: 'Insecure: Specify whether to use insecure communication
                      when puling data from the S3 bucket.'
                    type: boolean
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster bucket source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster bucket source with the remote.'
                    type: integer
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      S3 bucket.'
                    type: string
                type: object
              complianceState:
                description: |-
                  ComplianceState: Combined status of the Flux Kubernetes resources created by the fluxConfiguration or created by the
                  managed objects.
                type: string
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configurationProtectedSettings:
                additionalProperties:
                  type: string
                description: 'ConfigurationProtectedSettings: Key-value pairs of protected
                  configuration settings for the configuration'
                type: object
              errorMessage:
                description: 'ErrorMessage: Error message returned to the user in
                  the case of provisioning failure.'
                type: string
              gitRepository:
                description: 'GitRepository: Parameters to reconcile to the GitRepository
                  source kind type.'
                properties:
                  httpsUser:
                    description: 'HttpsUser: Plaintext HTTPS username used to access
                      private git repositories over HTTPS'
                    type: string
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  provider:
                    description: 'Provider: Name of the provider used for authentication.'
                    type: string
                  repositoryRef:
                    description: 'RepositoryRef: The source reference for the GitRepository
                      object.'
                    properties:
                      branch:
                        description: 'Branch: The git repository branch name to checkout.'
                        type: string
                      commit:
                        description: |-
                          Commit: The commit SHA to checkout. This value must be combined with the branch name to be valid. This takes precedence
                          over semver.
                        type: string
                      semver:
                        description: 'Semver: The semver range used to match against
                          git repository tags. This takes precedence over tag.'
                        type: string
                      tag:
                        description: 'Tag: The git repository tag name to checkout.
                          This takes precedence over branch.'
                        type: string
                    type: object
                  sshKnownHosts:
                    description: |-
                      SshKnownHosts: Base64-encoded known_hosts value containing public SSH keys required to access private git repositories
                      over SSH
                    type: string
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster git repository source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster git repository source with the remote.'
                    type: integer
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      git repository.'
                    type: string
                type: object
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. Ex -
                  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                type: string
              kustomizations:
                additionalProperties:
                  description: The Kustomization defining how to reconcile the artifact
                    pulled by the source type on the cluster.
                  properties:
                    dependsOn:
                      description: |-
                        DependsOn: Specifies other Kustomizations that this Kustomization depends on. This Kustomization will not reconcile
                        until all dependencies have completed their reconciliation.
                      items:
                        type: string
                      type: array
                    force:
                      description: |-
                        Force: Enable/disable re-creating Kubernetes resources on the cluster when patching fails due to an immutable field
                        change.
                      type: boolean
                    name:
                      description: 'Name: Name of the Kustomization, matching the
                        key in the Kustomizations object map.'
                      type: string
                    path:
                      description: 'Path: The path in the source reference to reconcile
                        on the cluster.'
                      type: string
                    postBuild:
                      description: 'PostBuild: Used for variable substitution for
                        this Kustomization after kustomize build.'
                      properties:
                        substitute:
                          additionalProperties:
                            type: string
                          description: 'Substitute: Key/value pairs holding the variables
                            to be substituted in this Kustomization.'
                          type: object
                        substituteFrom:
                          description: 'SubstituteFrom: Array of ConfigMaps/Secrets
                            from which the variables are substituted for this Kustomization.'
                          items:
                            description: Array of ConfigMaps/Secrets from which the
                              variables are substituted for this Kustomization.
                            properties:
                              kind:
                                description: 'Kind: Define whether it is ConfigMap
                                  or Secret that holds the variables to be used in
                                  substitution.'
                                type: string
                              name:
                                description: 'Name: Name of the ConfigMap/Secret that
                                  holds the variables to be used in substitution.'
                                type: string
                              optional:
                                description: 'Optional: Set to True to proceed without
                                  ConfigMap/Secret, if it is not present.'
                                type: boolean
                            type: object
                          type: array
                      type: object
                    prune:
                      description: 'Prune: Enable/disable garbage collections of Kubernetes
                        objects created by this Kustomization.'
                      type: boolean
                    retryIntervalInSeconds:
                      description: |-
                        RetryIntervalInSeconds: The interval at which to re-reconcile the Kustomization on the cluster in the event of failure
                        on reconciliation.
                      type: integer
                    syncIntervalInSeconds:
                      description: 'SyncIntervalInSeconds: The interval at which to
                        re-reconcile the Kustomization on the cluster.'
                      type: integer
                    timeoutInSeconds:
                      description: 'TimeoutInSeconds: The maximum time to attempt
                        to reconcile the Kustomization on the cluster.'
                      type: integer
                    wait:
                      description: 'Wait: Enable/disable health check for all Kubernetes
                        objects created by this Kustomization.'
                      type: boolean
                  type: object
                description: 'Kustomizations: Array of kustomizations used to reconcile
                  the artifact pulled by the source type on the cluster.'
                type: object
              name:
                description: 'Name: The name of the resource'
                type: string
              namespace:
                description: |-
                  Namespace: The namespace to which this configuration is installed to. Maximum of 253 lower case alphanumeric characters,
                  hyphen and period only.
                type: string
              ociRepository:
                description: 'OciRepository: Parameters to reconcile to the OCIRepository
                  source kind type.'
                properties:
                  insecure:
                    description: 'Insecure: Specify whether to allow connecting to
                      a non-TLS HTTP container registry.'
                    type: boolean
                  layerSelector:
                    description: 'LayerSelector: The layer to be pulled from the OCI
                      artifact.'
                    properties:
                      mediaType:
                        description: 'MediaType: The first layer matching the specified
                          media type will be used.'
                        type: string
                      operation:
                        description: |-
                          Operation: The operation to be performed on the selected layer. The default value is 'extract', but it can be set to
                          'copy'.
                        type: string
                    type: object
                  localAuthRef:
                    description: |-
                      LocalAuthRef: Name of a local secret on the Kubernetes cluster to use as the authentication secret rather than the
                      managed or user-provided configuration secrets.
                    type: string
                  repositoryRef:
                    description: 'RepositoryRef: The source reference for the OCIRepository
                      object.'
                    properties:
                      digest:
                        description: |-
                          Digest: The image digest to pull from OCI repository, the value should be in the format ‘sha256:’. This takes
                          precedence over semver.
                        type: string
                      semver:
                        description: 'Semver: The semver range used to match against
                          OCI repository tags. This takes precedence over tag.'
                        type: string
                      tag:
                        description: 'Tag: The OCI repository image tag name to pull.
                          This defaults to ''latest''.'
                        type: string
                    type: object
                  serviceAccountName:
                    description: 'ServiceAccountName: The service account name to
                      authenticate with the OCI repository.'
                    type: string
                  syncIntervalInSeconds:
                    description: 'SyncIntervalInSeconds: The interval at which to
                      re-reconcile the cluster OCI repository source with the remote.'
                    type: integer
                  timeoutInSeconds:
                    description: 'TimeoutInSeconds: The maximum time to attempt to
                      reconcile the cluster OCI repository source with the remote.'
                    type: integer
                  tlsConfig:
                    description: 'TlsConfig: Parameters to authenticate using TLS
                      config for OCI repository.'
                    type: object
                  url:
                    description: 'Url: The URL to sync for the flux configuration
                      OCI repository.'
                    type: string
                  useWorkloadIdentity:
                    description: 'UseWorkloadIdentity: Specifies whether to use Workload
                      Identity to authenticate with the OCI repository.'
                    type: boolean
                  verify:
                    description: 'Verify: Verification of the authenticity of an OCI
                      Artifact.'
                    properties:
                      matchOidcIdentity:
                        description: 'MatchOidcIdentity: Array defining the criteria
                          for matching the identity while verifying an OCI artifact.'
                        items:
                          description: MatchOIDCIdentity defines the criteria for
                            matching the identity while verifying an OCI artifact.
                          properties:
                            issuer:
                              description: 'Issuer: The regex pattern to match against
                                to verify the OIDC issuer.'
                              type: string
                            subject:
                              description: 'Subject: The regex pattern to match against
                                to verify the identity subject.'
                              type: string
                          type: object
                        type: array
                      provider:
                        description: 'Provider: Verification provider name.'
                        type: string
                      verificationConfig:
                        additionalProperties:
                          type: string
                        description: 'VerificationConfig: An object containing trusted
                          public keys of trusted authors.'
                        type: object
                    type: object
                type: object
              reconciliationWaitDuration:
                description: 'ReconciliationWaitDuration: Maximum duration to wait
                  for flux configuration reconciliation. E.g PT1H, PT5M, P1D'
                type: string
              repositoryPublicKey:
                description: |-
                  RepositoryPublicKey: Public Key associated with this fluxConfiguration (either generated within the cluster or provided
                  by the user).
                type: string
              scope:
                description: 'Scope: Scope at which the operator will be installed.'
                type: string
              sourceKind:
                description: 'SourceKind: Source Kind to pull the configuration data
                  from.'
                type: string
              sourceSyncedCommitId:
                description: 'SourceSyncedCommitId: Branch and/or SHA of the source
                  commit synced with the cluster.'
                type: string
              sourceUpdatedAt:
                description: 'SourceUpdatedAt: Datetime the fluxConfiguration synced
                  its source on the cluster.'
                type: string
              statusUpdatedAt:
                description: 'StatusUpdatedAt: Datetime the fluxConfiguration synced
                  its status on the cluster with Azure.'
                type: string
              statuses:
                description: |-
                  Statuses: Statuses of the Flux Kubernetes resources created by the fluxConfiguration or created by the managed objects
                  provisioned by the fluxConfiguration.
                items:
                  description: Statuses of objects deployed by the user-specified
                    kustomizations from the git repository.
                  properties:
                    appliedBy:
                      description: 'AppliedBy: Object reference to the Kustomization
                        that applied this object'
                      properties:
                        name:
                          description: 'Name: Name of the object'
                          type: string
                        namespace:
                          description: 'Namespace: Namespace of the object'
                          type: string
                      type: object
                    complianceState:
                      description: |-
                        ComplianceState: Compliance state of the applied object showing whether the applied object has come into a ready state
                        on the cluster.
                      type: string
                    helmReleaseProperties:
                      description: 'HelmReleaseProperties: Additional properties that
                        are provided from objects of the HelmRelease kind'
                      properties:
                        failureCount:
                          description: 'FailureCount: Total number of times that the
                            HelmRelease failed to install or upgrade'
                          type: integer
                        helmChartRef:
                          description: 'HelmChartRef: The reference to the HelmChart
                            object used as the source to this HelmRelease'
                          properties:
                            name:
                              description: 'Name: Name of the object'
                              type: string
                            namespace:
                              description: 'Namespace: Namespace of the object'
                              type: string
                          type: object
                        installFailureCount:
                          description: 'InstallFailureCount: Number of times that
                            the HelmRelease failed to install'
                          type: integer
                        lastRevisionApplied:
                          description: 'LastRevisionApplied: The revision number of
                            the last released object change'
                          type: integer
                        upgradeFailureCount:
                          description: 'UpgradeFailureCount: Number of times that
                            the HelmRelease failed to upgrade'
                          type: integer
                      type: object
                    kind:
                      description: 'Kind: Kind of the applied object'
                      type: string
                    name:
                      description: 'Name: Name of the applied object'
                      type: string
                    namespace:
                      description: 'Namespace: Namespace of the applied object'
                      type: string
                    statusConditions:
                      description: 'StatusConditions: List of Kubernetes object status
                        conditions present on the cluster'
                      items:
                        description: Status condition of Kubernetes object
                        properties:
                          lastTransitionTime:
                            description: 'LastTransitionTime: Last time this status
                              condition has changed'
                            type: string
                          message:
                            description: 'Message: A more verbose description of the
                              object status condition'
                            type: string
                          reason:
                            description: 'Reason: Reason for the specified status
                              condition type status'
                            type: string
                          status:
                            description: 'Status: Status of the Kubernetes object
                              condition type'
                            type: string
                          type:
                            description: 'Type: Object status condition type for this
                              object'
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
              suspend:
                description: 'Suspend: Whether this configuration should suspend its
                  reconciliation of its kustomizations and sources.'
                type: boolean
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
              waitForReconciliation:
                description: 'WaitForReconciliation: Whether flux configuration deployment
                  should wait for cluster to reconcile the kustomizations.'
                type: boolean
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20241101storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20241101.FluxConfiguration
          Generator information:
          - Generated from: /kubernetesconfiguration/resource-manager/Microsoft.KubernetesConfiguration/fluxConfigurations/stable/2024-11-01/fluxconfiguration.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{clusterRp}/{clusterResourceName}/{clusterName}/providers/Microsoft.KubernetesConfiguration/fluxConfigurations/{fluxConfigurationName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20241101.FluxConfiguration_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              azureBlob:
                description: |-
                  Storage version of v1api20241101.AzureBlobDefinition
                  Parameters to reconcile to the AzureBlob source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  accountKey:
                    description: |-
                      SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  containerName:
                    type: string
                  localAuthRef:
                    type: string
                  managedIdentity:
                    description: |-
                      Storage version of v1api20241101.ManagedIdentityDefinition
                      Parameters to authenticate using a Managed Identity.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      clientId:
                        type: string
                    type: object
                  sasToken:
                    description: |-
                      SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  servicePrincipal:
                    description: |-
                      Storage version of v1api20241101.ServicePrincipalDefinition
                      Parameters to authenticate using Service Principal.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      clientCertificate:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientCertificatePassword:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientCertificateSendChain:
                        type: boolean
                      clientId:
                        type: string
                      clientIdFromConfig:
                        description: |-
                          ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientSecret:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      tenantId:
                        type: string
                      tenantIdFromConfig:
                        description: |-
                          ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  url:
                    type: string
                type: object
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              bucket:
                description: |-
                  Storage version of v1api20241101.BucketDefinition
                  Parameters to reconcile to the Bucket source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  accessKey:
                    description: |-
                      SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  bucketName:
                    type: string
                  insecure:
                    type: boolean
                  localAuthRef:
                    type: string
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  url:
                    type: string
                type: object
              configurationProtectedSettings:
                description: |-
                  SecretMapReference is a reference to a Kubernetes secret in the same namespace as
                  the resource it is on.
                properties:
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - name
                type: object
              gitRepository:
                description: |-
                  Storage version of v1api20241101.GitRepositoryDefinition
                  Parameters to reconcile to the GitRepository source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  httpsCACert:
                    description: |-
                      SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  httpsUser:
                    type: string
                  localAuthRef:
                    type: string
                  provider:
                    type: string
                  repositoryRef:
                    description: |-
                      Storage version of v1api20241101.RepositoryRefDefinition
                      The source reference for the GitRepository object.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      branch:
                        type: string
                      commit:
                        type: string
                      semver:
                        type: string
                      tag:
                        type: string
                    type: object
                  sshKnownHosts:
                    type: string
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  url:
                    type: string
                type: object
              kustomizations:
                additionalProperties:
                  description: |-
                    Storage version of v1api20241101.KustomizationDefinition
                    The Kustomization defining how to reconcile the artifact pulled by the source type on the cluster.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    dependsOn:
                      items:
                        type: string
                      type: array
                    force:
                      type: boolean
                    path:
                      type: string
                    postBuild:
                      description: |-
                        Storage version of v1api20241101.PostBuildDefinition
                        The postBuild definitions defining variable substitutions for this Kustomization after kustomize build.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        substitute:
                          additionalProperties:
                            type: string
                          type: object
                        substituteFrom:
                          items:
                            description: |-
                              Storage version of v1api20241101.SubstituteFromDefinition
                              Array of ConfigMaps/Secrets from which the variables are substituted for this Kustomization.
                            properties:
                              $propertyBag:
                                additionalProperties:
                                  type: string
                                description: |-
                                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                  resources, allowing for full fidelity round trip conversions
                                type: object
                              kind:
                                type: string
                              name:
                                type: string
                              optional:
                                type: boolean
                            type: object
                          type: array
                      type: object
                    prune:
                      type: boolean
                    retryIntervalInSeconds:
                      type: integer
                    syncIntervalInSeconds:
                      type: integer
                    timeoutInSeconds:
                      type: integer
                    wait:
                      type: boolean
                  type: object
                type: object
              namespace:
                type: string
              ociRepository:
                description: |-
                  Storage version of v1api20241101.OCIRepositoryDefinition
                  Parameters to reconcile to the OCIRepository source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  insecure:
                    type: boolean
                  layerSelector:
                    description: |-
                      Storage version of v1api20241101.LayerSelectorDefinition
                      Parameters to specify which layer to pull from the OCI artifact. By default, the first layer in the artifact is pulled.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      mediaType:
                        type: string
                      operation:
                        type: string
                    type: object
                  localAuthRef:
                    type: string
                  repositoryRef:
                    description: |-
                      Storage version of v1api20241101.OCIRepositoryRefDefinition
                      The source reference for the OCIRepository object.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      digest:
                        type: string
                      semver:
                        type: string
                      tag:
                        type: string
                    type: object
                  serviceAccountName:
                    type: string
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  tlsConfig:
                    description: |-
                      Storage version of v1api20241101.TlsConfigDefinition
                      Parameters to authenticate using TLS config for OCI repository.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      caCertificate:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientCertificate:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      privateKey:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  url:
                    type: string
                  useWorkloadIdentity:
                    type: boolean
                  verify:
                    description: |-
                      Storage version of v1api20241101.VerifyDefinition
                      Parameters to verify the authenticity of an OCI Artifact.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      matchOidcIdentity:
                        items:
                          description: |-
                            Storage version of v1api20241101.MatchOidcIdentityDefinition
                            MatchOIDCIdentity defines the criteria for matching the identity while verifying an OCI artifact.
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            issuer:
                              type: string
                            subject:
                              type: string
                          type: object
                        type: array
                      provider:
                        type: string
                      verificationConfig:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                type: object
              operatorSpec:
                description: |-
                  Storage version of v1api20241101.FluxConfigurationOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. This resource is an
                  extension resource, which means that any other Azure resource can be its owner.
                properties:
                  armId:
                    description: Ownership across namespaces is not supported.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              reconciliationWaitDuration:
                type: string
              scope:
                type: string
              sourceKind:
                type: string
              suspend:
                type: boolean
              waitForReconciliation:
                type: boolean
            required:
            - owner
            type: object
          status:
            description: |-
              Storage version of v1api20241101.FluxConfiguration_STATUS
              The Flux Configuration object returned in Get & Put response.
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              azureBlob:
                description: |-
                  Storage version of v1api20241101.AzureBlobDefinition_STATUS
                  Parameters to reconcile to the AzureBlob source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  containerName:
                    type: string
                  localAuthRef:
                    type: string
                  managedIdentity:
                    description: |-
                      Storage version of v1api20241101.ManagedIdentityDefinition_STATUS
                      Parameters to authenticate using a Managed Identity.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      clientId:
                        type: string
                    type: object
                  servicePrincipal:
                    description: |-
                      Storage version of v1api20241101.ServicePrincipalDefinition_STATUS
                      Parameters to authenticate using Service Principal.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      clientCertificateSendChain:
                        type: boolean
                      clientId:
                        type: string
                      tenantId:
                        type: string
                    type: object
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  url:
                    type: string
                type: object
              bucket:
                description: |-
                  Storage version of v1api20241101.BucketDefinition_STATUS
                  Parameters to reconcile to the Bucket source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  bucketName:
                    type: string
                  insecure:
                    type: boolean
                  localAuthRef:
                    type: string
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  url:
                    type: string
                type: object
              complianceState:
                type: string
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configurationProtectedSettings:
                additionalProperties:
                  type: string
                type: object
              errorMessage:
                type: string
              gitRepository:
                description: |-
                  Storage version of v1api20241101.GitRepositoryDefinition_STATUS
                  Parameters to reconcile to the GitRepository source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  httpsUser:
                    type: string
                  localAuthRef:
                    type: string
                  provider:
                    type: string
                  repositoryRef:
                    description: |-
                      Storage version of v1api20241101.RepositoryRefDefinition_STATUS
                      The source reference for the GitRepository object.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      branch:
                        type: string
                      commit:
                        type: string
                      semver:
                        type: string
                      tag:
                        type: string
                    type: object
                  sshKnownHosts:
                    type: string
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  url:
                    type: string
                type: object
              id:
                type: string
              kustomizations:
                additionalProperties:
                  description: |-
                    Storage version of v1api20241101.KustomizationDefinition_STATUS
                    The Kustomization defining how to reconcile the artifact pulled by the source type on the cluster.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    dependsOn:
                      items:
                        type: string
                      type: array
                    force:
                      type: boolean
                    name:
                      type: string
                    path:
                      type: string
                    postBuild:
                      description: |-
                        Storage version of v1api20241101.PostBuildDefinition_STATUS
                        The postBuild definitions defining variable substitutions for this Kustomization after kustomize build.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        substitute:
                          additionalProperties:
                            type: string
                          type: object
                        substituteFrom:
                          items:
                            description: |-
                              Storage version of v1api20241101.SubstituteFromDefinition_STATUS
                              Array of ConfigMaps/Secrets from which the variables are substituted for this Kustomization.
                            properties:
                              $propertyBag:
                                additionalProperties:
                                  type: string
                                description: |-
                                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                  resources, allowing for full fidelity round trip conversions
                                type: object
                              kind:
                                type: string
                              name:
                                type: string
                              optional:
                                type: boolean
                            type: object
                          type: array
                      type: object
                    prune:
                      type: boolean
                    retryIntervalInSeconds:
                      type: integer
                    syncIntervalInSeconds:
                      type: integer
                    timeoutInSeconds:
                      type: integer
                    wait:
                      type: boolean
                  type: object
                type: object
              name:
                type: string
              namespace:
                type: string
              ociRepository:
                description: |-
                  Storage version of v1api20241101.OCIRepositoryDefinition_STATUS
                  Parameters to reconcile to the OCIRepository source kind type.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  insecure:
                    type: boolean
                  layerSelector:
                    description: |-
                      Storage version of v1api20241101.LayerSelectorDefinition_STATUS
                      Parameters to specify which layer to pull from the OCI artifact. By default, the first layer in the artifact is pulled.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      mediaType:
                        type: string
                      operation:
                        type: string
                    type: object
                  localAuthRef:
                    type: string
                  repositoryRef:
                    description: |-
                      Storage version of v1api20241101.OCIRepositoryRefDefinition_STATUS
                      The source reference for the OCIRepository object.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      digest:
                        type: string
                      semver:
                        type: string
                      tag:
                        type: string
                    type: object
                  serviceAccountName:
                    type: string
                  syncIntervalInSeconds:
                    type: integer
                  timeoutInSeconds:
                    type: integer
                  tlsConfig:
                    description: |-
                      Storage version of v1api20241101.TlsConfigDefinition_STATUS
                      Parameters to authenticate using TLS config for OCI repository.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                    type: object
                  url:
                    type: string
                  useWorkloadIdentity:
                    type: boolean
                  verify:
                    description: |-
                      Storage version of v1api20241101.VerifyDefinition_STATUS
                      Parameters to verify the authenticity of an OCI Artifact.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      matchOidcIdentity:
                        items:
                          description: |-
                            Storage version of v1api20241101.MatchOidcIdentityDefinition_STATUS
                            MatchOIDCIdentity defines the criteria for matching the identity while verifying an OCI artifact.
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            issuer:
                              type: string
                            subject:
                              type: string
                          type: object
                        type: array
                      provider:
                        type: string
                      verificationConfig:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                type: object
              reconciliationWaitDuration:
                type: string
              repositoryPublicKey:
                type: string
              scope:
                type: string
              sourceKind:
                type: string
              sourceSyncedCommitId:
                type: string
              sourceUpdatedAt:
                type: string
              statusUpdatedAt:
                type: string
              statuses:
                items:
                  description: |-
                    Storage version of v1api20241101.ObjectStatusDefinition_STATUS
                    Statuses of objects deployed by the user-specified kustomizations from the git repository.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    appliedBy:
                      description: |-
                        Storage version of v1api20241101.ObjectReferenceDefinition_STATUS
                        Object reference to a Kubernetes object on a cluster
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        name:
                          type: string
                        namespace:
                          type: string
                      type: object
                    complianceState:
                      type: string
                    helmReleaseProperties:
                      description: |-
                        Storage version of v1api20241101.HelmReleasePropertiesDefinition_STATUS
                        Properties for HelmRelease objects
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        failureCount:
                          type: integer
                        helmChartRef:
                          description: |-
                            Storage version of v1api20241101.ObjectReferenceDefinition_STATUS
                            Object reference to a Kubernetes object on a cluster
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            name:
                              type: string
                            namespace:
                              type: string
                          type: object
                        installFailureCount:
                          type: integer
                        lastRevisionApplied:
                          type: integer
                        upgradeFailureCount:
                          type: integer
                      type: object
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    statusConditions:
                      items:
                        description: |-
                          Storage version of v1api20241101.ObjectStatusConditionDefinition_STATUS
                          Status condition of Kubernetes object
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          lastTransitionTime:
                            type: string
                          message:
                            type: string
                          reason:
                            type: string
                          status:
                            type: string
                          type:
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
              suspend:
                type: boolean
              type:
                type: string
              waitForReconciliation:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
