---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: accounts.cognitiveservices.azure.com
spec:
  group: cognitiveservices.azure.com
  names:
    kind: Account
    listKind: AccountList
    plural: accounts
    singular: account
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20250601
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /cognitiveservices/resource-manager/Microsoft.CognitiveServices/stable/2025-06-01/cognitiveservices.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.CognitiveServices/accounts/{accountName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                maxLength: 64
                minLength: 2
                pattern: ^[a-zA-Z0-9][a-zA-Z0-9_.-]*$
                type: string
              identity:
                description: 'Identity: Identity for the resource.'
                properties:
                  type:
                    description: 'Type: The identity type.'
                    enum:
                    - None
                    - SystemAssigned
                    - SystemAssigned, UserAssigned
                    - UserAssigned
                    type: string
                  userAssignedIdentities:
                    description: |-
                      UserAssignedIdentities: The list of user assigned identities associated with the resource. The user identity dictionary
                      key references will be ARM resource ids in the form:
                      '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}
                    items:
                      description: Information about the user assigned identity for
                        the resource
                      properties:
                        reference:
                          description: ResourceReference represents a resource reference,
                            either to a Kubernetes resource or directly to an Azure
                            resource via ARMID
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              kind:
                description: 'Kind: The Kind of the resource.'
                type: string
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: 'Secrets: configures where to place Azure generated
                      secrets.'
                    properties:
                      key1:
                        description: 'Key1: indicates where the Key1 secret should
                          be placed. If omitted, the secret will not be retrieved
                          from Azure.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      key2:
                        description: 'Key2: indicates where the Key2 secret should
                          be placed. If omitted, the secret will not be retrieved
                          from Azure.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              properties:
                description: 'Properties: Properties of Cognitive Services account.'
                properties:
                  allowProjectManagement:
                    description: |-
                      AllowProjectManagement: Specifies whether this resource support project management as child resources, used as
                      containers for access management, data isolation and cost in AI Foundry.
                    type: boolean
                  allowedFqdnList:
                    items:
                      type: string
                    type: array
                  amlWorkspace:
                    description: 'AmlWorkspace: The user owned AML account properties.'
                    properties:
                      identityClientId:
                        description: 'IdentityClientId: Identity Client id of a AML
                          account resource.'
                        type: string
                      resourceReference:
                        description: 'ResourceReference: Full resource id of a AML
                          account resource.'
                        properties:
                          armId:
                            description: |-
                              ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                              The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                              ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                            pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                            type: string
                          group:
                            description: Group is the Kubernetes group of the resource.
                            type: string
                          kind:
                            description: Kind is the Kubernetes kind of the resource.
                            type: string
                          name:
                            description: Name is the Kubernetes name of the resource.
                            type: string
                        type: object
                    type: object
                  apiProperties:
                    description: 'ApiProperties: The api properties for special APIs.'
                    properties:
                      aadClientId:
                        description: 'AadClientId: (Metrics Advisor Only) The Azure
                          AD Client Id (Application Id).'
                        maxLength: 500
                        type: string
                      aadClientIdFromConfig:
                        description: 'AadClientIdFromConfig: (Metrics Advisor Only)
                          The Azure AD Client Id (Application Id).'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      aadTenantId:
                        description: 'AadTenantId: (Metrics Advisor Only) The Azure
                          AD Tenant Id.'
                        maxLength: 500
                        type: string
                      aadTenantIdFromConfig:
                        description: 'AadTenantIdFromConfig: (Metrics Advisor Only)
                          The Azure AD Tenant Id.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      additionalProperties:
                        additionalProperties:
                          x-kubernetes-preserve-unknown-fields: true
                        type: object
                      eventHubConnectionString:
                        description: 'EventHubConnectionString: (Personalization Only)
                          The flag to enable statistics of Bing Search.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      qnaAzureSearchEndpointId:
                        description: 'QnaAzureSearchEndpointId: (QnAMaker Only) The
                          Azure Search endpoint id of QnAMaker.'
                        type: string
                      qnaAzureSearchEndpointIdFromConfig:
                        description: 'QnaAzureSearchEndpointIdFromConfig: (QnAMaker
                          Only) The Azure Search endpoint id of QnAMaker.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      qnaAzureSearchEndpointKey:
                        description: 'QnaAzureSearchEndpointKey: (QnAMaker Only) The
                          Azure Search endpoint key of QnAMaker.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      qnaRuntimeEndpoint:
                        description: 'QnaRuntimeEndpoint: (QnAMaker Only) The runtime
                          endpoint of QnAMaker.'
                        type: string
                      qnaRuntimeEndpointFromConfig:
                        description: 'QnaRuntimeEndpointFromConfig: (QnAMaker Only)
                          The runtime endpoint of QnAMaker.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      statisticsEnabled:
                        description: 'StatisticsEnabled: (Bing Search Only) The flag
                          to enable statistics of Bing Search.'
                        type: boolean
                      storageAccountConnectionString:
                        description: 'StorageAccountConnectionString: (Personalization
                          Only) The storage account connection string.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      superUser:
                        description: 'SuperUser: (Metrics Advisor Only) The super
                          user of Metrics Advisor.'
                        maxLength: 500
                        type: string
                      superUserFromConfig:
                        description: 'SuperUserFromConfig: (Metrics Advisor Only)
                          The super user of Metrics Advisor.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      websiteName:
                        description: 'WebsiteName: (Metrics Advisor Only) The website
                          name of Metrics Advisor.'
                        maxLength: 500
                        type: string
                      websiteNameFromConfig:
                        description: 'WebsiteNameFromConfig: (Metrics Advisor Only)
                          The website name of Metrics Advisor.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  associatedProjects:
                    description: 'AssociatedProjects: Specifies the projects, by project
                      name, that are associated with this resource.'
                    items:
                      type: string
                    type: array
                  customSubDomainName:
                    description: 'CustomSubDomainName: Optional subdomain name used
                      for token-based authentication.'
                    type: string
                  defaultProject:
                    description: |-
                      DefaultProject: Specifies the project, by project name, that is targeted when data plane endpoints are called without a
                      project parameter.
                    type: string
                  disableLocalAuth:
                    type: boolean
                  dynamicThrottlingEnabled:
                    description: 'DynamicThrottlingEnabled: The flag to enable dynamic
                      throttling.'
                    type: boolean
                  encryption:
                    description: 'Encryption: The encryption properties for this resource.'
                    properties:
                      keySource:
                        description: 'KeySource: Enumerates the possible value of
                          keySource for Encryption'
                        enum:
                        - Microsoft.CognitiveServices
                        - Microsoft.KeyVault
                        type: string
                      keyVaultProperties:
                        description: 'KeyVaultProperties: Properties of KeyVault'
                        properties:
                          identityClientId:
                            type: string
                          keyName:
                            description: 'KeyName: Name of the Key from KeyVault'
                            type: string
                          keyVaultUri:
                            description: 'KeyVaultUri: Uri of KeyVault'
                            type: string
                          keyVersion:
                            description: 'KeyVersion: Version of the Key from KeyVault'
                            type: string
                        type: object
                    type: object
                  locations:
                    description: 'Locations: The multiregion settings of Cognitive
                      Services account.'
                    properties:
                      regions:
                        items:
                          description: The call rate limit Cognitive Services account.
                          properties:
                            customsubdomain:
                              description: 'Customsubdomain: Maps the region to the
                                regional custom subdomain.'
                              type: string
                            name:
                              description: 'Name: Name of the region.'
                              type: string
                            value:
                              description: 'Value: A value for priority or weighted
                                routing methods.'
                              type: number
                          type: object
                        type: array
                      routingMethod:
                        description: 'RoutingMethod: Multiregion routing methods.'
                        enum:
                        - Performance
                        - Priority
                        - Weighted
                        type: string
                    type: object
                  migrationToken:
                    description: 'MigrationToken: Resource migration token.'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  networkAcls:
                    description: 'NetworkAcls: A collection of rules governing the
                      accessibility from specific network locations.'
                    properties:
                      bypass:
                        description: 'Bypass: Setting for trusted services.'
                        enum:
                        - AzureServices
                        - None
                        type: string
                      defaultAction:
                        description: |-
                          DefaultAction: The default action when no rule from ipRules and from virtualNetworkRules match. This is only used after
                          the bypass property has been evaluated.
                        enum:
                        - Allow
                        - Deny
                        type: string
                      ipRules:
                        description: 'IpRules: The list of IP address rules.'
                        items:
                          description: A rule governing the accessibility from a specific
                            ip address or ip range.
                          properties:
                            value:
                              description: |-
                                Value: An IPv4 address range in CIDR notation, such as '124.56.78.91' (simple IP address) or '124.56.78.0/24' (all
                                addresses that start with 124.56.78).
                              type: string
                          required:
                          - value
                          type: object
                        type: array
                      virtualNetworkRules:
                        description: 'VirtualNetworkRules: The list of virtual network
                          rules.'
                        items:
                          description: A rule governing the accessibility from a specific
                            virtual network.
                          properties:
                            ignoreMissingVnetServiceEndpoint:
                              description: 'IgnoreMissingVnetServiceEndpoint: Ignore
                                missing vnet service endpoint or not.'
                              type: boolean
                            reference:
                              description: |-
                                Reference: Full resource id of a vnet subnet, such as
                                '/subscriptions/subid/resourceGroups/rg1/providers/Microsoft.Network/virtualNetworks/test-vnet/subnets/subnet1'.
                              properties:
                                armId:
                                  description: |-
                                    ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                    The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                    ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                                  pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                                  type: string
                                group:
                                  description: Group is the Kubernetes group of the
                                    resource.
                                  type: string
                                kind:
                                  description: Kind is the Kubernetes kind of the
                                    resource.
                                  type: string
                                name:
                                  description: Name is the Kubernetes name of the
                                    resource.
                                  type: string
                              type: object
                            state:
                              description: 'State: Gets the state of virtual network
                                rule.'
                              type: string
                          required:
                          - reference
                          type: object
                        type: array
                    type: object
                  networkInjections:
                    description: |-
                      NetworkInjections: Specifies in AI Foundry where virtual network injection occurs to secure scenarios like Agents
                      entirely within the  user's private network, eliminating public internet exposure while maintaining control over network
                      configurations and  resources.
                    properties:
                      scenario:
                        description: |-
                          Scenario: Specifies what features in AI Foundry network injection applies to. Currently only supports 'agent' for agent
                          scenarios. 'none' means no network injection.
                        enum:
                        - agent
                        - none
                        type: string
                      subnetArmReference:
                        description: 'SubnetArmReference: Specify the subnet for which
                          your Agent Client is injected into.'
                        properties:
                          armId:
                            description: |-
                              ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                              The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                              ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                            pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                            type: string
                          group:
                            description: Group is the Kubernetes group of the resource.
                            type: string
                          kind:
                            description: Kind is the Kubernetes kind of the resource.
                            type: string
                          name:
                            description: Name is the Kubernetes name of the resource.
                            type: string
                        type: object
                      useMicrosoftManagedNetwork:
                        description: 'UseMicrosoftManagedNetwork: Boolean to enable
                          Microsoft Managed Network for subnet delegation'
                        type: boolean
                    type: object
                  publicNetworkAccess:
                    description: 'PublicNetworkAccess: Whether or not public endpoint
                      access is allowed for this account.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                  raiMonitorConfig:
                    description: 'RaiMonitorConfig: Cognitive Services Rai Monitor
                      Config.'
                    properties:
                      adxStorageResourceReference:
                        description: 'AdxStorageResourceReference: The storage resource
                          Id.'
                        properties:
                          armId:
                            description: |-
                              ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                              The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                              ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                            pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                            type: string
                          group:
                            description: Group is the Kubernetes group of the resource.
                            type: string
                          kind:
                            description: Kind is the Kubernetes kind of the resource.
                            type: string
                          name:
                            description: Name is the Kubernetes name of the resource.
                            type: string
                        type: object
                      identityClientId:
                        description: 'IdentityClientId: The identity client Id to
                          access the storage.'
                        type: string
                    type: object
                  restore:
                    type: boolean
                  restrictOutboundNetworkAccess:
                    type: boolean
                  userOwnedStorage:
                    description: 'UserOwnedStorage: The storage accounts for this
                      resource.'
                    items:
                      description: The user owned storage for Cognitive Services account.
                      properties:
                        identityClientId:
                          type: string
                        resourceReference:
                          description: 'ResourceReference: Full resource id of a Microsoft.Storage
                            resource.'
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              sku:
                description: 'Sku: The resource model definition representing SKU'
                properties:
                  capacity:
                    description: |-
                      Capacity: If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible
                      for the resource this may be omitted.
                    type: integer
                  family:
                    description: 'Family: If the service has different generations
                      of hardware, for the same SKU, then that can be captured here.'
                    type: string
                  name:
                    description: 'Name: The name of the SKU. Ex - P3. It is typically
                      a letter+number code'
                    type: string
                  size:
                    description: |-
                      Size: The SKU size. When the name field is the combination of tier and some other value, this would be the standalone
                      code.
                    type: string
                  tier:
                    description: |-
                      Tier: This field is required to be implemented by the Resource Provider if the service has more than one tier, but is
                      not required on a PUT.
                    enum:
                    - Basic
                    - Enterprise
                    - Free
                    - Premium
                    - Standard
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
            required:
            - owner
            type: object
          status:
            description: Cognitive Services account is an Azure resource representing
              the provisioned account, it's type, location and SKU.
            properties:
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              etag:
                description: 'Etag: Resource Etag.'
                type: string
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. Ex -
                  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                type: string
              identity:
                description: 'Identity: Identity for the resource.'
                properties:
                  principalId:
                    description: 'PrincipalId: The principal ID of resource identity.'
                    type: string
                  tenantId:
                    description: 'TenantId: The tenant ID of resource.'
                    type: string
                  type:
                    description: 'Type: The identity type.'
                    type: string
                  userAssignedIdentities:
                    additionalProperties:
                      description: User-assigned managed identity.
                      properties:
                        clientId:
                          description: 'ClientId: Client App Id associated with this
                            identity.'
                          type: string
                        principalId:
                          description: 'PrincipalId: Azure Active Directory principal
                            ID associated with this Identity.'
                          type: string
                      type: object
                    description: |-
                      UserAssignedIdentities: The list of user assigned identities associated with the resource. The user identity dictionary
                      key references will be ARM resource ids in the form:
                      '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}
                    type: object
                type: object
              kind:
                description: 'Kind: The Kind of the resource.'
                type: string
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              name:
                description: 'Name: The name of the resource'
                type: string
              properties:
                description: 'Properties: Properties of Cognitive Services account.'
                properties:
                  abusePenalty:
                    description: 'AbusePenalty: The abuse penalty.'
                    properties:
                      action:
                        description: 'Action: The action of AbusePenalty.'
                        type: string
                      expiration:
                        description: 'Expiration: The datetime of expiration of the
                          AbusePenalty.'
                        type: string
                      rateLimitPercentage:
                        description: 'RateLimitPercentage: The percentage of rate
                          limit.'
                        type: number
                    type: object
                  allowProjectManagement:
                    description: |-
                      AllowProjectManagement: Specifies whether this resource support project management as child resources, used as
                      containers for access management, data isolation and cost in AI Foundry.
                    type: boolean
                  allowedFqdnList:
                    items:
                      type: string
                    type: array
                  amlWorkspace:
                    description: 'AmlWorkspace: The user owned AML account properties.'
                    properties:
                      identityClientId:
                        description: 'IdentityClientId: Identity Client id of a AML
                          account resource.'
                        type: string
                      resourceId:
                        description: 'ResourceId: Full resource id of a AML account
                          resource.'
                        type: string
                    type: object
                  apiProperties:
                    description: 'ApiProperties: The api properties for special APIs.'
                    properties:
                      aadClientId:
                        description: 'AadClientId: (Metrics Advisor Only) The Azure
                          AD Client Id (Application Id).'
                        type: string
                      aadTenantId:
                        description: 'AadTenantId: (Metrics Advisor Only) The Azure
                          AD Tenant Id.'
                        type: string
                      additionalProperties:
                        additionalProperties:
                          x-kubernetes-preserve-unknown-fields: true
                        type: object
                      qnaAzureSearchEndpointId:
                        description: 'QnaAzureSearchEndpointId: (QnAMaker Only) The
                          Azure Search endpoint id of QnAMaker.'
                        type: string
                      qnaRuntimeEndpoint:
                        description: 'QnaRuntimeEndpoint: (QnAMaker Only) The runtime
                          endpoint of QnAMaker.'
                        type: string
                      statisticsEnabled:
                        description: 'StatisticsEnabled: (Bing Search Only) The flag
                          to enable statistics of Bing Search.'
                        type: boolean
                      superUser:
                        description: 'SuperUser: (Metrics Advisor Only) The super
                          user of Metrics Advisor.'
                        type: string
                      websiteName:
                        description: 'WebsiteName: (Metrics Advisor Only) The website
                          name of Metrics Advisor.'
                        type: string
                    type: object
                  associatedProjects:
                    description: 'AssociatedProjects: Specifies the projects, by project
                      name, that are associated with this resource.'
                    items:
                      type: string
                    type: array
                  callRateLimit:
                    description: 'CallRateLimit: The call rate limit Cognitive Services
                      account.'
                    properties:
                      count:
                        description: 'Count: The count value of Call Rate Limit.'
                        type: number
                      renewalPeriod:
                        description: 'RenewalPeriod: The renewal period in seconds
                          of Call Rate Limit.'
                        type: number
                      rules:
                        items:
                          properties:
                            count:
                              type: number
                            dynamicThrottlingEnabled:
                              type: boolean
                            key:
                              type: string
                            matchPatterns:
                              items:
                                properties:
                                  method:
                                    type: string
                                  path:
                                    type: string
                                type: object
                              type: array
                            minCount:
                              type: number
                            renewalPeriod:
                              type: number
                          type: object
                        type: array
                    type: object
                  capabilities:
                    description: |-
                      Capabilities: Gets the capabilities of the cognitive services account. Each item indicates the capability of a specific
                      feature. The values are read-only and for reference only.
                    items:
                      description: SkuCapability indicates the capability of a certain
                        feature.
                      properties:
                        name:
                          description: 'Name: The name of the SkuCapability.'
                          type: string
                        value:
                          description: 'Value: The value of the SkuCapability.'
                          type: string
                      type: object
                    type: array
                  commitmentPlanAssociations:
                    description: 'CommitmentPlanAssociations: The commitment plan
                      associations of Cognitive Services account.'
                    items:
                      description: The commitment plan association.
                      properties:
                        commitmentPlanId:
                          description: 'CommitmentPlanId: The Azure resource id of
                            the commitment plan.'
                          type: string
                        commitmentPlanLocation:
                          description: 'CommitmentPlanLocation: The location of of
                            the commitment plan.'
                          type: string
                      type: object
                    type: array
                  customSubDomainName:
                    description: 'CustomSubDomainName: Optional subdomain name used
                      for token-based authentication.'
                    type: string
                  dateCreated:
                    description: 'DateCreated: Gets the date of cognitive services
                      account creation.'
                    type: string
                  defaultProject:
                    description: |-
                      DefaultProject: Specifies the project, by project name, that is targeted when data plane endpoints are called without a
                      project parameter.
                    type: string
                  deletionDate:
                    description: 'DeletionDate: The deletion date, only available
                      for deleted account.'
                    type: string
                  disableLocalAuth:
                    type: boolean
                  dynamicThrottlingEnabled:
                    description: 'DynamicThrottlingEnabled: The flag to enable dynamic
                      throttling.'
                    type: boolean
                  encryption:
                    description: 'Encryption: The encryption properties for this resource.'
                    properties:
                      keySource:
                        description: 'KeySource: Enumerates the possible value of
                          keySource for Encryption'
                        type: string
                      keyVaultProperties:
                        description: 'KeyVaultProperties: Properties of KeyVault'
                        properties:
                          identityClientId:
                            type: string
                          keyName:
                            description: 'KeyName: Name of the Key from KeyVault'
                            type: string
                          keyVaultUri:
                            description: 'KeyVaultUri: Uri of KeyVault'
                            type: string
                          keyVersion:
                            description: 'KeyVersion: Version of the Key from KeyVault'
                            type: string
                        type: object
                    type: object
                  endpoint:
                    description: 'Endpoint: Endpoint of the created account.'
                    type: string
                  endpoints:
                    additionalProperties:
                      type: string
                    type: object
                  internalId:
                    description: 'InternalId: The internal identifier (deprecated,
                      do not use this property).'
                    type: string
                  isMigrated:
                    description: 'IsMigrated: If the resource is migrated from an
                      existing key.'
                    type: boolean
                  locations:
                    description: 'Locations: The multiregion settings of Cognitive
                      Services account.'
                    properties:
                      regions:
                        items:
                          description: The call rate limit Cognitive Services account.
                          properties:
                            customsubdomain:
                              description: 'Customsubdomain: Maps the region to the
                                regional custom subdomain.'
                              type: string
                            name:
                              description: 'Name: Name of the region.'
                              type: string
                            value:
                              description: 'Value: A value for priority or weighted
                                routing methods.'
                              type: number
                          type: object
                        type: array
                      routingMethod:
                        description: 'RoutingMethod: Multiregion routing methods.'
                        type: string
                    type: object
                  networkAcls:
                    description: 'NetworkAcls: A collection of rules governing the
                      accessibility from specific network locations.'
                    properties:
                      bypass:
                        description: 'Bypass: Setting for trusted services.'
                        type: string
                      defaultAction:
                        description: |-
                          DefaultAction: The default action when no rule from ipRules and from virtualNetworkRules match. This is only used after
                          the bypass property has been evaluated.
                        type: string
                      ipRules:
                        description: 'IpRules: The list of IP address rules.'
                        items:
                          description: A rule governing the accessibility from a specific
                            ip address or ip range.
                          properties:
                            value:
                              description: |-
                                Value: An IPv4 address range in CIDR notation, such as '124.56.78.91' (simple IP address) or '124.56.78.0/24' (all
                                addresses that start with 124.56.78).
                              type: string
                          type: object
                        type: array
                      virtualNetworkRules:
                        description: 'VirtualNetworkRules: The list of virtual network
                          rules.'
                        items:
                          description: A rule governing the accessibility from a specific
                            virtual network.
                          properties:
                            id:
                              description: |-
                                Id: Full resource id of a vnet subnet, such as
                                '/subscriptions/subid/resourceGroups/rg1/providers/Microsoft.Network/virtualNetworks/test-vnet/subnets/subnet1'.
                              type: string
                            ignoreMissingVnetServiceEndpoint:
                              description: 'IgnoreMissingVnetServiceEndpoint: Ignore
                                missing vnet service endpoint or not.'
                              type: boolean
                            state:
                              description: 'State: Gets the state of virtual network
                                rule.'
                              type: string
                          type: object
                        type: array
                    type: object
                  networkInjections:
                    description: |-
                      NetworkInjections: Specifies in AI Foundry where virtual network injection occurs to secure scenarios like Agents
                      entirely within the  user's private network, eliminating public internet exposure while maintaining control over network
                      configurations and  resources.
                    properties:
                      scenario:
                        description: |-
                          Scenario: Specifies what features in AI Foundry network injection applies to. Currently only supports 'agent' for agent
                          scenarios. 'none' means no network injection.
                        type: string
                      subnetArmId:
                        description: 'SubnetArmId: Specify the subnet for which your
                          Agent Client is injected into.'
                        type: string
                      useMicrosoftManagedNetwork:
                        description: 'UseMicrosoftManagedNetwork: Boolean to enable
                          Microsoft Managed Network for subnet delegation'
                        type: boolean
                    type: object
                  privateEndpointConnections:
                    description: 'PrivateEndpointConnections: The private endpoint
                      connection associated with the Cognitive Services account.'
                    items:
                      description: The Private Endpoint Connection resource.
                      properties:
                        id:
                          description: |-
                            Id: Fully qualified resource ID for the resource. Ex -
                            /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                          type: string
                      type: object
                    type: array
                  provisioningState:
                    description: 'ProvisioningState: Gets the status of the cognitive
                      services account at the time the operation was called.'
                    type: string
                  publicNetworkAccess:
                    description: 'PublicNetworkAccess: Whether or not public endpoint
                      access is allowed for this account.'
                    type: string
                  quotaLimit:
                    properties:
                      count:
                        type: number
                      renewalPeriod:
                        type: number
                      rules:
                        items:
                          properties:
                            count:
                              type: number
                            dynamicThrottlingEnabled:
                              type: boolean
                            key:
                              type: string
                            matchPatterns:
                              items:
                                properties:
                                  method:
                                    type: string
                                  path:
                                    type: string
                                type: object
                              type: array
                            minCount:
                              type: number
                            renewalPeriod:
                              type: number
                          type: object
                        type: array
                    type: object
                  raiMonitorConfig:
                    description: 'RaiMonitorConfig: Cognitive Services Rai Monitor
                      Config.'
                    properties:
                      adxStorageResourceId:
                        description: 'AdxStorageResourceId: The storage resource Id.'
                        type: string
                      identityClientId:
                        description: 'IdentityClientId: The identity client Id to
                          access the storage.'
                        type: string
                    type: object
                  restore:
                    type: boolean
                  restrictOutboundNetworkAccess:
                    type: boolean
                  scheduledPurgeDate:
                    description: 'ScheduledPurgeDate: The scheduled purge date, only
                      available for deleted account.'
                    type: string
                  skuChangeInfo:
                    description: 'SkuChangeInfo: Sku change info of account.'
                    properties:
                      countOfDowngrades:
                        description: 'CountOfDowngrades: Gets the count of downgrades.'
                        type: number
                      countOfUpgradesAfterDowngrades:
                        description: 'CountOfUpgradesAfterDowngrades: Gets the count
                          of upgrades after downgrades.'
                        type: number
                      lastChangeDate:
                        description: 'LastChangeDate: Gets the last change date.'
                        type: string
                    type: object
                  userOwnedStorage:
                    description: 'UserOwnedStorage: The storage accounts for this
                      resource.'
                    items:
                      description: The user owned storage for Cognitive Services account.
                      properties:
                        identityClientId:
                          type: string
                        resourceId:
                          description: 'ResourceId: Full resource id of a Microsoft.Storage
                            resource.'
                          type: string
                      type: object
                    type: array
                type: object
              sku:
                description: 'Sku: The resource model definition representing SKU'
                properties:
                  capacity:
                    description: |-
                      Capacity: If the SKU supports scale out/in then the capacity integer should be included. If scale out/in is not possible
                      for the resource this may be omitted.
                    type: integer
                  family:
                    description: 'Family: If the service has different generations
                      of hardware, for the same SKU, then that can be captured here.'
                    type: string
                  name:
                    description: 'Name: The name of the SKU. Ex - P3. It is typically
                      a letter+number code'
                    type: string
                  size:
                    description: |-
                      Size: The SKU size. When the name field is the combination of tier and some other value, this would be the standalone
                      code.
                    type: string
                  tier:
                    description: |-
                      Tier: This field is required to be implemented by the Resource Provider if the service has more than one tier, but is
                      not required on a PUT.
                    type: string
                type: object
              systemData:
                description: 'SystemData: Metadata pertaining to creation and last
                  modification of the resource.'
                properties:
                  createdAt:
                    description: 'CreatedAt: The timestamp of resource creation (UTC).'
                    type: string
                  createdBy:
                    description: 'CreatedBy: The identity that created the resource.'
                    type: string
                  createdByType:
                    description: 'CreatedByType: The type of identity that created
                      the resource.'
                    type: string
                  lastModifiedAt:
                    description: 'LastModifiedAt: The timestamp of resource last modification
                      (UTC)'
                    type: string
                  lastModifiedBy:
                    description: 'LastModifiedBy: The identity that last modified
                      the resource.'
                    type: string
                  lastModifiedByType:
                    description: 'LastModifiedByType: The type of identity that last
                      modified the resource.'
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20250601storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20250601.Account
          Generator information:
          - Generated from: /cognitiveservices/resource-manager/Microsoft.CognitiveServices/stable/2025-06-01/cognitiveservices.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.CognitiveServices/accounts/{accountName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20250601.Account_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              identity:
                description: |-
                  Storage version of v1api20250601.Identity
                  Identity for the resource.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  type:
                    type: string
                  userAssignedIdentities:
                    items:
                      description: |-
                        Storage version of v1api20250601.UserAssignedIdentityDetails
                        Information about the user assigned identity for the resource
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        reference:
                          description: ResourceReference represents a resource reference,
                            either to a Kubernetes resource or directly to an Azure
                            resource via ARMID
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              kind:
                type: string
              location:
                type: string
              operatorSpec:
                description: |-
                  Storage version of v1api20250601.AccountOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: Storage version of v1api20250601.AccountOperatorSecrets
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      key1:
                        description: |-
                          SecretDestination describes the location to store a single secret value.
                          Note: This is similar to: ConfigMapDestination in configmaps.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      key2:
                        description: |-
                          SecretDestination describes the location to store a single secret value.
                          Note: This is similar to: ConfigMapDestination in configmaps.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              properties:
                description: |-
                  Storage version of v1api20250601.AccountProperties
                  Properties of Cognitive Services account.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  allowProjectManagement:
                    type: boolean
                  allowedFqdnList:
                    items:
                      type: string
                    type: array
                  amlWorkspace:
                    description: |-
                      Storage version of v1api20250601.UserOwnedAmlWorkspace
                      The user owned AML account for Cognitive Services account.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      identityClientId:
                        type: string
                      resourceReference:
                        description: 'ResourceReference: Full resource id of a AML
                          account resource.'
                        properties:
                          armId:
                            description: |-
                              ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                              The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                              ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                            pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                            type: string
                          group:
                            description: Group is the Kubernetes group of the resource.
                            type: string
                          kind:
                            description: Kind is the Kubernetes kind of the resource.
                            type: string
                          name:
                            description: Name is the Kubernetes name of the resource.
                            type: string
                        type: object
                    type: object
                  apiProperties:
                    description: |-
                      Storage version of v1api20250601.ApiProperties
                      The api properties for special APIs.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      aadClientId:
                        type: string
                      aadClientIdFromConfig:
                        description: |-
                          ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      aadTenantId:
                        type: string
                      aadTenantIdFromConfig:
                        description: |-
                          ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      additionalProperties:
                        additionalProperties:
                          x-kubernetes-preserve-unknown-fields: true
                        type: object
                      eventHubConnectionString:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      qnaAzureSearchEndpointId:
                        type: string
                      qnaAzureSearchEndpointIdFromConfig:
                        description: |-
                          ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      qnaAzureSearchEndpointKey:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      qnaRuntimeEndpoint:
                        type: string
                      qnaRuntimeEndpointFromConfig:
                        description: |-
                          ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      statisticsEnabled:
                        type: boolean
                      storageAccountConnectionString:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      superUser:
                        type: string
                      superUserFromConfig:
                        description: |-
                          ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      websiteName:
                        type: string
                      websiteNameFromConfig:
                        description: |-
                          ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes configmap
                              being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes configmap being referenced.
                              The configmap must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  associatedProjects:
                    items:
                      type: string
                    type: array
                  customSubDomainName:
                    type: string
                  defaultProject:
                    type: string
                  disableLocalAuth:
                    type: boolean
                  dynamicThrottlingEnabled:
                    type: boolean
                  encryption:
                    description: |-
                      Storage version of v1api20250601.Encryption
                      Properties to configure Encryption
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      keySource:
                        type: string
                      keyVaultProperties:
                        description: |-
                          Storage version of v1api20250601.KeyVaultProperties
                          Properties to configure keyVault Properties
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          identityClientId:
                            type: string
                          keyName:
                            type: string
                          keyVaultUri:
                            type: string
                          keyVersion:
                            type: string
                        type: object
                    type: object
                  locations:
                    description: |-
                      Storage version of v1api20250601.MultiRegionSettings
                      The multiregion settings Cognitive Services account.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      regions:
                        items:
                          description: |-
                            Storage version of v1api20250601.RegionSetting
                            The call rate limit Cognitive Services account.
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            customsubdomain:
                              type: string
                            name:
                              type: string
                            value:
                              type: number
                          type: object
                        type: array
                      routingMethod:
                        type: string
                    type: object
                  migrationToken:
                    description: |-
                      SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  networkAcls:
                    description: |-
                      Storage version of v1api20250601.NetworkRuleSet
                      A set of rules governing the network accessibility.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      bypass:
                        type: string
                      defaultAction:
                        type: string
                      ipRules:
                        items:
                          description: |-
                            Storage version of v1api20250601.IpRule
                            A rule governing the accessibility from a specific ip address or ip range.
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            value:
                              type: string
                          type: object
                        type: array
                      virtualNetworkRules:
                        items:
                          description: |-
                            Storage version of v1api20250601.VirtualNetworkRule
                            A rule governing the accessibility from a specific virtual network.
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            ignoreMissingVnetServiceEndpoint:
                              type: boolean
                            reference:
                              description: |-
                                Reference: Full resource id of a vnet subnet, such as
                                '/subscriptions/subid/resourceGroups/rg1/providers/Microsoft.Network/virtualNetworks/test-vnet/subnets/subnet1'.
                              properties:
                                armId:
                                  description: |-
                                    ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                    The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                    ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                                  pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                                  type: string
                                group:
                                  description: Group is the Kubernetes group of the
                                    resource.
                                  type: string
                                kind:
                                  description: Kind is the Kubernetes kind of the
                                    resource.
                                  type: string
                                name:
                                  description: Name is the Kubernetes name of the
                                    resource.
                                  type: string
                              type: object
                            state:
                              type: string
                          required:
                          - reference
                          type: object
                        type: array
                    type: object
                  networkInjections:
                    description: |-
                      Storage version of v1api20250601.NetworkInjections
                      Specifies in AI Foundry where virtual network injection occurs to secure scenarios like Agents entirely within the
                      user's private network, eliminating public internet exposure while maintaining control over network configurations and
                      resources.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      scenario:
                        type: string
                      subnetArmReference:
                        description: 'SubnetArmReference: Specify the subnet for which
                          your Agent Client is injected into.'
                        properties:
                          armId:
                            description: |-
                              ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                              The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                              ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                            pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                            type: string
                          group:
                            description: Group is the Kubernetes group of the resource.
                            type: string
                          kind:
                            description: Kind is the Kubernetes kind of the resource.
                            type: string
                          name:
                            description: Name is the Kubernetes name of the resource.
                            type: string
                        type: object
                      useMicrosoftManagedNetwork:
                        type: boolean
                    type: object
                  publicNetworkAccess:
                    type: string
                  raiMonitorConfig:
                    description: |-
                      Storage version of v1api20250601.RaiMonitorConfig
                      Cognitive Services Rai Monitor Config.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      adxStorageResourceReference:
                        description: 'AdxStorageResourceReference: The storage resource
                          Id.'
                        properties:
                          armId:
                            description: |-
                              ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                              The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                              ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                            pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                            type: string
                          group:
                            description: Group is the Kubernetes group of the resource.
                            type: string
                          kind:
                            description: Kind is the Kubernetes kind of the resource.
                            type: string
                          name:
                            description: Name is the Kubernetes name of the resource.
                            type: string
                        type: object
                      identityClientId:
                        type: string
                    type: object
                  restore:
                    type: boolean
                  restrictOutboundNetworkAccess:
                    type: boolean
                  userOwnedStorage:
                    items:
                      description: |-
                        Storage version of v1api20250601.UserOwnedStorage
                        The user owned storage for Cognitive Services account.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        identityClientId:
                          type: string
                        resourceReference:
                          description: 'ResourceReference: Full resource id of a Microsoft.Storage
                            resource.'
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              sku:
                description: |-
                  Storage version of v1api20250601.Sku
                  The resource model definition representing SKU
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  capacity:
                    type: integer
                  family:
                    type: string
                  name:
                    type: string
                  size:
                    type: string
                  tier:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - owner
            type: object
          status:
            description: |-
              Storage version of v1api20250601.Account_STATUS
              Cognitive Services account is an Azure resource representing the provisioned account, it's type, location and SKU.
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              etag:
                type: string
              id:
                type: string
              identity:
                description: |-
                  Storage version of v1api20250601.Identity_STATUS
                  Identity for the resource.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  principalId:
                    type: string
                  tenantId:
                    type: string
                  type:
                    type: string
                  userAssignedIdentities:
                    additionalProperties:
                      description: |-
                        Storage version of v1api20250601.UserAssignedIdentity_STATUS
                        User-assigned managed identity.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        clientId:
                          type: string
                        principalId:
                          type: string
                      type: object
                    type: object
                type: object
              kind:
                type: string
              location:
                type: string
              name:
                type: string
              properties:
                description: |-
                  Storage version of v1api20250601.AccountProperties_STATUS
                  Properties of Cognitive Services account.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  abusePenalty:
                    description: |-
                      Storage version of v1api20250601.AbusePenalty_STATUS
                      The abuse penalty.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      action:
                        type: string
                      expiration:
                        type: string
                      rateLimitPercentage:
                        type: number
                    type: object
                  allowProjectManagement:
                    type: boolean
                  allowedFqdnList:
                    items:
                      type: string
                    type: array
                  amlWorkspace:
                    description: |-
                      Storage version of v1api20250601.UserOwnedAmlWorkspace_STATUS
                      The user owned AML account for Cognitive Services account.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      identityClientId:
                        type: string
                      resourceId:
                        type: string
                    type: object
                  apiProperties:
                    description: |-
                      Storage version of v1api20250601.ApiProperties_STATUS
                      The api properties for special APIs.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      aadClientId:
                        type: string
                      aadTenantId:
                        type: string
                      additionalProperties:
                        additionalProperties:
                          x-kubernetes-preserve-unknown-fields: true
                        type: object
                      qnaAzureSearchEndpointId:
                        type: string
                      qnaRuntimeEndpoint:
                        type: string
                      statisticsEnabled:
                        type: boolean
                      superUser:
                        type: string
                      websiteName:
                        type: string
                    type: object
                  associatedProjects:
                    items:
                      type: string
                    type: array
                  callRateLimit:
                    description: |-
                      Storage version of v1api20250601.CallRateLimit_STATUS
                      The call rate limit Cognitive Services account.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      count:
                        type: number
                      renewalPeriod:
                        type: number
                      rules:
                        items:
                          description: Storage version of v1api20250601.ThrottlingRule_STATUS
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            count:
                              type: number
                            dynamicThrottlingEnabled:
                              type: boolean
                            key:
                              type: string
                            matchPatterns:
                              items:
                                description: Storage version of v1api20250601.RequestMatchPattern_STATUS
                                properties:
                                  $propertyBag:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                      resources, allowing for full fidelity round trip conversions
                                    type: object
                                  method:
                                    type: string
                                  path:
                                    type: string
                                type: object
                              type: array
                            minCount:
                              type: number
                            renewalPeriod:
                              type: number
                          type: object
                        type: array
                    type: object
                  capabilities:
                    items:
                      description: |-
                        Storage version of v1api20250601.SkuCapability_STATUS
                        SkuCapability indicates the capability of a certain feature.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        name:
                          type: string
                        value:
                          type: string
                      type: object
                    type: array
                  commitmentPlanAssociations:
                    items:
                      description: |-
                        Storage version of v1api20250601.CommitmentPlanAssociation_STATUS
                        The commitment plan association.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        commitmentPlanId:
                          type: string
                        commitmentPlanLocation:
                          type: string
                      type: object
                    type: array
                  customSubDomainName:
                    type: string
                  dateCreated:
                    type: string
                  defaultProject:
                    type: string
                  deletionDate:
                    type: string
                  disableLocalAuth:
                    type: boolean
                  dynamicThrottlingEnabled:
                    type: boolean
                  encryption:
                    description: |-
                      Storage version of v1api20250601.Encryption_STATUS
                      Properties to configure Encryption
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      keySource:
                        type: string
                      keyVaultProperties:
                        description: |-
                          Storage version of v1api20250601.KeyVaultProperties_STATUS
                          Properties to configure keyVault Properties
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          identityClientId:
                            type: string
                          keyName:
                            type: string
                          keyVaultUri:
                            type: string
                          keyVersion:
                            type: string
                        type: object
                    type: object
                  endpoint:
                    type: string
                  endpoints:
                    additionalProperties:
                      type: string
                    type: object
                  internalId:
                    type: string
                  isMigrated:
                    type: boolean
                  locations:
                    description: |-
                      Storage version of v1api20250601.MultiRegionSettings_STATUS
                      The multiregion settings Cognitive Services account.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      regions:
                        items:
                          description: |-
                            Storage version of v1api20250601.RegionSetting_STATUS
                            The call rate limit Cognitive Services account.
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            customsubdomain:
                              type: string
                            name:
                              type: string
                            value:
                              type: number
                          type: object
                        type: array
                      routingMethod:
                        type: string
                    type: object
                  networkAcls:
                    description: |-
                      Storage version of v1api20250601.NetworkRuleSet_STATUS
                      A set of rules governing the network accessibility.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      bypass:
                        type: string
                      defaultAction:
                        type: string
                      ipRules:
                        items:
                          description: |-
                            Storage version of v1api20250601.IpRule_STATUS
                            A rule governing the accessibility from a specific ip address or ip range.
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            value:
                              type: string
                          type: object
                        type: array
                      virtualNetworkRules:
                        items:
                          description: |-
                            Storage version of v1api20250601.VirtualNetworkRule_STATUS
                            A rule governing the accessibility from a specific virtual network.
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            id:
                              type: string
                            ignoreMissingVnetServiceEndpoint:
                              type: boolean
                            state:
                              type: string
                          type: object
                        type: array
                    type: object
                  networkInjections:
                    description: |-
                      Storage version of v1api20250601.NetworkInjections_STATUS
                      Specifies in AI Foundry where virtual network injection occurs to secure scenarios like Agents entirely within the
                      user's private network, eliminating public internet exposure while maintaining control over network configurations and
                      resources.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      scenario:
                        type: string
                      subnetArmId:
                        type: string
                      useMicrosoftManagedNetwork:
                        type: boolean
                    type: object
                  privateEndpointConnections:
                    items:
                      description: |-
                        Storage version of v1api20250601.PrivateEndpointConnection_STATUS
                        The Private Endpoint Connection resource.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        id:
                          type: string
                      type: object
                    type: array
                  provisioningState:
                    type: string
                  publicNetworkAccess:
                    type: string
                  quotaLimit:
                    description: Storage version of v1api20250601.QuotaLimit_STATUS
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      count:
                        type: number
                      renewalPeriod:
                        type: number
                      rules:
                        items:
                          description: Storage version of v1api20250601.ThrottlingRule_STATUS
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            count:
                              type: number
                            dynamicThrottlingEnabled:
                              type: boolean
                            key:
                              type: string
                            matchPatterns:
                              items:
                                description: Storage version of v1api20250601.RequestMatchPattern_STATUS
                                properties:
                                  $propertyBag:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                      resources, allowing for full fidelity round trip conversions
                                    type: object
                                  method:
                                    type: string
                                  path:
                                    type: string
                                type: object
                              type: array
                            minCount:
                              type: number
                            renewalPeriod:
                              type: number
                          type: object
                        type: array
                    type: object
                  raiMonitorConfig:
                    description: |-
                      Storage version of v1api20250601.RaiMonitorConfig_STATUS
                      Cognitive Services Rai Monitor Config.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      adxStorageResourceId:
                        type: string
                      identityClientId:
                        type: string
                    type: object
                  restore:
                    type: boolean
                  restrictOutboundNetworkAccess:
                    type: boolean
                  scheduledPurgeDate:
                    type: string
                  skuChangeInfo:
                    description: |-
                      Storage version of v1api20250601.SkuChangeInfo_STATUS
                      Sku change info of account.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      countOfDowngrades:
                        type: number
                      countOfUpgradesAfterDowngrades:
                        type: number
                      lastChangeDate:
                        type: string
                    type: object
                  userOwnedStorage:
                    items:
                      description: |-
                        Storage version of v1api20250601.UserOwnedStorage_STATUS
                        The user owned storage for Cognitive Services account.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        identityClientId:
                          type: string
                        resourceId:
                          type: string
                      type: object
                    type: array
                type: object
              sku:
                description: |-
                  Storage version of v1api20250601.Sku_STATUS
                  The resource model definition representing SKU
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  capacity:
                    type: integer
                  family:
                    type: string
                  name:
                    type: string
                  size:
                    type: string
                  tier:
                    type: string
                type: object
              systemData:
                description: |-
                  Storage version of v1api20250601.SystemData_STATUS
                  Metadata pertaining to creation and last modification of the resource.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  createdAt:
                    type: string
                  createdBy:
                    type: string
                  createdByType:
                    type: string
                  lastModifiedAt:
                    type: string
                  lastModifiedBy:
                    type: string
                  lastModifiedByType:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              type:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
