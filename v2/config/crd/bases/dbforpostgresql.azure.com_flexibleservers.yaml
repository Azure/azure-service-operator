---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: flexibleservers.dbforpostgresql.azure.com
spec:
  group: dbforpostgresql.azure.com
  names:
    kind: FlexibleServer
    listKind: FlexibleServerList
    plural: flexibleservers
    singular: flexibleserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20210601
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /postgresql/resource-manager/Microsoft.DBforPostgreSQL/stable/2021-06-01/postgresql.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DBforPostgreSQL/flexibleServers/{serverName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              administratorLogin:
                description: |-
                  AdministratorLogin: The administrator's login name of a server. Can only be specified when the server is being created
                  (and is required for creation).
                type: string
              administratorLoginPassword:
                description: 'AdministratorLoginPassword: The administrator login
                  password (required for server creation).'
                properties:
                  key:
                    description: Key is the key in the Kubernetes secret being referenced
                    type: string
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - key
                - name
                type: object
              availabilityZone:
                description: 'AvailabilityZone: availability zone information of the
                  server.'
                type: string
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              backup:
                description: 'Backup: Backup properties of a server.'
                properties:
                  backupRetentionDays:
                    description: 'BackupRetentionDays: Backup retention days for the
                      server.'
                    type: integer
                  geoRedundantBackup:
                    description: 'GeoRedundantBackup: A value indicating whether Geo-Redundant
                      backup is enabled on the server.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                type: object
              createMode:
                description: 'CreateMode: The mode to create a new PostgreSQL server.'
                enum:
                - Create
                - Default
                - PointInTimeRestore
                - Update
                type: string
              highAvailability:
                description: 'HighAvailability: High availability properties of a
                  server.'
                properties:
                  mode:
                    description: 'Mode: The HA mode for the server.'
                    enum:
                    - Disabled
                    - ZoneRedundant
                    type: string
                  standbyAvailabilityZone:
                    description: 'StandbyAvailabilityZone: availability zone information
                      of the standby.'
                    type: string
                type: object
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              maintenanceWindow:
                description: 'MaintenanceWindow: Maintenance window properties of
                  a server.'
                properties:
                  customWindow:
                    description: 'CustomWindow: indicates whether custom window is
                      enabled or disabled'
                    type: string
                  dayOfWeek:
                    description: 'DayOfWeek: day of week for maintenance window'
                    type: integer
                  startHour:
                    description: 'StartHour: start hour for maintenance window'
                    type: integer
                  startMinute:
                    description: 'StartMinute: start minute for maintenance window'
                    type: integer
                type: object
              network:
                description: 'Network: Network properties of a server.'
                properties:
                  delegatedSubnetResourceReference:
                    description: 'DelegatedSubnetResourceReference: delegated subnet
                      arm resource id.'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  privateDnsZoneArmResourceReference:
                    description: 'PrivateDnsZoneArmResourceReference: private dns
                      zone arm resource id.'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                type: object
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: 'Secrets: configures where to place Azure generated
                      secrets.'
                    properties:
                      fullyQualifiedDomainName:
                        description: |-
                          FullyQualifiedDomainName: indicates where the FullyQualifiedDomainName secret should be placed. If omitted, the secret
                          will not be retrieved from Azure.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              pointInTimeUTC:
                description: |-
                  PointInTimeUTC: Restore point creation time (ISO8601 format), specifying the time to restore from. It's required when
                  'createMode' is 'PointInTimeRestore'.
                type: string
              sku:
                description: 'Sku: The SKU (pricing tier) of the server.'
                properties:
                  name:
                    description: 'Name: The name of the sku, typically, tier + family
                      + cores, e.g. Standard_D4s_v3.'
                    type: string
                  tier:
                    description: 'Tier: The tier of the particular SKU, e.g. Burstable.'
                    enum:
                    - Burstable
                    - GeneralPurpose
                    - MemoryOptimized
                    type: string
                required:
                - name
                - tier
                type: object
              sourceServerResourceReference:
                description: |-
                  SourceServerResourceReference: The source server resource ID to restore from. It's required when 'createMode' is
                  'PointInTimeRestore'.
                properties:
                  armId:
                    description: |-
                      ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                      The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                      ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: Name is the Kubernetes name of the resource.
                    type: string
                type: object
              storage:
                description: 'Storage: Storage properties of a server.'
                properties:
                  storageSizeGB:
                    description: 'StorageSizeGB: Max storage allowed for a server.'
                    type: integer
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              version:
                description: 'Version: PostgreSQL Server version.'
                enum:
                - "11"
                - "12"
                - "13"
                - "14"
                type: string
            required:
            - location
            - owner
            type: object
          status:
            properties:
              administratorLogin:
                description: |-
                  AdministratorLogin: The administrator's login name of a server. Can only be specified when the server is being created
                  (and is required for creation).
                type: string
              availabilityZone:
                description: 'AvailabilityZone: availability zone information of the
                  server.'
                type: string
              backup:
                description: 'Backup: Backup properties of a server.'
                properties:
                  backupRetentionDays:
                    description: 'BackupRetentionDays: Backup retention days for the
                      server.'
                    type: integer
                  earliestRestoreDate:
                    description: 'EarliestRestoreDate: The earliest restore point
                      time (ISO8601 format) for server.'
                    type: string
                  geoRedundantBackup:
                    description: 'GeoRedundantBackup: A value indicating whether Geo-Redundant
                      backup is enabled on the server.'
                    type: string
                type: object
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createMode:
                description: 'CreateMode: The mode to create a new PostgreSQL server.'
                type: string
              fullyQualifiedDomainName:
                description: 'FullyQualifiedDomainName: The fully qualified domain
                  name of a server.'
                type: string
              highAvailability:
                description: 'HighAvailability: High availability properties of a
                  server.'
                properties:
                  mode:
                    description: 'Mode: The HA mode for the server.'
                    type: string
                  standbyAvailabilityZone:
                    description: 'StandbyAvailabilityZone: availability zone information
                      of the standby.'
                    type: string
                  state:
                    description: 'State: A state of a HA server that is visible to
                      user.'
                    type: string
                type: object
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. Ex -
                  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                type: string
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              maintenanceWindow:
                description: 'MaintenanceWindow: Maintenance window properties of
                  a server.'
                properties:
                  customWindow:
                    description: 'CustomWindow: indicates whether custom window is
                      enabled or disabled'
                    type: string
                  dayOfWeek:
                    description: 'DayOfWeek: day of week for maintenance window'
                    type: integer
                  startHour:
                    description: 'StartHour: start hour for maintenance window'
                    type: integer
                  startMinute:
                    description: 'StartMinute: start minute for maintenance window'
                    type: integer
                type: object
              minorVersion:
                description: 'MinorVersion: The minor version of the server.'
                type: string
              name:
                description: 'Name: The name of the resource'
                type: string
              network:
                description: 'Network: Network properties of a server.'
                properties:
                  delegatedSubnetResourceId:
                    description: 'DelegatedSubnetResourceId: delegated subnet arm
                      resource id.'
                    type: string
                  privateDnsZoneArmResourceId:
                    description: 'PrivateDnsZoneArmResourceId: private dns zone arm
                      resource id.'
                    type: string
                  publicNetworkAccess:
                    description: 'PublicNetworkAccess: public network access is enabled
                      or not'
                    type: string
                type: object
              pointInTimeUTC:
                description: |-
                  PointInTimeUTC: Restore point creation time (ISO8601 format), specifying the time to restore from. It's required when
                  'createMode' is 'PointInTimeRestore'.
                type: string
              sku:
                description: 'Sku: The SKU (pricing tier) of the server.'
                properties:
                  name:
                    description: 'Name: The name of the sku, typically, tier + family
                      + cores, e.g. Standard_D4s_v3.'
                    type: string
                  tier:
                    description: 'Tier: The tier of the particular SKU, e.g. Burstable.'
                    type: string
                type: object
              sourceServerResourceId:
                description: |-
                  SourceServerResourceId: The source server resource ID to restore from. It's required when 'createMode' is
                  'PointInTimeRestore'.
                type: string
              state:
                description: 'State: A state of a server that is visible to user.'
                type: string
              storage:
                description: 'Storage: Storage properties of a server.'
                properties:
                  storageSizeGB:
                    description: 'StorageSizeGB: Max storage allowed for a server.'
                    type: integer
                type: object
              systemData:
                description: 'SystemData: The system metadata relating to this resource.'
                properties:
                  createdAt:
                    description: 'CreatedAt: The timestamp of resource creation (UTC).'
                    type: string
                  createdBy:
                    description: 'CreatedBy: The identity that created the resource.'
                    type: string
                  createdByType:
                    description: 'CreatedByType: The type of identity that created
                      the resource.'
                    type: string
                  lastModifiedAt:
                    description: 'LastModifiedAt: The timestamp of resource last modification
                      (UTC)'
                    type: string
                  lastModifiedBy:
                    description: 'LastModifiedBy: The identity that last modified
                      the resource.'
                    type: string
                  lastModifiedByType:
                    description: 'LastModifiedByType: The type of identity that last
                      modified the resource.'
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
              version:
                description: 'Version: PostgreSQL Server version.'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20210601storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20210601.FlexibleServer
          Generator information:
          - Generated from: /postgresql/resource-manager/Microsoft.DBforPostgreSQL/stable/2021-06-01/postgresql.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DBforPostgreSQL/flexibleServers/{serverName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20210601.FlexibleServer_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              administratorLogin:
                type: string
              administratorLoginPassword:
                description: |-
                  SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                  the resource it is on.
                properties:
                  key:
                    description: Key is the key in the Kubernetes secret being referenced
                    type: string
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - key
                - name
                type: object
              availabilityZone:
                type: string
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              backup:
                description: |-
                  Storage version of v1api20210601.Backup
                  Backup properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  backupRetentionDays:
                    type: integer
                  geoRedundantBackup:
                    type: string
                type: object
              createMode:
                type: string
              highAvailability:
                description: |-
                  Storage version of v1api20210601.HighAvailability
                  High availability properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  mode:
                    type: string
                  standbyAvailabilityZone:
                    type: string
                type: object
              location:
                type: string
              maintenanceWindow:
                description: |-
                  Storage version of v1api20210601.MaintenanceWindow
                  Maintenance window properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  customWindow:
                    type: string
                  dayOfWeek:
                    type: integer
                  startHour:
                    type: integer
                  startMinute:
                    type: integer
                type: object
              network:
                description: |-
                  Storage version of v1api20210601.Network
                  Network properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  delegatedSubnetResourceReference:
                    description: 'DelegatedSubnetResourceReference: delegated subnet
                      arm resource id.'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  privateDnsZoneArmResourceReference:
                    description: 'PrivateDnsZoneArmResourceReference: private dns
                      zone arm resource id.'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                type: object
              operatorSpec:
                description: |-
                  Storage version of v1api20210601.FlexibleServerOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: Storage version of v1api20210601.FlexibleServerOperatorSecrets
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      fullyQualifiedDomainName:
                        description: |-
                          SecretDestination describes the location to store a single secret value.
                          Note: This is similar to: ConfigMapDestination in configmaps.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              pointInTimeUTC:
                type: string
              sku:
                description: |-
                  Storage version of v1api20210601.Sku
                  Sku information related properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              sourceServerResourceReference:
                description: |-
                  SourceServerResourceReference: The source server resource ID to restore from. It's required when 'createMode' is
                  'PointInTimeRestore'.
                properties:
                  armId:
                    description: |-
                      ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                      The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                      ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: Name is the Kubernetes name of the resource.
                    type: string
                type: object
              storage:
                description: |-
                  Storage version of v1api20210601.Storage
                  Storage properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  storageSizeGB:
                    type: integer
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              version:
                type: string
            required:
            - owner
            type: object
          status:
            description: Storage version of v1api20210601.FlexibleServer_STATUS
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              administratorLogin:
                type: string
              availabilityZone:
                type: string
              backup:
                description: |-
                  Storage version of v1api20210601.Backup_STATUS
                  Backup properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  backupRetentionDays:
                    type: integer
                  earliestRestoreDate:
                    type: string
                  geoRedundantBackup:
                    type: string
                type: object
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createMode:
                type: string
              fullyQualifiedDomainName:
                type: string
              highAvailability:
                description: |-
                  Storage version of v1api20210601.HighAvailability_STATUS
                  High availability properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  mode:
                    type: string
                  standbyAvailabilityZone:
                    type: string
                  state:
                    type: string
                type: object
              id:
                type: string
              location:
                type: string
              maintenanceWindow:
                description: |-
                  Storage version of v1api20210601.MaintenanceWindow_STATUS
                  Maintenance window properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  customWindow:
                    type: string
                  dayOfWeek:
                    type: integer
                  startHour:
                    type: integer
                  startMinute:
                    type: integer
                type: object
              minorVersion:
                type: string
              name:
                type: string
              network:
                description: |-
                  Storage version of v1api20210601.Network_STATUS
                  Network properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  delegatedSubnetResourceId:
                    type: string
                  privateDnsZoneArmResourceId:
                    type: string
                  publicNetworkAccess:
                    type: string
                type: object
              pointInTimeUTC:
                type: string
              sku:
                description: |-
                  Storage version of v1api20210601.Sku_STATUS
                  Sku information related properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              sourceServerResourceId:
                type: string
              state:
                type: string
              storage:
                description: |-
                  Storage version of v1api20210601.Storage_STATUS
                  Storage properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  storageSizeGB:
                    type: integer
                type: object
              systemData:
                description: |-
                  Storage version of v1api20210601.SystemData_STATUS
                  Metadata pertaining to creation and last modification of the resource.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  createdAt:
                    type: string
                  createdBy:
                    type: string
                  createdByType:
                    type: string
                  lastModifiedAt:
                    type: string
                  lastModifiedBy:
                    type: string
                  lastModifiedByType:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              type:
                type: string
              version:
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20220120preview
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /postgresql/resource-manager/Microsoft.DBforPostgreSQL/preview/2022-01-20-preview/postgresql.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DBforPostgreSQL/flexibleServers/{serverName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              administratorLogin:
                description: |-
                  AdministratorLogin: The administrator's login name of a server. Can only be specified when the server is being created
                  (and is required for creation).
                type: string
              administratorLoginPassword:
                description: 'AdministratorLoginPassword: The administrator login
                  password (required for server creation).'
                properties:
                  key:
                    description: Key is the key in the Kubernetes secret being referenced
                    type: string
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - key
                - name
                type: object
              availabilityZone:
                description: 'AvailabilityZone: availability zone information of the
                  server.'
                type: string
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              backup:
                description: 'Backup: Backup properties of a server.'
                properties:
                  backupRetentionDays:
                    description: 'BackupRetentionDays: Backup retention days for the
                      server.'
                    type: integer
                  geoRedundantBackup:
                    description: 'GeoRedundantBackup: A value indicating whether Geo-Redundant
                      backup is enabled on the server.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                type: object
              createMode:
                description: 'CreateMode: The mode to create a new PostgreSQL server.'
                enum:
                - Create
                - Default
                - PointInTimeRestore
                - Update
                type: string
              highAvailability:
                description: 'HighAvailability: High availability properties of a
                  server.'
                properties:
                  mode:
                    description: 'Mode: The HA mode for the server.'
                    enum:
                    - Disabled
                    - SameZone
                    - ZoneRedundant
                    type: string
                  standbyAvailabilityZone:
                    description: 'StandbyAvailabilityZone: availability zone information
                      of the standby.'
                    type: string
                type: object
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              maintenanceWindow:
                description: 'MaintenanceWindow: Maintenance window properties of
                  a server.'
                properties:
                  customWindow:
                    description: 'CustomWindow: indicates whether custom window is
                      enabled or disabled'
                    type: string
                  dayOfWeek:
                    description: 'DayOfWeek: day of week for maintenance window'
                    type: integer
                  startHour:
                    description: 'StartHour: start hour for maintenance window'
                    type: integer
                  startMinute:
                    description: 'StartMinute: start minute for maintenance window'
                    type: integer
                type: object
              network:
                description: 'Network: Network properties of a server.'
                properties:
                  delegatedSubnetResourceReference:
                    description: 'DelegatedSubnetResourceReference: delegated subnet
                      arm resource id.'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  privateDnsZoneArmResourceReference:
                    description: 'PrivateDnsZoneArmResourceReference: private dns
                      zone arm resource id.'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                type: object
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  configMaps:
                    description: 'ConfigMaps: configures where to place operator written
                      ConfigMaps.'
                    properties:
                      fullyQualifiedDomainName:
                        description: |-
                          FullyQualifiedDomainName: indicates where the FullyQualifiedDomainName config map should be placed. If omitted, no
                          config map will be created.
                        properties:
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: 'Secrets: configures where to place Azure generated
                      secrets.'
                    properties:
                      fullyQualifiedDomainName:
                        description: |-
                          FullyQualifiedDomainName: indicates where the FullyQualifiedDomainName secret should be placed. If omitted, the secret
                          will not be retrieved from Azure.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              pointInTimeUTC:
                description: |-
                  PointInTimeUTC: Restore point creation time (ISO8601 format), specifying the time to restore from. It's required when
                  'createMode' is 'PointInTimeRestore'.
                type: string
              sku:
                description: 'Sku: The SKU (pricing tier) of the server.'
                properties:
                  name:
                    description: 'Name: The name of the sku, typically, tier + family
                      + cores, e.g. Standard_D4s_v3.'
                    type: string
                  tier:
                    description: 'Tier: The tier of the particular SKU, e.g. Burstable.'
                    enum:
                    - Burstable
                    - GeneralPurpose
                    - MemoryOptimized
                    type: string
                required:
                - name
                - tier
                type: object
              sourceServerResourceReference:
                description: |-
                  SourceServerResourceReference: The source server resource ID to restore from. It's required when 'createMode' is
                  'PointInTimeRestore'.
                properties:
                  armId:
                    description: |-
                      ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                      The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                      ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: Name is the Kubernetes name of the resource.
                    type: string
                type: object
              storage:
                description: 'Storage: Storage properties of a server.'
                properties:
                  storageSizeGB:
                    description: 'StorageSizeGB: Max storage allowed for a server.'
                    type: integer
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              version:
                description: 'Version: PostgreSQL Server version.'
                enum:
                - "11"
                - "12"
                - "13"
                - "14"
                type: string
            required:
            - location
            - owner
            type: object
          status:
            properties:
              administratorLogin:
                description: |-
                  AdministratorLogin: The administrator's login name of a server. Can only be specified when the server is being created
                  (and is required for creation).
                type: string
              availabilityZone:
                description: 'AvailabilityZone: availability zone information of the
                  server.'
                type: string
              backup:
                description: 'Backup: Backup properties of a server.'
                properties:
                  backupRetentionDays:
                    description: 'BackupRetentionDays: Backup retention days for the
                      server.'
                    type: integer
                  earliestRestoreDate:
                    description: 'EarliestRestoreDate: The earliest restore point
                      time (ISO8601 format) for server.'
                    type: string
                  geoRedundantBackup:
                    description: 'GeoRedundantBackup: A value indicating whether Geo-Redundant
                      backup is enabled on the server.'
                    type: string
                type: object
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createMode:
                description: 'CreateMode: The mode to create a new PostgreSQL server.'
                type: string
              fullyQualifiedDomainName:
                description: 'FullyQualifiedDomainName: The fully qualified domain
                  name of a server.'
                type: string
              highAvailability:
                description: 'HighAvailability: High availability properties of a
                  server.'
                properties:
                  mode:
                    description: 'Mode: The HA mode for the server.'
                    type: string
                  standbyAvailabilityZone:
                    description: 'StandbyAvailabilityZone: availability zone information
                      of the standby.'
                    type: string
                  state:
                    description: 'State: A state of a HA server that is visible to
                      user.'
                    type: string
                type: object
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. Ex -
                  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                type: string
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              maintenanceWindow:
                description: 'MaintenanceWindow: Maintenance window properties of
                  a server.'
                properties:
                  customWindow:
                    description: 'CustomWindow: indicates whether custom window is
                      enabled or disabled'
                    type: string
                  dayOfWeek:
                    description: 'DayOfWeek: day of week for maintenance window'
                    type: integer
                  startHour:
                    description: 'StartHour: start hour for maintenance window'
                    type: integer
                  startMinute:
                    description: 'StartMinute: start minute for maintenance window'
                    type: integer
                type: object
              minorVersion:
                description: 'MinorVersion: The minor version of the server.'
                type: string
              name:
                description: 'Name: The name of the resource'
                type: string
              network:
                description: 'Network: Network properties of a server.'
                properties:
                  delegatedSubnetResourceId:
                    description: 'DelegatedSubnetResourceId: delegated subnet arm
                      resource id.'
                    type: string
                  privateDnsZoneArmResourceId:
                    description: 'PrivateDnsZoneArmResourceId: private dns zone arm
                      resource id.'
                    type: string
                  publicNetworkAccess:
                    description: 'PublicNetworkAccess: public network access is enabled
                      or not'
                    type: string
                type: object
              pointInTimeUTC:
                description: |-
                  PointInTimeUTC: Restore point creation time (ISO8601 format), specifying the time to restore from. It's required when
                  'createMode' is 'PointInTimeRestore'.
                type: string
              sku:
                description: 'Sku: The SKU (pricing tier) of the server.'
                properties:
                  name:
                    description: 'Name: The name of the sku, typically, tier + family
                      + cores, e.g. Standard_D4s_v3.'
                    type: string
                  tier:
                    description: 'Tier: The tier of the particular SKU, e.g. Burstable.'
                    type: string
                type: object
              sourceServerResourceId:
                description: |-
                  SourceServerResourceId: The source server resource ID to restore from. It's required when 'createMode' is
                  'PointInTimeRestore'.
                type: string
              state:
                description: 'State: A state of a server that is visible to user.'
                type: string
              storage:
                description: 'Storage: Storage properties of a server.'
                properties:
                  storageSizeGB:
                    description: 'StorageSizeGB: Max storage allowed for a server.'
                    type: integer
                type: object
              systemData:
                description: 'SystemData: The system metadata relating to this resource.'
                properties:
                  createdAt:
                    description: 'CreatedAt: The timestamp of resource creation (UTC).'
                    type: string
                  createdBy:
                    description: 'CreatedBy: The identity that created the resource.'
                    type: string
                  createdByType:
                    description: 'CreatedByType: The type of identity that created
                      the resource.'
                    type: string
                  lastModifiedAt:
                    description: 'LastModifiedAt: The timestamp of resource last modification
                      (UTC)'
                    type: string
                  lastModifiedBy:
                    description: 'LastModifiedBy: The identity that last modified
                      the resource.'
                    type: string
                  lastModifiedByType:
                    description: 'LastModifiedByType: The type of identity that last
                      modified the resource.'
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
              version:
                description: 'Version: PostgreSQL Server version.'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20220120previewstorage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20220120preview.FlexibleServer
          Generator information:
          - Generated from: /postgresql/resource-manager/Microsoft.DBforPostgreSQL/preview/2022-01-20-preview/postgresql.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DBforPostgreSQL/flexibleServers/{serverName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20220120preview.FlexibleServer_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              administratorLogin:
                type: string
              administratorLoginPassword:
                description: |-
                  SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                  the resource it is on.
                properties:
                  key:
                    description: Key is the key in the Kubernetes secret being referenced
                    type: string
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - key
                - name
                type: object
              availabilityZone:
                type: string
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              backup:
                description: |-
                  Storage version of v1api20220120preview.Backup
                  Backup properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  backupRetentionDays:
                    type: integer
                  geoRedundantBackup:
                    type: string
                type: object
              createMode:
                type: string
              highAvailability:
                description: |-
                  Storage version of v1api20220120preview.HighAvailability
                  High availability properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  mode:
                    type: string
                  standbyAvailabilityZone:
                    type: string
                type: object
              location:
                type: string
              maintenanceWindow:
                description: |-
                  Storage version of v1api20220120preview.MaintenanceWindow
                  Maintenance window properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  customWindow:
                    type: string
                  dayOfWeek:
                    type: integer
                  startHour:
                    type: integer
                  startMinute:
                    type: integer
                type: object
              network:
                description: |-
                  Storage version of v1api20220120preview.Network
                  Network properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  delegatedSubnetResourceReference:
                    description: 'DelegatedSubnetResourceReference: delegated subnet
                      arm resource id.'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  privateDnsZoneArmResourceReference:
                    description: 'PrivateDnsZoneArmResourceReference: private dns
                      zone arm resource id.'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                type: object
              operatorSpec:
                description: |-
                  Storage version of v1api20220120preview.FlexibleServerOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  configMaps:
                    description: Storage version of v1api20220120preview.FlexibleServerOperatorConfigMaps
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      fullyQualifiedDomainName:
                        description: |-
                          ConfigMapDestination describes the location to store a single configmap value
                          Note: This is similar to: SecretDestination in secrets.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: Storage version of v1api20220120preview.FlexibleServerOperatorSecrets
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      fullyQualifiedDomainName:
                        description: |-
                          SecretDestination describes the location to store a single secret value.
                          Note: This is similar to: ConfigMapDestination in configmaps.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              pointInTimeUTC:
                type: string
              sku:
                description: |-
                  Storage version of v1api20220120preview.Sku
                  Sku information related properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              sourceServerResourceReference:
                description: |-
                  SourceServerResourceReference: The source server resource ID to restore from. It's required when 'createMode' is
                  'PointInTimeRestore'.
                properties:
                  armId:
                    description: |-
                      ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                      The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                      ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: Name is the Kubernetes name of the resource.
                    type: string
                type: object
              storage:
                description: |-
                  Storage version of v1api20220120preview.Storage
                  Storage properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  storageSizeGB:
                    type: integer
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              version:
                type: string
            required:
            - owner
            type: object
          status:
            description: Storage version of v1api20220120preview.FlexibleServer_STATUS
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              administratorLogin:
                type: string
              availabilityZone:
                type: string
              backup:
                description: |-
                  Storage version of v1api20220120preview.Backup_STATUS
                  Backup properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  backupRetentionDays:
                    type: integer
                  earliestRestoreDate:
                    type: string
                  geoRedundantBackup:
                    type: string
                type: object
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createMode:
                type: string
              fullyQualifiedDomainName:
                type: string
              highAvailability:
                description: |-
                  Storage version of v1api20220120preview.HighAvailability_STATUS
                  High availability properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  mode:
                    type: string
                  standbyAvailabilityZone:
                    type: string
                  state:
                    type: string
                type: object
              id:
                type: string
              location:
                type: string
              maintenanceWindow:
                description: |-
                  Storage version of v1api20220120preview.MaintenanceWindow_STATUS
                  Maintenance window properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  customWindow:
                    type: string
                  dayOfWeek:
                    type: integer
                  startHour:
                    type: integer
                  startMinute:
                    type: integer
                type: object
              minorVersion:
                type: string
              name:
                type: string
              network:
                description: |-
                  Storage version of v1api20220120preview.Network_STATUS
                  Network properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  delegatedSubnetResourceId:
                    type: string
                  privateDnsZoneArmResourceId:
                    type: string
                  publicNetworkAccess:
                    type: string
                type: object
              pointInTimeUTC:
                type: string
              sku:
                description: |-
                  Storage version of v1api20220120preview.Sku_STATUS
                  Sku information related properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              sourceServerResourceId:
                type: string
              state:
                type: string
              storage:
                description: |-
                  Storage version of v1api20220120preview.Storage_STATUS
                  Storage properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  storageSizeGB:
                    type: integer
                type: object
              systemData:
                description: |-
                  Storage version of v1api20220120preview.SystemData_STATUS
                  Metadata pertaining to creation and last modification of the resource.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  createdAt:
                    type: string
                  createdBy:
                    type: string
                  createdByType:
                    type: string
                  lastModifiedAt:
                    type: string
                  lastModifiedBy:
                    type: string
                  lastModifiedByType:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              type:
                type: string
              version:
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20221201
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /postgresql/resource-manager/Microsoft.DBforPostgreSQL/stable/2022-12-01/FlexibleServers.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DBforPostgreSQL/flexibleServers/{serverName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              administratorLogin:
                description: |-
                  AdministratorLogin: The administrator's login name of a server. Can only be specified when the server is being created
                  (and is required for creation).
                type: string
              administratorLoginPassword:
                description: 'AdministratorLoginPassword: The administrator login
                  password (required for server creation).'
                properties:
                  key:
                    description: Key is the key in the Kubernetes secret being referenced
                    type: string
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - key
                - name
                type: object
              authConfig:
                description: 'AuthConfig: AuthConfig properties of a server.'
                properties:
                  activeDirectoryAuth:
                    description: 'ActiveDirectoryAuth: If Enabled, Azure Active Directory
                      authentication is enabled.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                  passwordAuth:
                    description: 'PasswordAuth: If Enabled, Password authentication
                      is enabled.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                  tenantId:
                    description: 'TenantId: Tenant id of the server.'
                    type: string
                type: object
              availabilityZone:
                description: 'AvailabilityZone: availability zone information of the
                  server.'
                type: string
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                maxLength: 63
                minLength: 3
                pattern: ^[a-zA-Z0-9]+(-[a-zA-Z0-9]+)*
                type: string
              backup:
                description: 'Backup: Backup properties of a server.'
                properties:
                  backupRetentionDays:
                    description: 'BackupRetentionDays: Backup retention days for the
                      server.'
                    type: integer
                  geoRedundantBackup:
                    description: 'GeoRedundantBackup: A value indicating whether Geo-Redundant
                      backup is enabled on the server.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                type: object
              createMode:
                description: 'CreateMode: The mode to create a new PostgreSQL server.'
                enum:
                - Create
                - Default
                - GeoRestore
                - PointInTimeRestore
                - Replica
                - Update
                type: string
              dataEncryption:
                description: 'DataEncryption: Data encryption properties of a server.'
                properties:
                  primaryKeyURI:
                    description: 'PrimaryKeyURI: URI for the key for data encryption
                      for primary server.'
                    type: string
                  primaryKeyURIFromConfig:
                    description: 'PrimaryKeyURIFromConfig: URI for the key for data
                      encryption for primary server.'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  primaryUserAssignedIdentityReference:
                    description: |-
                      PrimaryUserAssignedIdentityReference: Resource Id for the User assigned identity to be used for data encryption for
                      primary server.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  type:
                    description: 'Type: Data encryption type to depict if it is System
                      Managed vs Azure Key vault.'
                    enum:
                    - AzureKeyVault
                    - SystemManaged
                    type: string
                type: object
              highAvailability:
                description: 'HighAvailability: High availability properties of a
                  server.'
                properties:
                  mode:
                    description: 'Mode: The HA mode for the server.'
                    enum:
                    - Disabled
                    - SameZone
                    - ZoneRedundant
                    type: string
                  standbyAvailabilityZone:
                    description: 'StandbyAvailabilityZone: availability zone information
                      of the standby.'
                    type: string
                type: object
              identity:
                description: 'Identity: Describes the identity of the application.'
                properties:
                  type:
                    description: 'Type: the types of identities associated with this
                      resource; currently restricted to ''None and UserAssigned'''
                    enum:
                    - None
                    - UserAssigned
                    type: string
                  userAssignedIdentities:
                    description: 'UserAssignedIdentities: represents user assigned
                      identities map.'
                    items:
                      description: Information about the user assigned identity for
                        the resource
                      properties:
                        reference:
                          description: ResourceReference represents a resource reference,
                            either to a Kubernetes resource or directly to an Azure
                            resource via ARMID
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                required:
                - type
                type: object
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              maintenanceWindow:
                description: 'MaintenanceWindow: Maintenance window properties of
                  a server.'
                properties:
                  customWindow:
                    description: 'CustomWindow: indicates whether custom window is
                      enabled or disabled'
                    type: string
                  dayOfWeek:
                    description: 'DayOfWeek: day of week for maintenance window'
                    type: integer
                  startHour:
                    description: 'StartHour: start hour for maintenance window'
                    type: integer
                  startMinute:
                    description: 'StartMinute: start minute for maintenance window'
                    type: integer
                type: object
              network:
                description: |-
                  Network: Network properties of a server. This Network property is required to be passed only in case you want the server
                  to be Private access server.
                properties:
                  delegatedSubnetResourceReference:
                    description: |-
                      DelegatedSubnetResourceReference: Delegated subnet arm resource id. This is required to be passed during create, in case
                      we want the server to be VNET injected, i.e. Private access server. During update, pass this only if we want to update
                      the value for Private DNS zone.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  privateDnsZoneArmResourceReference:
                    description: |-
                      PrivateDnsZoneArmResourceReference: Private dns zone arm resource id. This is required to be passed during create, in
                      case we want the server to be VNET injected, i.e. Private access server. During update, pass this only if we want to
                      update the value for Private DNS zone.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                type: object
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  configMaps:
                    description: 'ConfigMaps: configures where to place operator written
                      ConfigMaps.'
                    properties:
                      fullyQualifiedDomainName:
                        description: |-
                          FullyQualifiedDomainName: indicates where the FullyQualifiedDomainName config map should be placed. If omitted, no
                          config map will be created.
                        properties:
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: 'Secrets: configures where to place Azure generated
                      secrets.'
                    properties:
                      fullyQualifiedDomainName:
                        description: |-
                          FullyQualifiedDomainName: indicates where the FullyQualifiedDomainName secret should be placed. If omitted, the secret
                          will not be retrieved from Azure.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              pointInTimeUTC:
                description: |-
                  PointInTimeUTC: Restore point creation time (ISO8601 format), specifying the time to restore from. It's required when
                  'createMode' is 'PointInTimeRestore' or 'GeoRestore'.
                type: string
              replicationRole:
                description: 'ReplicationRole: Replication role of the server'
                enum:
                - AsyncReplica
                - GeoAsyncReplica
                - None
                - Primary
                type: string
              sku:
                description: 'Sku: The SKU (pricing tier) of the server.'
                properties:
                  name:
                    description: 'Name: The name of the sku, typically, tier + family
                      + cores, e.g. Standard_D4s_v3.'
                    type: string
                  tier:
                    description: 'Tier: The tier of the particular SKU, e.g. Burstable.'
                    enum:
                    - Burstable
                    - GeneralPurpose
                    - MemoryOptimized
                    type: string
                required:
                - name
                - tier
                type: object
              sourceServerResourceReference:
                description: |-
                  SourceServerResourceReference: The source server resource ID to restore from. It's required when 'createMode' is
                  'PointInTimeRestore' or 'GeoRestore' or 'Replica'. This property is returned only for Replica server
                properties:
                  armId:
                    description: |-
                      ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                      The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                      ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: Name is the Kubernetes name of the resource.
                    type: string
                type: object
              storage:
                description: 'Storage: Storage properties of a server.'
                properties:
                  storageSizeGB:
                    description: 'StorageSizeGB: Max storage allowed for a server.'
                    type: integer
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              version:
                description: 'Version: PostgreSQL Server version.'
                enum:
                - "11"
                - "12"
                - "13"
                - "14"
                type: string
            required:
            - location
            - owner
            type: object
          status:
            properties:
              administratorLogin:
                description: |-
                  AdministratorLogin: The administrator's login name of a server. Can only be specified when the server is being created
                  (and is required for creation).
                type: string
              authConfig:
                description: 'AuthConfig: AuthConfig properties of a server.'
                properties:
                  activeDirectoryAuth:
                    description: 'ActiveDirectoryAuth: If Enabled, Azure Active Directory
                      authentication is enabled.'
                    type: string
                  passwordAuth:
                    description: 'PasswordAuth: If Enabled, Password authentication
                      is enabled.'
                    type: string
                  tenantId:
                    description: 'TenantId: Tenant id of the server.'
                    type: string
                type: object
              availabilityZone:
                description: 'AvailabilityZone: availability zone information of the
                  server.'
                type: string
              backup:
                description: 'Backup: Backup properties of a server.'
                properties:
                  backupRetentionDays:
                    description: 'BackupRetentionDays: Backup retention days for the
                      server.'
                    type: integer
                  earliestRestoreDate:
                    description: 'EarliestRestoreDate: The earliest restore point
                      time (ISO8601 format) for server.'
                    type: string
                  geoRedundantBackup:
                    description: 'GeoRedundantBackup: A value indicating whether Geo-Redundant
                      backup is enabled on the server.'
                    type: string
                type: object
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createMode:
                description: 'CreateMode: The mode to create a new PostgreSQL server.'
                type: string
              dataEncryption:
                description: 'DataEncryption: Data encryption properties of a server.'
                properties:
                  primaryKeyURI:
                    description: 'PrimaryKeyURI: URI for the key for data encryption
                      for primary server.'
                    type: string
                  primaryUserAssignedIdentityId:
                    description: |-
                      PrimaryUserAssignedIdentityId: Resource Id for the User assigned identity to be used for data encryption for primary
                      server.
                    type: string
                  type:
                    description: 'Type: Data encryption type to depict if it is System
                      Managed vs Azure Key vault.'
                    type: string
                type: object
              fullyQualifiedDomainName:
                description: 'FullyQualifiedDomainName: The fully qualified domain
                  name of a server.'
                type: string
              highAvailability:
                description: 'HighAvailability: High availability properties of a
                  server.'
                properties:
                  mode:
                    description: 'Mode: The HA mode for the server.'
                    type: string
                  standbyAvailabilityZone:
                    description: 'StandbyAvailabilityZone: availability zone information
                      of the standby.'
                    type: string
                  state:
                    description: 'State: A state of a HA server that is visible to
                      user.'
                    type: string
                type: object
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. Ex -
                  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                type: string
              identity:
                description: 'Identity: Describes the identity of the application.'
                properties:
                  tenantId:
                    description: 'TenantId: Tenant id of the server.'
                    type: string
                  type:
                    description: 'Type: the types of identities associated with this
                      resource; currently restricted to ''None and UserAssigned'''
                    type: string
                  userAssignedIdentities:
                    additionalProperties:
                      description: Describes a single user-assigned identity associated
                        with the application.
                      properties:
                        clientId:
                          description: 'ClientId: the client identifier of the Service
                            Principal which this identity represents.'
                          type: string
                        principalId:
                          description: 'PrincipalId: the object identifier of the
                            Service Principal which this identity represents.'
                          type: string
                      type: object
                    description: 'UserAssignedIdentities: represents user assigned
                      identities map.'
                    type: object
                type: object
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              maintenanceWindow:
                description: 'MaintenanceWindow: Maintenance window properties of
                  a server.'
                properties:
                  customWindow:
                    description: 'CustomWindow: indicates whether custom window is
                      enabled or disabled'
                    type: string
                  dayOfWeek:
                    description: 'DayOfWeek: day of week for maintenance window'
                    type: integer
                  startHour:
                    description: 'StartHour: start hour for maintenance window'
                    type: integer
                  startMinute:
                    description: 'StartMinute: start minute for maintenance window'
                    type: integer
                type: object
              minorVersion:
                description: 'MinorVersion: The minor version of the server.'
                type: string
              name:
                description: 'Name: The name of the resource'
                type: string
              network:
                description: |-
                  Network: Network properties of a server. This Network property is required to be passed only in case you want the server
                  to be Private access server.
                properties:
                  delegatedSubnetResourceId:
                    description: |-
                      DelegatedSubnetResourceId: Delegated subnet arm resource id. This is required to be passed during create, in case we
                      want the server to be VNET injected, i.e. Private access server. During update, pass this only if we want to update the
                      value for Private DNS zone.
                    type: string
                  privateDnsZoneArmResourceId:
                    description: |-
                      PrivateDnsZoneArmResourceId: Private dns zone arm resource id. This is required to be passed during create, in case we
                      want the server to be VNET injected, i.e. Private access server. During update, pass this only if we want to update the
                      value for Private DNS zone.
                    type: string
                  publicNetworkAccess:
                    description: 'PublicNetworkAccess: public network access is enabled
                      or not'
                    type: string
                type: object
              pointInTimeUTC:
                description: |-
                  PointInTimeUTC: Restore point creation time (ISO8601 format), specifying the time to restore from. It's required when
                  'createMode' is 'PointInTimeRestore' or 'GeoRestore'.
                type: string
              replicaCapacity:
                description: 'ReplicaCapacity: Replicas allowed for a server.'
                type: integer
              replicationRole:
                description: 'ReplicationRole: Replication role of the server'
                type: string
              sku:
                description: 'Sku: The SKU (pricing tier) of the server.'
                properties:
                  name:
                    description: 'Name: The name of the sku, typically, tier + family
                      + cores, e.g. Standard_D4s_v3.'
                    type: string
                  tier:
                    description: 'Tier: The tier of the particular SKU, e.g. Burstable.'
                    type: string
                type: object
              sourceServerResourceId:
                description: |-
                  SourceServerResourceId: The source server resource ID to restore from. It's required when 'createMode' is
                  'PointInTimeRestore' or 'GeoRestore' or 'Replica'. This property is returned only for Replica server
                type: string
              state:
                description: 'State: A state of a server that is visible to user.'
                type: string
              storage:
                description: 'Storage: Storage properties of a server.'
                properties:
                  storageSizeGB:
                    description: 'StorageSizeGB: Max storage allowed for a server.'
                    type: integer
                type: object
              systemData:
                description: 'SystemData: Azure Resource Manager metadata containing
                  createdBy and modifiedBy information.'
                properties:
                  createdAt:
                    description: 'CreatedAt: The timestamp of resource creation (UTC).'
                    type: string
                  createdBy:
                    description: 'CreatedBy: The identity that created the resource.'
                    type: string
                  createdByType:
                    description: 'CreatedByType: The type of identity that created
                      the resource.'
                    type: string
                  lastModifiedAt:
                    description: 'LastModifiedAt: The timestamp of resource last modification
                      (UTC)'
                    type: string
                  lastModifiedBy:
                    description: 'LastModifiedBy: The identity that last modified
                      the resource.'
                    type: string
                  lastModifiedByType:
                    description: 'LastModifiedByType: The type of identity that last
                      modified the resource.'
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
              version:
                description: 'Version: PostgreSQL Server version.'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20221201storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20221201.FlexibleServer
          Generator information:
          - Generated from: /postgresql/resource-manager/Microsoft.DBforPostgreSQL/stable/2022-12-01/FlexibleServers.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DBforPostgreSQL/flexibleServers/{serverName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20221201.FlexibleServer_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              administratorLogin:
                type: string
              administratorLoginPassword:
                description: |-
                  SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                  the resource it is on.
                properties:
                  key:
                    description: Key is the key in the Kubernetes secret being referenced
                    type: string
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - key
                - name
                type: object
              authConfig:
                description: |-
                  Storage version of v1api20221201.AuthConfig
                  Authentication configuration properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  activeDirectoryAuth:
                    type: string
                  passwordAuth:
                    type: string
                  tenantId:
                    type: string
                type: object
              availabilityZone:
                type: string
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              backup:
                description: |-
                  Storage version of v1api20221201.Backup
                  Backup properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  backupRetentionDays:
                    type: integer
                  geoRedundantBackup:
                    type: string
                type: object
              createMode:
                type: string
              dataEncryption:
                description: |-
                  Storage version of v1api20221201.DataEncryption
                  Data encryption properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  primaryKeyURI:
                    type: string
                  primaryKeyURIFromConfig:
                    description: |-
                      ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  primaryUserAssignedIdentityReference:
                    description: |-
                      PrimaryUserAssignedIdentityReference: Resource Id for the User assigned identity to be used for data encryption for
                      primary server.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  type:
                    type: string
                type: object
              highAvailability:
                description: |-
                  Storage version of v1api20221201.HighAvailability
                  High availability properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  mode:
                    type: string
                  standbyAvailabilityZone:
                    type: string
                type: object
              identity:
                description: |-
                  Storage version of v1api20221201.UserAssignedIdentity
                  Information describing the identities associated with this application.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  type:
                    type: string
                  userAssignedIdentities:
                    items:
                      description: |-
                        Storage version of v1api20221201.UserAssignedIdentityDetails
                        Information about the user assigned identity for the resource
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        reference:
                          description: ResourceReference represents a resource reference,
                            either to a Kubernetes resource or directly to an Azure
                            resource via ARMID
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              location:
                type: string
              maintenanceWindow:
                description: |-
                  Storage version of v1api20221201.MaintenanceWindow
                  Maintenance window properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  customWindow:
                    type: string
                  dayOfWeek:
                    type: integer
                  startHour:
                    type: integer
                  startMinute:
                    type: integer
                type: object
              network:
                description: |-
                  Storage version of v1api20221201.Network
                  Network properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  delegatedSubnetResourceReference:
                    description: |-
                      DelegatedSubnetResourceReference: Delegated subnet arm resource id. This is required to be passed during create, in case
                      we want the server to be VNET injected, i.e. Private access server. During update, pass this only if we want to update
                      the value for Private DNS zone.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  privateDnsZoneArmResourceReference:
                    description: |-
                      PrivateDnsZoneArmResourceReference: Private dns zone arm resource id. This is required to be passed during create, in
                      case we want the server to be VNET injected, i.e. Private access server. During update, pass this only if we want to
                      update the value for Private DNS zone.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                type: object
              operatorSpec:
                description: |-
                  Storage version of v1api20221201.FlexibleServerOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  configMaps:
                    description: Storage version of v1api20221201.FlexibleServerOperatorConfigMaps
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      fullyQualifiedDomainName:
                        description: |-
                          ConfigMapDestination describes the location to store a single configmap value
                          Note: This is similar to: SecretDestination in secrets.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: Storage version of v1api20221201.FlexibleServerOperatorSecrets
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      fullyQualifiedDomainName:
                        description: |-
                          SecretDestination describes the location to store a single secret value.
                          Note: This is similar to: ConfigMapDestination in configmaps.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              pointInTimeUTC:
                type: string
              replicationRole:
                type: string
              sku:
                description: |-
                  Storage version of v1api20221201.Sku
                  Sku information related properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              sourceServerResourceReference:
                description: |-
                  SourceServerResourceReference: The source server resource ID to restore from. It's required when 'createMode' is
                  'PointInTimeRestore' or 'GeoRestore' or 'Replica'. This property is returned only for Replica server
                properties:
                  armId:
                    description: |-
                      ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                      The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                      ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: Name is the Kubernetes name of the resource.
                    type: string
                type: object
              storage:
                description: |-
                  Storage version of v1api20221201.Storage
                  Storage properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  storageSizeGB:
                    type: integer
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              version:
                type: string
            required:
            - owner
            type: object
          status:
            description: Storage version of v1api20221201.FlexibleServer_STATUS
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              administratorLogin:
                type: string
              authConfig:
                description: |-
                  Storage version of v1api20221201.AuthConfig_STATUS
                  Authentication configuration properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  activeDirectoryAuth:
                    type: string
                  passwordAuth:
                    type: string
                  tenantId:
                    type: string
                type: object
              availabilityZone:
                type: string
              backup:
                description: |-
                  Storage version of v1api20221201.Backup_STATUS
                  Backup properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  backupRetentionDays:
                    type: integer
                  earliestRestoreDate:
                    type: string
                  geoRedundantBackup:
                    type: string
                type: object
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createMode:
                type: string
              dataEncryption:
                description: |-
                  Storage version of v1api20221201.DataEncryption_STATUS
                  Data encryption properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  primaryKeyURI:
                    type: string
                  primaryUserAssignedIdentityId:
                    type: string
                  type:
                    type: string
                type: object
              fullyQualifiedDomainName:
                type: string
              highAvailability:
                description: |-
                  Storage version of v1api20221201.HighAvailability_STATUS
                  High availability properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  mode:
                    type: string
                  standbyAvailabilityZone:
                    type: string
                  state:
                    type: string
                type: object
              id:
                type: string
              identity:
                description: |-
                  Storage version of v1api20221201.UserAssignedIdentity_STATUS
                  Information describing the identities associated with this application.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  tenantId:
                    type: string
                  type:
                    type: string
                  userAssignedIdentities:
                    additionalProperties:
                      description: |-
                        Storage version of v1api20221201.UserIdentity_STATUS
                        Describes a single user-assigned identity associated with the application.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        clientId:
                          type: string
                        principalId:
                          type: string
                      type: object
                    type: object
                type: object
              location:
                type: string
              maintenanceWindow:
                description: |-
                  Storage version of v1api20221201.MaintenanceWindow_STATUS
                  Maintenance window properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  customWindow:
                    type: string
                  dayOfWeek:
                    type: integer
                  startHour:
                    type: integer
                  startMinute:
                    type: integer
                type: object
              minorVersion:
                type: string
              name:
                type: string
              network:
                description: |-
                  Storage version of v1api20221201.Network_STATUS
                  Network properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  delegatedSubnetResourceId:
                    type: string
                  privateDnsZoneArmResourceId:
                    type: string
                  publicNetworkAccess:
                    type: string
                type: object
              pointInTimeUTC:
                type: string
              replicaCapacity:
                type: integer
              replicationRole:
                type: string
              sku:
                description: |-
                  Storage version of v1api20221201.Sku_STATUS
                  Sku information related properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              sourceServerResourceId:
                type: string
              state:
                type: string
              storage:
                description: |-
                  Storage version of v1api20221201.Storage_STATUS
                  Storage properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  storageSizeGB:
                    type: integer
                type: object
              systemData:
                description: |-
                  Storage version of v1api20221201.SystemData_STATUS
                  Metadata pertaining to creation and last modification of the resource.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  createdAt:
                    type: string
                  createdBy:
                    type: string
                  createdByType:
                    type: string
                  lastModifiedAt:
                    type: string
                  lastModifiedBy:
                    type: string
                  lastModifiedByType:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              type:
                type: string
              version:
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20230601preview
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /postgresql/resource-manager/Microsoft.DBforPostgreSQL/preview/2023-06-01-preview/FlexibleServers.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DBforPostgreSQL/flexibleServers/{serverName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              administratorLogin:
                description: |-
                  AdministratorLogin: The administrator's login name of a server. Can only be specified when the server is being created
                  (and is required for creation).
                type: string
              administratorLoginPassword:
                description: 'AdministratorLoginPassword: The administrator login
                  password (required for server creation).'
                properties:
                  key:
                    description: Key is the key in the Kubernetes secret being referenced
                    type: string
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - key
                - name
                type: object
              authConfig:
                description: 'AuthConfig: AuthConfig properties of a server.'
                properties:
                  activeDirectoryAuth:
                    description: 'ActiveDirectoryAuth: If Enabled, Azure Active Directory
                      authentication is enabled.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                  passwordAuth:
                    description: 'PasswordAuth: If Enabled, Password authentication
                      is enabled.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                  tenantId:
                    description: 'TenantId: Tenant id of the server.'
                    type: string
                type: object
              availabilityZone:
                description: 'AvailabilityZone: availability zone information of the
                  server.'
                type: string
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                maxLength: 63
                minLength: 3
                pattern: ^[a-zA-Z0-9]+(-[a-zA-Z0-9]+)*
                type: string
              backup:
                description: 'Backup: Backup properties of a server.'
                properties:
                  backupRetentionDays:
                    description: 'BackupRetentionDays: Backup retention days for the
                      server.'
                    type: integer
                  geoRedundantBackup:
                    description: 'GeoRedundantBackup: A value indicating whether Geo-Redundant
                      backup is enabled on the server.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                type: object
              createMode:
                description: 'CreateMode: The mode to create a new PostgreSQL server.'
                enum:
                - Create
                - Default
                - GeoRestore
                - PointInTimeRestore
                - Replica
                - ReviveDropped
                - Update
                type: string
              dataEncryption:
                description: 'DataEncryption: Data encryption properties of a server.'
                properties:
                  geoBackupEncryptionKeyStatus:
                    description: 'GeoBackupEncryptionKeyStatus: Geo-backup encryption
                      key status for Data encryption enabled server.'
                    enum:
                    - Invalid
                    - Valid
                    type: string
                  geoBackupKeyURI:
                    description: 'GeoBackupKeyURI: URI for the key in keyvault for
                      data encryption for geo-backup of server.'
                    type: string
                  geoBackupKeyURIFromConfig:
                    description: 'GeoBackupKeyURIFromConfig: URI for the key in keyvault
                      for data encryption for geo-backup of server.'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  geoBackupUserAssignedIdentityReference:
                    description: |-
                      GeoBackupUserAssignedIdentityReference: Resource Id for the User assigned identity to be used for data encryption for
                      geo-backup of server.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  primaryEncryptionKeyStatus:
                    description: 'PrimaryEncryptionKeyStatus: Primary encryption key
                      status for Data encryption enabled server.'
                    enum:
                    - Invalid
                    - Valid
                    type: string
                  primaryKeyURI:
                    description: 'PrimaryKeyURI: URI for the key in keyvault for data
                      encryption of the primary server.'
                    type: string
                  primaryKeyURIFromConfig:
                    description: 'PrimaryKeyURIFromConfig: URI for the key in keyvault
                      for data encryption of the primary server.'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  primaryUserAssignedIdentityReference:
                    description: |-
                      PrimaryUserAssignedIdentityReference: Resource Id for the User assigned identity to be used for data encryption of the
                      primary server.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  type:
                    description: 'Type: Data encryption type to depict if it is System
                      Managed vs Azure Key vault.'
                    enum:
                    - AzureKeyVault
                    - SystemManaged
                    type: string
                type: object
              highAvailability:
                description: 'HighAvailability: High availability properties of a
                  server.'
                properties:
                  mode:
                    description: 'Mode: The HA mode for the server.'
                    enum:
                    - Disabled
                    - SameZone
                    - ZoneRedundant
                    type: string
                  standbyAvailabilityZone:
                    description: 'StandbyAvailabilityZone: availability zone information
                      of the standby.'
                    type: string
                type: object
              identity:
                description: 'Identity: Describes the identity of the application.'
                properties:
                  type:
                    description: 'Type: the types of identities associated with this
                      resource; currently restricted to ''None and UserAssigned'''
                    enum:
                    - None
                    - UserAssigned
                    type: string
                  userAssignedIdentities:
                    description: 'UserAssignedIdentities: represents user assigned
                      identities map.'
                    items:
                      description: Information about the user assigned identity for
                        the resource
                      properties:
                        reference:
                          description: ResourceReference represents a resource reference,
                            either to a Kubernetes resource or directly to an Azure
                            resource via ARMID
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                required:
                - type
                type: object
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              maintenanceWindow:
                description: 'MaintenanceWindow: Maintenance window properties of
                  a server.'
                properties:
                  customWindow:
                    description: 'CustomWindow: indicates whether custom window is
                      enabled or disabled'
                    type: string
                  dayOfWeek:
                    description: 'DayOfWeek: day of week for maintenance window'
                    type: integer
                  startHour:
                    description: 'StartHour: start hour for maintenance window'
                    type: integer
                  startMinute:
                    description: 'StartMinute: start minute for maintenance window'
                    type: integer
                type: object
              network:
                description: |-
                  Network: Network properties of a server. This Network property is required to be passed only in case you want the server
                  to be Private access server.
                properties:
                  delegatedSubnetResourceReference:
                    description: |-
                      DelegatedSubnetResourceReference: Delegated subnet arm resource id. This is required to be passed during create, in case
                      we want the server to be VNET injected, i.e. Private access server. During update, pass this only if we want to update
                      the value for Private DNS zone.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  privateDnsZoneArmResourceReference:
                    description: |-
                      PrivateDnsZoneArmResourceReference: Private dns zone arm resource id. This is required to be passed during create, in
                      case we want the server to be VNET injected, i.e. Private access server. During update, pass this only if we want to
                      update the value for Private DNS zone.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  publicNetworkAccess:
                    description: 'PublicNetworkAccess: public network access is enabled
                      or not'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                type: object
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  configMaps:
                    description: 'ConfigMaps: configures where to place operator written
                      ConfigMaps.'
                    properties:
                      fullyQualifiedDomainName:
                        description: |-
                          FullyQualifiedDomainName: indicates where the FullyQualifiedDomainName config map should be placed. If omitted, no
                          config map will be created.
                        properties:
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: 'Secrets: configures where to place Azure generated
                      secrets.'
                    properties:
                      fullyQualifiedDomainName:
                        description: |-
                          FullyQualifiedDomainName: indicates where the FullyQualifiedDomainName secret should be placed. If omitted, the secret
                          will not be retrieved from Azure.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              pointInTimeUTC:
                description: |-
                  PointInTimeUTC: Restore point creation time (ISO8601 format), specifying the time to restore from. It's required when
                  'createMode' is 'PointInTimeRestore' or 'GeoRestore' or 'ReviveDropped'.
                type: string
              replica:
                description: |-
                  Replica: Replica properties of a server. These Replica properties are required to be passed only in case you want to
                  Promote a server.
                properties:
                  promoteMode:
                    description: 'PromoteMode: Sets the promote mode for a replica
                      server. This is a write only property.'
                    enum:
                    - standalone
                    - switchover
                    type: string
                  promoteOption:
                    description: 'PromoteOption: Sets the promote options for a replica
                      server. This is a write only property.'
                    enum:
                    - forced
                    - planned
                    type: string
                  role:
                    description: 'Role: Used to indicate role of the server in replication
                      set.'
                    enum:
                    - AsyncReplica
                    - GeoAsyncReplica
                    - None
                    - Primary
                    type: string
                type: object
              replicationRole:
                description: 'ReplicationRole: Replication role of the server'
                enum:
                - AsyncReplica
                - GeoAsyncReplica
                - None
                - Primary
                type: string
              sku:
                description: 'Sku: The SKU (pricing tier) of the server.'
                properties:
                  name:
                    description: 'Name: The name of the sku, typically, tier + family
                      + cores, e.g. Standard_D4s_v3.'
                    type: string
                  tier:
                    description: 'Tier: The tier of the particular SKU, e.g. Burstable.'
                    enum:
                    - Burstable
                    - GeneralPurpose
                    - MemoryOptimized
                    type: string
                required:
                - name
                - tier
                type: object
              sourceServerResourceReference:
                description: |-
                  SourceServerResourceReference: The source server resource ID to restore from. It's required when 'createMode' is
                  'PointInTimeRestore' or 'GeoRestore' or 'Replica' or 'ReviveDropped'. This property is returned only for Replica server
                properties:
                  armId:
                    description: |-
                      ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                      The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                      ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: Name is the Kubernetes name of the resource.
                    type: string
                type: object
              storage:
                description: 'Storage: Storage properties of a server.'
                properties:
                  autoGrow:
                    description: 'AutoGrow: Flag to enable / disable Storage Auto
                      grow for flexible server.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                  iops:
                    description: 'Iops: Storage tier IOPS quantity. This property
                      is required to be set for storage Type PremiumV2_LRS'
                    type: integer
                  storageSizeGB:
                    description: 'StorageSizeGB: Max storage allowed for a server.'
                    type: integer
                  throughput:
                    description: 'Throughput: Storage throughput for the server. This
                      is required to be set for storage Type PremiumV2_LRS'
                    type: integer
                  tier:
                    description: 'Tier: Name of storage tier for IOPS.'
                    enum:
                    - P1
                    - P10
                    - P15
                    - P2
                    - P20
                    - P3
                    - P30
                    - P4
                    - P40
                    - P50
                    - P6
                    - P60
                    - P70
                    - P80
                    type: string
                  type:
                    description: |-
                      Type: Storage type for the server. Allowed values are Premium_LRS and PremiumV2_LRS, and default is Premium_LRS if not
                      specified
                    enum:
                    - PremiumV2_LRS
                    - Premium_LRS
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              version:
                description: 'Version: PostgreSQL Server version.'
                enum:
                - "11"
                - "12"
                - "13"
                - "14"
                - "15"
                - "16"
                type: string
            required:
            - location
            - owner
            type: object
          status:
            properties:
              administratorLogin:
                description: |-
                  AdministratorLogin: The administrator's login name of a server. Can only be specified when the server is being created
                  (and is required for creation).
                type: string
              authConfig:
                description: 'AuthConfig: AuthConfig properties of a server.'
                properties:
                  activeDirectoryAuth:
                    description: 'ActiveDirectoryAuth: If Enabled, Azure Active Directory
                      authentication is enabled.'
                    type: string
                  passwordAuth:
                    description: 'PasswordAuth: If Enabled, Password authentication
                      is enabled.'
                    type: string
                  tenantId:
                    description: 'TenantId: Tenant id of the server.'
                    type: string
                type: object
              availabilityZone:
                description: 'AvailabilityZone: availability zone information of the
                  server.'
                type: string
              backup:
                description: 'Backup: Backup properties of a server.'
                properties:
                  backupRetentionDays:
                    description: 'BackupRetentionDays: Backup retention days for the
                      server.'
                    type: integer
                  earliestRestoreDate:
                    description: 'EarliestRestoreDate: The earliest restore point
                      time (ISO8601 format) for server.'
                    type: string
                  geoRedundantBackup:
                    description: 'GeoRedundantBackup: A value indicating whether Geo-Redundant
                      backup is enabled on the server.'
                    type: string
                type: object
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createMode:
                description: 'CreateMode: The mode to create a new PostgreSQL server.'
                type: string
              dataEncryption:
                description: 'DataEncryption: Data encryption properties of a server.'
                properties:
                  geoBackupEncryptionKeyStatus:
                    description: 'GeoBackupEncryptionKeyStatus: Geo-backup encryption
                      key status for Data encryption enabled server.'
                    type: string
                  geoBackupKeyURI:
                    description: 'GeoBackupKeyURI: URI for the key in keyvault for
                      data encryption for geo-backup of server.'
                    type: string
                  geoBackupUserAssignedIdentityId:
                    description: |-
                      GeoBackupUserAssignedIdentityId: Resource Id for the User assigned identity to be used for data encryption for
                      geo-backup of server.
                    type: string
                  primaryEncryptionKeyStatus:
                    description: 'PrimaryEncryptionKeyStatus: Primary encryption key
                      status for Data encryption enabled server.'
                    type: string
                  primaryKeyURI:
                    description: 'PrimaryKeyURI: URI for the key in keyvault for data
                      encryption of the primary server.'
                    type: string
                  primaryUserAssignedIdentityId:
                    description: |-
                      PrimaryUserAssignedIdentityId: Resource Id for the User assigned identity to be used for data encryption of the primary
                      server.
                    type: string
                  type:
                    description: 'Type: Data encryption type to depict if it is System
                      Managed vs Azure Key vault.'
                    type: string
                type: object
              fullyQualifiedDomainName:
                description: 'FullyQualifiedDomainName: The fully qualified domain
                  name of a server.'
                type: string
              highAvailability:
                description: 'HighAvailability: High availability properties of a
                  server.'
                properties:
                  mode:
                    description: 'Mode: The HA mode for the server.'
                    type: string
                  standbyAvailabilityZone:
                    description: 'StandbyAvailabilityZone: availability zone information
                      of the standby.'
                    type: string
                  state:
                    description: 'State: A state of a HA server that is visible to
                      user.'
                    type: string
                type: object
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. E.g.
                  "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}"
                type: string
              identity:
                description: 'Identity: Describes the identity of the application.'
                properties:
                  tenantId:
                    description: 'TenantId: Tenant id of the server.'
                    type: string
                  type:
                    description: 'Type: the types of identities associated with this
                      resource; currently restricted to ''None and UserAssigned'''
                    type: string
                  userAssignedIdentities:
                    additionalProperties:
                      description: Describes a single user-assigned identity associated
                        with the application.
                      properties:
                        clientId:
                          description: 'ClientId: the client identifier of the Service
                            Principal which this identity represents.'
                          type: string
                        principalId:
                          description: 'PrincipalId: the object identifier of the
                            Service Principal which this identity represents.'
                          type: string
                      type: object
                    description: 'UserAssignedIdentities: represents user assigned
                      identities map.'
                    type: object
                type: object
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              maintenanceWindow:
                description: 'MaintenanceWindow: Maintenance window properties of
                  a server.'
                properties:
                  customWindow:
                    description: 'CustomWindow: indicates whether custom window is
                      enabled or disabled'
                    type: string
                  dayOfWeek:
                    description: 'DayOfWeek: day of week for maintenance window'
                    type: integer
                  startHour:
                    description: 'StartHour: start hour for maintenance window'
                    type: integer
                  startMinute:
                    description: 'StartMinute: start minute for maintenance window'
                    type: integer
                type: object
              minorVersion:
                description: 'MinorVersion: The minor version of the server.'
                type: string
              name:
                description: 'Name: The name of the resource'
                type: string
              network:
                description: |-
                  Network: Network properties of a server. This Network property is required to be passed only in case you want the server
                  to be Private access server.
                properties:
                  delegatedSubnetResourceId:
                    description: |-
                      DelegatedSubnetResourceId: Delegated subnet arm resource id. This is required to be passed during create, in case we
                      want the server to be VNET injected, i.e. Private access server. During update, pass this only if we want to update the
                      value for Private DNS zone.
                    type: string
                  privateDnsZoneArmResourceId:
                    description: |-
                      PrivateDnsZoneArmResourceId: Private dns zone arm resource id. This is required to be passed during create, in case we
                      want the server to be VNET injected, i.e. Private access server. During update, pass this only if we want to update the
                      value for Private DNS zone.
                    type: string
                  publicNetworkAccess:
                    description: 'PublicNetworkAccess: public network access is enabled
                      or not'
                    type: string
                type: object
              pointInTimeUTC:
                description: |-
                  PointInTimeUTC: Restore point creation time (ISO8601 format), specifying the time to restore from. It's required when
                  'createMode' is 'PointInTimeRestore' or 'GeoRestore' or 'ReviveDropped'.
                type: string
              privateEndpointConnections:
                description: 'PrivateEndpointConnections: List of private endpoint
                  connections associated with the specified resource.'
                items:
                  description: The private endpoint connection resource.
                  properties:
                    id:
                      description: |-
                        Id: Fully qualified resource ID for the resource. E.g.
                        "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}"
                      type: string
                  type: object
                type: array
              replica:
                description: |-
                  Replica: Replica properties of a server. These Replica properties are required to be passed only in case you want to
                  Promote a server.
                properties:
                  capacity:
                    description: 'Capacity: Replicas allowed for a server.'
                    type: integer
                  promoteMode:
                    description: 'PromoteMode: Sets the promote mode for a replica
                      server. This is a write only property.'
                    type: string
                  promoteOption:
                    description: 'PromoteOption: Sets the promote options for a replica
                      server. This is a write only property.'
                    type: string
                  replicationState:
                    description: |-
                      ReplicationState: Gets the replication state of a replica server. This property is returned only for replicas api call.
                      Supported values are Active, Catchup, Provisioning, Updating, Broken, Reconfiguring
                    type: string
                  role:
                    description: 'Role: Used to indicate role of the server in replication
                      set.'
                    type: string
                type: object
              replicaCapacity:
                description: 'ReplicaCapacity: Replicas allowed for a server.'
                type: integer
              replicationRole:
                description: 'ReplicationRole: Replication role of the server'
                type: string
              sku:
                description: 'Sku: The SKU (pricing tier) of the server.'
                properties:
                  name:
                    description: 'Name: The name of the sku, typically, tier + family
                      + cores, e.g. Standard_D4s_v3.'
                    type: string
                  tier:
                    description: 'Tier: The tier of the particular SKU, e.g. Burstable.'
                    type: string
                type: object
              sourceServerResourceId:
                description: |-
                  SourceServerResourceId: The source server resource ID to restore from. It's required when 'createMode' is
                  'PointInTimeRestore' or 'GeoRestore' or 'Replica' or 'ReviveDropped'. This property is returned only for Replica server
                type: string
              state:
                description: 'State: A state of a server that is visible to user.'
                type: string
              storage:
                description: 'Storage: Storage properties of a server.'
                properties:
                  autoGrow:
                    description: 'AutoGrow: Flag to enable / disable Storage Auto
                      grow for flexible server.'
                    type: string
                  iops:
                    description: 'Iops: Storage tier IOPS quantity. This property
                      is required to be set for storage Type PremiumV2_LRS'
                    type: integer
                  storageSizeGB:
                    description: 'StorageSizeGB: Max storage allowed for a server.'
                    type: integer
                  throughput:
                    description: 'Throughput: Storage throughput for the server. This
                      is required to be set for storage Type PremiumV2_LRS'
                    type: integer
                  tier:
                    description: 'Tier: Name of storage tier for IOPS.'
                    type: string
                  type:
                    description: |-
                      Type: Storage type for the server. Allowed values are Premium_LRS and PremiumV2_LRS, and default is Premium_LRS if not
                      specified
                    type: string
                type: object
              systemData:
                description: 'SystemData: Azure Resource Manager metadata containing
                  createdBy and modifiedBy information.'
                properties:
                  createdAt:
                    description: 'CreatedAt: The timestamp of resource creation (UTC).'
                    type: string
                  createdBy:
                    description: 'CreatedBy: The identity that created the resource.'
                    type: string
                  createdByType:
                    description: 'CreatedByType: The type of identity that created
                      the resource.'
                    type: string
                  lastModifiedAt:
                    description: 'LastModifiedAt: The timestamp of resource last modification
                      (UTC)'
                    type: string
                  lastModifiedBy:
                    description: 'LastModifiedBy: The identity that last modified
                      the resource.'
                    type: string
                  lastModifiedByType:
                    description: 'LastModifiedByType: The type of identity that last
                      modified the resource.'
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
              version:
                description: 'Version: PostgreSQL Server version.'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20230601previewstorage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20230601preview.FlexibleServer
          Generator information:
          - Generated from: /postgresql/resource-manager/Microsoft.DBforPostgreSQL/preview/2023-06-01-preview/FlexibleServers.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DBforPostgreSQL/flexibleServers/{serverName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20230601preview.FlexibleServer_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              administratorLogin:
                type: string
              administratorLoginPassword:
                description: |-
                  SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                  the resource it is on.
                properties:
                  key:
                    description: Key is the key in the Kubernetes secret being referenced
                    type: string
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - key
                - name
                type: object
              authConfig:
                description: |-
                  Storage version of v1api20230601preview.AuthConfig
                  Authentication configuration properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  activeDirectoryAuth:
                    type: string
                  passwordAuth:
                    type: string
                  tenantId:
                    type: string
                type: object
              availabilityZone:
                type: string
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              backup:
                description: |-
                  Storage version of v1api20230601preview.Backup
                  Backup properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  backupRetentionDays:
                    type: integer
                  geoRedundantBackup:
                    type: string
                type: object
              createMode:
                type: string
              dataEncryption:
                description: |-
                  Storage version of v1api20230601preview.DataEncryption
                  Data encryption properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  geoBackupEncryptionKeyStatus:
                    type: string
                  geoBackupKeyURI:
                    type: string
                  geoBackupKeyURIFromConfig:
                    description: |-
                      ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  geoBackupUserAssignedIdentityReference:
                    description: |-
                      GeoBackupUserAssignedIdentityReference: Resource Id for the User assigned identity to be used for data encryption for
                      geo-backup of server.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  primaryEncryptionKeyStatus:
                    type: string
                  primaryKeyURI:
                    type: string
                  primaryKeyURIFromConfig:
                    description: |-
                      ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  primaryUserAssignedIdentityReference:
                    description: |-
                      PrimaryUserAssignedIdentityReference: Resource Id for the User assigned identity to be used for data encryption of the
                      primary server.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  type:
                    type: string
                type: object
              highAvailability:
                description: |-
                  Storage version of v1api20230601preview.HighAvailability
                  High availability properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  mode:
                    type: string
                  standbyAvailabilityZone:
                    type: string
                type: object
              identity:
                description: |-
                  Storage version of v1api20230601preview.UserAssignedIdentity
                  Information describing the identities associated with this application.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  type:
                    type: string
                  userAssignedIdentities:
                    items:
                      description: |-
                        Storage version of v1api20230601preview.UserAssignedIdentityDetails
                        Information about the user assigned identity for the resource
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        reference:
                          description: ResourceReference represents a resource reference,
                            either to a Kubernetes resource or directly to an Azure
                            resource via ARMID
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              location:
                type: string
              maintenanceWindow:
                description: |-
                  Storage version of v1api20230601preview.MaintenanceWindow
                  Maintenance window properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  customWindow:
                    type: string
                  dayOfWeek:
                    type: integer
                  startHour:
                    type: integer
                  startMinute:
                    type: integer
                type: object
              network:
                description: |-
                  Storage version of v1api20230601preview.Network
                  Network properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  delegatedSubnetResourceReference:
                    description: |-
                      DelegatedSubnetResourceReference: Delegated subnet arm resource id. This is required to be passed during create, in case
                      we want the server to be VNET injected, i.e. Private access server. During update, pass this only if we want to update
                      the value for Private DNS zone.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  privateDnsZoneArmResourceReference:
                    description: |-
                      PrivateDnsZoneArmResourceReference: Private dns zone arm resource id. This is required to be passed during create, in
                      case we want the server to be VNET injected, i.e. Private access server. During update, pass this only if we want to
                      update the value for Private DNS zone.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  publicNetworkAccess:
                    type: string
                type: object
              operatorSpec:
                description: |-
                  Storage version of v1api20230601preview.FlexibleServerOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  configMaps:
                    description: Storage version of v1api20230601preview.FlexibleServerOperatorConfigMaps
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      fullyQualifiedDomainName:
                        description: |-
                          ConfigMapDestination describes the location to store a single configmap value
                          Note: This is similar to: SecretDestination in secrets.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: Storage version of v1api20230601preview.FlexibleServerOperatorSecrets
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      fullyQualifiedDomainName:
                        description: |-
                          SecretDestination describes the location to store a single secret value.
                          Note: This is similar to: ConfigMapDestination in configmaps.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              pointInTimeUTC:
                type: string
              replica:
                description: |-
                  Storage version of v1api20230601preview.Replica
                  Replica properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  promoteMode:
                    type: string
                  promoteOption:
                    type: string
                  role:
                    type: string
                type: object
              replicationRole:
                type: string
              sku:
                description: |-
                  Storage version of v1api20230601preview.Sku
                  Sku information related properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              sourceServerResourceReference:
                description: |-
                  SourceServerResourceReference: The source server resource ID to restore from. It's required when 'createMode' is
                  'PointInTimeRestore' or 'GeoRestore' or 'Replica' or 'ReviveDropped'. This property is returned only for Replica server
                properties:
                  armId:
                    description: |-
                      ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                      The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                      ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: Name is the Kubernetes name of the resource.
                    type: string
                type: object
              storage:
                description: |-
                  Storage version of v1api20230601preview.Storage
                  Storage properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  autoGrow:
                    type: string
                  iops:
                    type: integer
                  storageSizeGB:
                    type: integer
                  throughput:
                    type: integer
                  tier:
                    type: string
                  type:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              version:
                type: string
            required:
            - owner
            type: object
          status:
            description: Storage version of v1api20230601preview.FlexibleServer_STATUS
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              administratorLogin:
                type: string
              authConfig:
                description: |-
                  Storage version of v1api20230601preview.AuthConfig_STATUS
                  Authentication configuration properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  activeDirectoryAuth:
                    type: string
                  passwordAuth:
                    type: string
                  tenantId:
                    type: string
                type: object
              availabilityZone:
                type: string
              backup:
                description: |-
                  Storage version of v1api20230601preview.Backup_STATUS
                  Backup properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  backupRetentionDays:
                    type: integer
                  earliestRestoreDate:
                    type: string
                  geoRedundantBackup:
                    type: string
                type: object
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createMode:
                type: string
              dataEncryption:
                description: |-
                  Storage version of v1api20230601preview.DataEncryption_STATUS
                  Data encryption properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  geoBackupEncryptionKeyStatus:
                    type: string
                  geoBackupKeyURI:
                    type: string
                  geoBackupUserAssignedIdentityId:
                    type: string
                  primaryEncryptionKeyStatus:
                    type: string
                  primaryKeyURI:
                    type: string
                  primaryUserAssignedIdentityId:
                    type: string
                  type:
                    type: string
                type: object
              fullyQualifiedDomainName:
                type: string
              highAvailability:
                description: |-
                  Storage version of v1api20230601preview.HighAvailability_STATUS
                  High availability properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  mode:
                    type: string
                  standbyAvailabilityZone:
                    type: string
                  state:
                    type: string
                type: object
              id:
                type: string
              identity:
                description: |-
                  Storage version of v1api20230601preview.UserAssignedIdentity_STATUS
                  Information describing the identities associated with this application.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  tenantId:
                    type: string
                  type:
                    type: string
                  userAssignedIdentities:
                    additionalProperties:
                      description: |-
                        Storage version of v1api20230601preview.UserIdentity_STATUS
                        Describes a single user-assigned identity associated with the application.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        clientId:
                          type: string
                        principalId:
                          type: string
                      type: object
                    type: object
                type: object
              location:
                type: string
              maintenanceWindow:
                description: |-
                  Storage version of v1api20230601preview.MaintenanceWindow_STATUS
                  Maintenance window properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  customWindow:
                    type: string
                  dayOfWeek:
                    type: integer
                  startHour:
                    type: integer
                  startMinute:
                    type: integer
                type: object
              minorVersion:
                type: string
              name:
                type: string
              network:
                description: |-
                  Storage version of v1api20230601preview.Network_STATUS
                  Network properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  delegatedSubnetResourceId:
                    type: string
                  privateDnsZoneArmResourceId:
                    type: string
                  publicNetworkAccess:
                    type: string
                type: object
              pointInTimeUTC:
                type: string
              privateEndpointConnections:
                items:
                  description: |-
                    Storage version of v1api20230601preview.PrivateEndpointConnection_STATUS
                    The private endpoint connection resource.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    id:
                      type: string
                  type: object
                type: array
              replica:
                description: |-
                  Storage version of v1api20230601preview.Replica_STATUS
                  Replica properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  capacity:
                    type: integer
                  promoteMode:
                    type: string
                  promoteOption:
                    type: string
                  replicationState:
                    type: string
                  role:
                    type: string
                type: object
              replicaCapacity:
                type: integer
              replicationRole:
                type: string
              sku:
                description: |-
                  Storage version of v1api20230601preview.Sku_STATUS
                  Sku information related properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              sourceServerResourceId:
                type: string
              state:
                type: string
              storage:
                description: |-
                  Storage version of v1api20230601preview.Storage_STATUS
                  Storage properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  autoGrow:
                    type: string
                  iops:
                    type: integer
                  storageSizeGB:
                    type: integer
                  throughput:
                    type: integer
                  tier:
                    type: string
                  type:
                    type: string
                type: object
              systemData:
                description: |-
                  Storage version of v1api20230601preview.SystemData_STATUS
                  Metadata pertaining to creation and last modification of the resource.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  createdAt:
                    type: string
                  createdBy:
                    type: string
                  createdByType:
                    type: string
                  lastModifiedAt:
                    type: string
                  lastModifiedBy:
                    type: string
                  lastModifiedByType:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              type:
                type: string
              version:
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20240801
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /postgresql/resource-manager/Microsoft.DBforPostgreSQL/stable/2024-08-01/FlexibleServers.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DBforPostgreSQL/flexibleServers/{serverName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              administratorLogin:
                description: |-
                  AdministratorLogin: Name of the login designated as the first password based administrator assigned to your instance of
                  PostgreSQL. Must be specified the first time that you enable password based authentication on a server. Once set to a
                  given value, it cannot be changed for the rest of the life of a server. If you disable password based authentication on
                  a server which had it enabled, this password based role isn't deleted.
                type: string
              administratorLoginPassword:
                description: |-
                  AdministratorLoginPassword: Password assigned to the administrator login. As long as password authentication is enabled,
                  this password can be changed at any time.
                properties:
                  key:
                    description: Key is the key in the Kubernetes secret being referenced
                    type: string
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - key
                - name
                type: object
              authConfig:
                description: 'AuthConfig: Authentication configuration properties
                  of a flexible server.'
                properties:
                  activeDirectoryAuth:
                    description: 'ActiveDirectoryAuth: Indicates if the server supports
                      Microsoft Entra authentication.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                  passwordAuth:
                    description: 'PasswordAuth: Indicates if the server supports password
                      based authentication.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                  tenantId:
                    description: 'TenantId: Identifier of the tenant of the delegated
                      resource.'
                    type: string
                type: object
              availabilityZone:
                description: 'AvailabilityZone: Availability zone of a flexible server.'
                type: string
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                maxLength: 63
                minLength: 3
                pattern: ^[a-zA-Z0-9]+(-[a-zA-Z0-9]+)*
                type: string
              backup:
                description: 'Backup: Backup properties of a flexible server.'
                properties:
                  backupRetentionDays:
                    description: 'BackupRetentionDays: Backup retention days for the
                      flexible server.'
                    type: integer
                  geoRedundantBackup:
                    description: 'GeoRedundantBackup: Indicates if the server is configured
                      to create geographically redundant backups.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                type: object
              createMode:
                description: 'CreateMode: Creation mode of a new flexible server.'
                enum:
                - Create
                - Default
                - GeoRestore
                - PointInTimeRestore
                - Replica
                - ReviveDropped
                - Update
                type: string
              dataEncryption:
                description: 'DataEncryption: Data encryption properties of a flexible
                  server.'
                properties:
                  geoBackupEncryptionKeyStatus:
                    description: |-
                      GeoBackupEncryptionKeyStatus: Status of key used by a flexible server configured with data encryption based on customer
                      managed key, to encrypt the geographically redundant storage associated to the server when it is configured to support
                      geographically redundant backups.
                    enum:
                    - Invalid
                    - Valid
                    type: string
                  geoBackupKeyURI:
                    description: |-
                      GeoBackupKeyURI: Identifier of the user assigned managed identity used to access the key in Azure Key Vault for data
                      encryption of the geographically redundant storage associated to a flexible server that is configured to support
                      geographically redundant backups.
                    type: string
                  geoBackupKeyURIFromConfig:
                    description: |-
                      GeoBackupKeyURIFromConfig: Identifier of the user assigned managed identity used to access the key in Azure Key Vault
                      for data encryption of the geographically redundant storage associated to a flexible server that is configured to
                      support geographically redundant backups.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  geoBackupUserAssignedIdentityReference:
                    description: |-
                      GeoBackupUserAssignedIdentityReference: Identifier of the user assigned managed identity used to access the key in Azure
                      Key Vault for data encryption of the geographically redundant storage associated to a flexible server that is configured
                      to support geographically redundant backups.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  primaryEncryptionKeyStatus:
                    description: |-
                      PrimaryEncryptionKeyStatus: Status of key used by a flexible server configured with data encryption based on customer
                      managed key, to encrypt the primary storage associated to the server.
                    enum:
                    - Invalid
                    - Valid
                    type: string
                  primaryKeyURI:
                    description: |-
                      PrimaryKeyURI: URI of the key in Azure Key Vault used for data encryption of the primary storage associated to a
                      flexible server.
                    type: string
                  primaryKeyURIFromConfig:
                    description: |-
                      PrimaryKeyURIFromConfig: URI of the key in Azure Key Vault used for data encryption of the primary storage associated to
                      a flexible server.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  primaryUserAssignedIdentityReference:
                    description: |-
                      PrimaryUserAssignedIdentityReference: Identifier of the user assigned managed identity used to access the key in Azure
                      Key Vault for data encryption of the primary storage associated to a flexible server.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  type:
                    description: 'Type: Data encryption type used by a flexible server.'
                    enum:
                    - AzureKeyVault
                    - SystemManaged
                    type: string
                type: object
              highAvailability:
                description: 'HighAvailability: High availability properties of a
                  flexible server.'
                properties:
                  mode:
                    description: 'Mode: High availability mode for a flexible server.'
                    enum:
                    - Disabled
                    - SameZone
                    - ZoneRedundant
                    type: string
                  standbyAvailabilityZone:
                    description: |-
                      StandbyAvailabilityZone: Availability zone associated to the standby server created when high availability is set to
                      SameZone or ZoneRedundant.
                    type: string
                type: object
              identity:
                description: 'Identity: User assigned managed identities assigned
                  to the flexible server.'
                properties:
                  principalId:
                    description: 'PrincipalId: Identifier of the object of the service
                      principal associated to the user assigned managed identity.'
                    type: string
                  type:
                    description: 'Type: Types of identities associated with a flexible
                      server.'
                    enum:
                    - None
                    - SystemAssigned
                    - SystemAssigned,UserAssigned
                    - UserAssigned
                    type: string
                  userAssignedIdentities:
                    description: 'UserAssignedIdentities: Map of user assigned managed
                      identities.'
                    items:
                      description: Information about the user assigned identity for
                        the resource
                      properties:
                        reference:
                          description: ResourceReference represents a resource reference,
                            either to a Kubernetes resource or directly to an Azure
                            resource via ARMID
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                required:
                - type
                type: object
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              maintenanceWindow:
                description: 'MaintenanceWindow: Maintenance window properties of
                  a flexible server.'
                properties:
                  customWindow:
                    description: 'CustomWindow: Indicates whether custom window is
                      enabled or disabled.'
                    type: string
                  dayOfWeek:
                    description: 'DayOfWeek: Day of the week to be used for maintenance
                      window.'
                    type: integer
                  startHour:
                    description: 'StartHour: Start hour to be used for maintenance
                      window.'
                    type: integer
                  startMinute:
                    description: 'StartMinute: Start minute to be used for maintenance
                      window.'
                    type: integer
                type: object
              network:
                description: |-
                  Network: Network properties of a flexible server. Only required if you want your server to be integrated into a virtual
                  network provided by customer.
                properties:
                  delegatedSubnetResourceReference:
                    description: |-
                      DelegatedSubnetResourceReference: Resource identifier of the delegated subnet. Required during creation of a new server,
                      in case you want the server to be integrated into your own virtual network. For an update operation, you only have to
                      provide this property if you want to change the value assigned for the private DNS zone.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  privateDnsZoneArmResourceReference:
                    description: |-
                      PrivateDnsZoneArmResourceReference: Identifier of the private DNS zone. Required during creation of a new server, in
                      case you want the server to be integrated into your own virtual network. For an update operation, you only have to
                      provide this property if you want to change the value assigned for the private DNS zone.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  publicNetworkAccess:
                    description: 'PublicNetworkAccess: Indicates if public network
                      access is enabled or not.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                type: object
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  configMaps:
                    description: 'ConfigMaps: configures where to place operator written
                      ConfigMaps.'
                    properties:
                      fullyQualifiedDomainName:
                        description: |-
                          FullyQualifiedDomainName: indicates where the FullyQualifiedDomainName config map should be placed. If omitted, no
                          config map will be created.
                        properties:
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: 'Secrets: configures where to place Azure generated
                      secrets.'
                    properties:
                      fullyQualifiedDomainName:
                        description: |-
                          FullyQualifiedDomainName: indicates where the FullyQualifiedDomainName secret should be placed. If omitted, the secret
                          will not be retrieved from Azure.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              pointInTimeUTC:
                description: |-
                  PointInTimeUTC: Creation time (in ISO8601 format) of the backup which you want to restore in the new flexible server.
                  It's required when 'createMode' is 'PointInTimeRestore', 'GeoRestore', or 'ReviveDropped'.
                type: string
              replica:
                description: 'Replica: Read replica properties of a flexible server.
                  Required only in case that you want to promote a server.'
                properties:
                  promoteMode:
                    description: |-
                      PromoteMode: Type of operation to apply on the read replica. This property is write only. Standalone means that the read
                      replica will be promoted to a standalone server, and will become a completely independent entity from the replication
                      set. Switchover means that the read replica will roles with the primary server.
                    enum:
                    - standalone
                    - switchover
                    type: string
                  promoteOption:
                    description: |-
                      PromoteOption: Data synchronization option to use when processing the operation specified in the promoteMode property
                      This property is write only. Planned means that the operation will wait for data in the read replica to be fully
                      synchronized with its source server before it initiates the operation. Forced means that the operation will not wait for
                      data in the read replica to be synchronized with its source server before it initiates the operation.
                    enum:
                    - forced
                    - planned
                    type: string
                  role:
                    description: 'Role: Role of the server in a replication set.'
                    enum:
                    - AsyncReplica
                    - GeoAsyncReplica
                    - None
                    - Primary
                    type: string
                type: object
              replicationRole:
                description: 'ReplicationRole: Role of the server in a replication
                  set.'
                enum:
                - AsyncReplica
                - GeoAsyncReplica
                - None
                - Primary
                type: string
              sku:
                description: 'Sku: Compute tier and size of a flexible server.'
                properties:
                  name:
                    description: 'Name: Name by which is known a given compute size
                      assigned to a flexible server.'
                    type: string
                  tier:
                    description: 'Tier: Tier of the compute assigned to a flexible
                      server.'
                    enum:
                    - Burstable
                    - GeneralPurpose
                    - MemoryOptimized
                    type: string
                required:
                - name
                - tier
                type: object
              sourceServerResourceReference:
                description: |-
                  SourceServerResourceReference: Identifier of the flexible server to be used as the source of the new flexible server.
                  Required when 'createMode' is 'PointInTimeRestore', 'GeoRestore', 'Replica', or 'ReviveDropped'. This property is
                  returned only when the target flexible server is a read replica.
                properties:
                  armId:
                    description: |-
                      ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                      The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                      ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: Name is the Kubernetes name of the resource.
                    type: string
                type: object
              storage:
                description: 'Storage: Storage properties of a flexible server.'
                properties:
                  autoGrow:
                    description: |-
                      AutoGrow: Flag to enable or disable the automatic growth of storage size of a flexible server when available space is
                      nearing zero and conditions allow for automatically growing storage size.
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                  iops:
                    description: 'Iops: Maximum IOPS supported for storage. Required
                      when type of storage is PremiumV2_LRS.'
                    type: integer
                  storageSizeGB:
                    description: 'StorageSizeGB: Size of storage assigned to a flexible
                      server.'
                    type: integer
                  throughput:
                    description: 'Throughput: Maximum throughput supported for storage.
                      Required when type of storage is PremiumV2_LRS.'
                    type: integer
                  tier:
                    description: 'Tier: Storage tier of a flexible server.'
                    enum:
                    - P1
                    - P10
                    - P15
                    - P2
                    - P20
                    - P3
                    - P30
                    - P4
                    - P40
                    - P50
                    - P6
                    - P60
                    - P70
                    - P80
                    type: string
                  type:
                    description: |-
                      Type: Type of storage assigned to a flexible server. Allowed values are Premium_LRS or PremiumV2_LRS. If not specified,
                      it defaults to Premium_LRS.
                    enum:
                    - PremiumV2_LRS
                    - Premium_LRS
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              version:
                description: 'Version: Major version of PostgreSQL database engine.'
                enum:
                - "11"
                - "12"
                - "13"
                - "14"
                - "15"
                - "16"
                type: string
            required:
            - location
            - owner
            type: object
          status:
            properties:
              administratorLogin:
                description: |-
                  AdministratorLogin: Name of the login designated as the first password based administrator assigned to your instance of
                  PostgreSQL. Must be specified the first time that you enable password based authentication on a server. Once set to a
                  given value, it cannot be changed for the rest of the life of a server. If you disable password based authentication on
                  a server which had it enabled, this password based role isn't deleted.
                type: string
              authConfig:
                description: 'AuthConfig: Authentication configuration properties
                  of a flexible server.'
                properties:
                  activeDirectoryAuth:
                    description: 'ActiveDirectoryAuth: Indicates if the server supports
                      Microsoft Entra authentication.'
                    type: string
                  passwordAuth:
                    description: 'PasswordAuth: Indicates if the server supports password
                      based authentication.'
                    type: string
                  tenantId:
                    description: 'TenantId: Identifier of the tenant of the delegated
                      resource.'
                    type: string
                type: object
              availabilityZone:
                description: 'AvailabilityZone: Availability zone of a flexible server.'
                type: string
              backup:
                description: 'Backup: Backup properties of a flexible server.'
                properties:
                  backupRetentionDays:
                    description: 'BackupRetentionDays: Backup retention days for the
                      flexible server.'
                    type: integer
                  earliestRestoreDate:
                    description: 'EarliestRestoreDate: Earliest restore point time
                      (ISO8601 format) for a flexible server.'
                    type: string
                  geoRedundantBackup:
                    description: 'GeoRedundantBackup: Indicates if the server is configured
                      to create geographically redundant backups.'
                    type: string
                type: object
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createMode:
                description: 'CreateMode: Creation mode of a new flexible server.'
                type: string
              dataEncryption:
                description: 'DataEncryption: Data encryption properties of a flexible
                  server.'
                properties:
                  geoBackupEncryptionKeyStatus:
                    description: |-
                      GeoBackupEncryptionKeyStatus: Status of key used by a flexible server configured with data encryption based on customer
                      managed key, to encrypt the geographically redundant storage associated to the server when it is configured to support
                      geographically redundant backups.
                    type: string
                  geoBackupKeyURI:
                    description: |-
                      GeoBackupKeyURI: Identifier of the user assigned managed identity used to access the key in Azure Key Vault for data
                      encryption of the geographically redundant storage associated to a flexible server that is configured to support
                      geographically redundant backups.
                    type: string
                  geoBackupUserAssignedIdentityId:
                    description: |-
                      GeoBackupUserAssignedIdentityId: Identifier of the user assigned managed identity used to access the key in Azure Key
                      Vault for data encryption of the geographically redundant storage associated to a flexible server that is configured to
                      support geographically redundant backups.
                    type: string
                  primaryEncryptionKeyStatus:
                    description: |-
                      PrimaryEncryptionKeyStatus: Status of key used by a flexible server configured with data encryption based on customer
                      managed key, to encrypt the primary storage associated to the server.
                    type: string
                  primaryKeyURI:
                    description: |-
                      PrimaryKeyURI: URI of the key in Azure Key Vault used for data encryption of the primary storage associated to a
                      flexible server.
                    type: string
                  primaryUserAssignedIdentityId:
                    description: |-
                      PrimaryUserAssignedIdentityId: Identifier of the user assigned managed identity used to access the key in Azure Key
                      Vault for data encryption of the primary storage associated to a flexible server.
                    type: string
                  type:
                    description: 'Type: Data encryption type used by a flexible server.'
                    type: string
                type: object
              fullyQualifiedDomainName:
                description: 'FullyQualifiedDomainName: Fully qualified domain name
                  of a flexible server.'
                type: string
              highAvailability:
                description: 'HighAvailability: High availability properties of a
                  flexible server.'
                properties:
                  mode:
                    description: 'Mode: High availability mode for a flexible server.'
                    type: string
                  standbyAvailabilityZone:
                    description: |-
                      StandbyAvailabilityZone: Availability zone associated to the standby server created when high availability is set to
                      SameZone or ZoneRedundant.
                    type: string
                  state:
                    description: 'State: Possible states of the standby server created
                      when high availability is set to SameZone or ZoneRedundant.'
                    type: string
                type: object
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. E.g.
                  "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}"
                type: string
              identity:
                description: 'Identity: User assigned managed identities assigned
                  to the flexible server.'
                properties:
                  principalId:
                    description: 'PrincipalId: Identifier of the object of the service
                      principal associated to the user assigned managed identity.'
                    type: string
                  tenantId:
                    description: 'TenantId: Identifier of the tenant of a flexible
                      server.'
                    type: string
                  type:
                    description: 'Type: Types of identities associated with a flexible
                      server.'
                    type: string
                  userAssignedIdentities:
                    additionalProperties:
                      description: User assigned managed identity associated with
                        a flexible server.
                      properties:
                        clientId:
                          description: 'ClientId: Identifier of the client of the
                            service principal associated to the user assigned managed
                            identity.'
                          type: string
                        principalId:
                          description: 'PrincipalId: Identifier of the object of the
                            service principal associated to the user assigned managed
                            identity.'
                          type: string
                      type: object
                    description: 'UserAssignedIdentities: Map of user assigned managed
                      identities.'
                    type: object
                type: object
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              maintenanceWindow:
                description: 'MaintenanceWindow: Maintenance window properties of
                  a flexible server.'
                properties:
                  customWindow:
                    description: 'CustomWindow: Indicates whether custom window is
                      enabled or disabled.'
                    type: string
                  dayOfWeek:
                    description: 'DayOfWeek: Day of the week to be used for maintenance
                      window.'
                    type: integer
                  startHour:
                    description: 'StartHour: Start hour to be used for maintenance
                      window.'
                    type: integer
                  startMinute:
                    description: 'StartMinute: Start minute to be used for maintenance
                      window.'
                    type: integer
                type: object
              minorVersion:
                description: 'MinorVersion: Minor version of PostgreSQL database engine.'
                type: string
              name:
                description: 'Name: The name of the resource'
                type: string
              network:
                description: |-
                  Network: Network properties of a flexible server. Only required if you want your server to be integrated into a virtual
                  network provided by customer.
                properties:
                  delegatedSubnetResourceId:
                    description: |-
                      DelegatedSubnetResourceId: Resource identifier of the delegated subnet. Required during creation of a new server, in
                      case you want the server to be integrated into your own virtual network. For an update operation, you only have to
                      provide this property if you want to change the value assigned for the private DNS zone.
                    type: string
                  privateDnsZoneArmResourceId:
                    description: |-
                      PrivateDnsZoneArmResourceId: Identifier of the private DNS zone. Required during creation of a new server, in case you
                      want the server to be integrated into your own virtual network. For an update operation, you only have to provide this
                      property if you want to change the value assigned for the private DNS zone.
                    type: string
                  publicNetworkAccess:
                    description: 'PublicNetworkAccess: Indicates if public network
                      access is enabled or not.'
                    type: string
                type: object
              pointInTimeUTC:
                description: |-
                  PointInTimeUTC: Creation time (in ISO8601 format) of the backup which you want to restore in the new flexible server.
                  It's required when 'createMode' is 'PointInTimeRestore', 'GeoRestore', or 'ReviveDropped'.
                type: string
              privateEndpointConnections:
                description: 'PrivateEndpointConnections: List of private endpoint
                  connections associated with the specified flexible server.'
                items:
                  description: The private endpoint connection resource.
                  properties:
                    id:
                      description: |-
                        Id: Fully qualified resource ID for the resource. E.g.
                        "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}"
                      type: string
                  type: object
                type: array
              replica:
                description: 'Replica: Read replica properties of a flexible server.
                  Required only in case that you want to promote a server.'
                properties:
                  capacity:
                    description: 'Capacity: Maximum number of read replicas allowed
                      for a flexible server.'
                    type: integer
                  promoteMode:
                    description: |-
                      PromoteMode: Type of operation to apply on the read replica. This property is write only. Standalone means that the read
                      replica will be promoted to a standalone server, and will become a completely independent entity from the replication
                      set. Switchover means that the read replica will roles with the primary server.
                    type: string
                  promoteOption:
                    description: |-
                      PromoteOption: Data synchronization option to use when processing the operation specified in the promoteMode property
                      This property is write only. Planned means that the operation will wait for data in the read replica to be fully
                      synchronized with its source server before it initiates the operation. Forced means that the operation will not wait for
                      data in the read replica to be synchronized with its source server before it initiates the operation.
                    type: string
                  replicationState:
                    description: |-
                      ReplicationState: Indicates the replication state of a read replica. This property is returned only when the target
                      flexible server is a read replica. Possible  values are Active, Broken, Catchup, Provisioning, Reconfiguring, and
                      Updating
                    type: string
                  role:
                    description: 'Role: Role of the server in a replication set.'
                    type: string
                type: object
              replicaCapacity:
                description: 'ReplicaCapacity: Maximum number of read replicas allowed
                  for a flexible server.'
                type: integer
              replicationRole:
                description: 'ReplicationRole: Role of the server in a replication
                  set.'
                type: string
              sku:
                description: 'Sku: Compute tier and size of a flexible server.'
                properties:
                  name:
                    description: 'Name: Name by which is known a given compute size
                      assigned to a flexible server.'
                    type: string
                  tier:
                    description: 'Tier: Tier of the compute assigned to a flexible
                      server.'
                    type: string
                type: object
              sourceServerResourceId:
                description: |-
                  SourceServerResourceId: Identifier of the flexible server to be used as the source of the new flexible server. Required
                  when 'createMode' is 'PointInTimeRestore', 'GeoRestore', 'Replica', or 'ReviveDropped'. This property is returned only
                  when the target flexible server is a read replica.
                type: string
              state:
                description: 'State: Possible states of a flexible server.'
                type: string
              storage:
                description: 'Storage: Storage properties of a flexible server.'
                properties:
                  autoGrow:
                    description: |-
                      AutoGrow: Flag to enable or disable the automatic growth of storage size of a flexible server when available space is
                      nearing zero and conditions allow for automatically growing storage size.
                    type: string
                  iops:
                    description: 'Iops: Maximum IOPS supported for storage. Required
                      when type of storage is PremiumV2_LRS.'
                    type: integer
                  storageSizeGB:
                    description: 'StorageSizeGB: Size of storage assigned to a flexible
                      server.'
                    type: integer
                  throughput:
                    description: 'Throughput: Maximum throughput supported for storage.
                      Required when type of storage is PremiumV2_LRS.'
                    type: integer
                  tier:
                    description: 'Tier: Storage tier of a flexible server.'
                    type: string
                  type:
                    description: |-
                      Type: Type of storage assigned to a flexible server. Allowed values are Premium_LRS or PremiumV2_LRS. If not specified,
                      it defaults to Premium_LRS.
                    type: string
                type: object
              systemData:
                description: 'SystemData: Azure Resource Manager metadata containing
                  createdBy and modifiedBy information.'
                properties:
                  createdAt:
                    description: 'CreatedAt: The timestamp of resource creation (UTC).'
                    type: string
                  createdBy:
                    description: 'CreatedBy: The identity that created the resource.'
                    type: string
                  createdByType:
                    description: 'CreatedByType: The type of identity that created
                      the resource.'
                    type: string
                  lastModifiedAt:
                    description: 'LastModifiedAt: The timestamp of resource last modification
                      (UTC)'
                    type: string
                  lastModifiedBy:
                    description: 'LastModifiedBy: The identity that last modified
                      the resource.'
                    type: string
                  lastModifiedByType:
                    description: 'LastModifiedByType: The type of identity that last
                      modified the resource.'
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
              version:
                description: 'Version: Major version of PostgreSQL database engine.'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20240801storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20240801.FlexibleServer
          Generator information:
          - Generated from: /postgresql/resource-manager/Microsoft.DBforPostgreSQL/stable/2024-08-01/FlexibleServers.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DBforPostgreSQL/flexibleServers/{serverName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20240801.FlexibleServer_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              administratorLogin:
                type: string
              administratorLoginPassword:
                description: |-
                  SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                  the resource it is on.
                properties:
                  key:
                    description: Key is the key in the Kubernetes secret being referenced
                    type: string
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - key
                - name
                type: object
              authConfig:
                description: |-
                  Storage version of v1api20240801.AuthConfig
                  Authentication configuration properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  activeDirectoryAuth:
                    type: string
                  passwordAuth:
                    type: string
                  tenantId:
                    type: string
                type: object
              availabilityZone:
                type: string
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              backup:
                description: |-
                  Storage version of v1api20240801.Backup
                  Backup properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  backupRetentionDays:
                    type: integer
                  geoRedundantBackup:
                    type: string
                type: object
              createMode:
                type: string
              dataEncryption:
                description: |-
                  Storage version of v1api20240801.DataEncryption
                  Data encryption properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  geoBackupEncryptionKeyStatus:
                    type: string
                  geoBackupKeyURI:
                    type: string
                  geoBackupKeyURIFromConfig:
                    description: |-
                      ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  geoBackupUserAssignedIdentityReference:
                    description: |-
                      GeoBackupUserAssignedIdentityReference: Identifier of the user assigned managed identity used to access the key in Azure
                      Key Vault for data encryption of the geographically redundant storage associated to a flexible server that is configured
                      to support geographically redundant backups.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  primaryEncryptionKeyStatus:
                    type: string
                  primaryKeyURI:
                    type: string
                  primaryKeyURIFromConfig:
                    description: |-
                      ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  primaryUserAssignedIdentityReference:
                    description: |-
                      PrimaryUserAssignedIdentityReference: Identifier of the user assigned managed identity used to access the key in Azure
                      Key Vault for data encryption of the primary storage associated to a flexible server.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  type:
                    type: string
                type: object
              highAvailability:
                description: |-
                  Storage version of v1api20240801.HighAvailability
                  High availability properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  mode:
                    type: string
                  standbyAvailabilityZone:
                    type: string
                type: object
              identity:
                description: |-
                  Storage version of v1api20240801.UserAssignedIdentity
                  Identities associated with a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  principalId:
                    type: string
                  type:
                    type: string
                  userAssignedIdentities:
                    items:
                      description: |-
                        Storage version of v1api20240801.UserAssignedIdentityDetails
                        Information about the user assigned identity for the resource
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        reference:
                          description: ResourceReference represents a resource reference,
                            either to a Kubernetes resource or directly to an Azure
                            resource via ARMID
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              location:
                type: string
              maintenanceWindow:
                description: |-
                  Storage version of v1api20240801.MaintenanceWindow
                  Maintenance window properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  customWindow:
                    type: string
                  dayOfWeek:
                    type: integer
                  startHour:
                    type: integer
                  startMinute:
                    type: integer
                type: object
              network:
                description: |-
                  Storage version of v1api20240801.Network
                  Network properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  delegatedSubnetResourceReference:
                    description: |-
                      DelegatedSubnetResourceReference: Resource identifier of the delegated subnet. Required during creation of a new server,
                      in case you want the server to be integrated into your own virtual network. For an update operation, you only have to
                      provide this property if you want to change the value assigned for the private DNS zone.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  privateDnsZoneArmResourceReference:
                    description: |-
                      PrivateDnsZoneArmResourceReference: Identifier of the private DNS zone. Required during creation of a new server, in
                      case you want the server to be integrated into your own virtual network. For an update operation, you only have to
                      provide this property if you want to change the value assigned for the private DNS zone.
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  publicNetworkAccess:
                    type: string
                type: object
              operatorSpec:
                description: |-
                  Storage version of v1api20240801.FlexibleServerOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  configMaps:
                    description: Storage version of v1api20240801.FlexibleServerOperatorConfigMaps
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      fullyQualifiedDomainName:
                        description: |-
                          ConfigMapDestination describes the location to store a single configmap value
                          Note: This is similar to: SecretDestination in secrets.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: Storage version of v1api20240801.FlexibleServerOperatorSecrets
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      fullyQualifiedDomainName:
                        description: |-
                          SecretDestination describes the location to store a single secret value.
                          Note: This is similar to: ConfigMapDestination in configmaps.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              pointInTimeUTC:
                type: string
              replica:
                description: |-
                  Storage version of v1api20240801.Replica
                  Replica properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  promoteMode:
                    type: string
                  promoteOption:
                    type: string
                  role:
                    type: string
                type: object
              replicationRole:
                type: string
              sku:
                description: |-
                  Storage version of v1api20240801.Sku
                  Compute information of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              sourceServerResourceReference:
                description: |-
                  SourceServerResourceReference: Identifier of the flexible server to be used as the source of the new flexible server.
                  Required when 'createMode' is 'PointInTimeRestore', 'GeoRestore', 'Replica', or 'ReviveDropped'. This property is
                  returned only when the target flexible server is a read replica.
                properties:
                  armId:
                    description: |-
                      ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                      The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                      ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  group:
                    description: Group is the Kubernetes group of the resource.
                    type: string
                  kind:
                    description: Kind is the Kubernetes kind of the resource.
                    type: string
                  name:
                    description: Name is the Kubernetes name of the resource.
                    type: string
                type: object
              storage:
                description: |-
                  Storage version of v1api20240801.Storage
                  Storage properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  autoGrow:
                    type: string
                  iops:
                    type: integer
                  storageSizeGB:
                    type: integer
                  throughput:
                    type: integer
                  tier:
                    type: string
                  type:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              version:
                type: string
            required:
            - owner
            type: object
          status:
            description: Storage version of v1api20240801.FlexibleServer_STATUS
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              administratorLogin:
                type: string
              authConfig:
                description: |-
                  Storage version of v1api20240801.AuthConfig_STATUS
                  Authentication configuration properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  activeDirectoryAuth:
                    type: string
                  passwordAuth:
                    type: string
                  tenantId:
                    type: string
                type: object
              availabilityZone:
                type: string
              backup:
                description: |-
                  Storage version of v1api20240801.Backup_STATUS
                  Backup properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  backupRetentionDays:
                    type: integer
                  earliestRestoreDate:
                    type: string
                  geoRedundantBackup:
                    type: string
                type: object
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createMode:
                type: string
              dataEncryption:
                description: |-
                  Storage version of v1api20240801.DataEncryption_STATUS
                  Data encryption properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  geoBackupEncryptionKeyStatus:
                    type: string
                  geoBackupKeyURI:
                    type: string
                  geoBackupUserAssignedIdentityId:
                    type: string
                  primaryEncryptionKeyStatus:
                    type: string
                  primaryKeyURI:
                    type: string
                  primaryUserAssignedIdentityId:
                    type: string
                  type:
                    type: string
                type: object
              fullyQualifiedDomainName:
                type: string
              highAvailability:
                description: |-
                  Storage version of v1api20240801.HighAvailability_STATUS
                  High availability properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  mode:
                    type: string
                  standbyAvailabilityZone:
                    type: string
                  state:
                    type: string
                type: object
              id:
                type: string
              identity:
                description: |-
                  Storage version of v1api20240801.UserAssignedIdentity_STATUS
                  Identities associated with a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  principalId:
                    type: string
                  tenantId:
                    type: string
                  type:
                    type: string
                  userAssignedIdentities:
                    additionalProperties:
                      description: |-
                        Storage version of v1api20240801.UserIdentity_STATUS
                        User assigned managed identity associated with a flexible server.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        clientId:
                          type: string
                        principalId:
                          type: string
                      type: object
                    type: object
                type: object
              location:
                type: string
              maintenanceWindow:
                description: |-
                  Storage version of v1api20240801.MaintenanceWindow_STATUS
                  Maintenance window properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  customWindow:
                    type: string
                  dayOfWeek:
                    type: integer
                  startHour:
                    type: integer
                  startMinute:
                    type: integer
                type: object
              minorVersion:
                type: string
              name:
                type: string
              network:
                description: |-
                  Storage version of v1api20240801.Network_STATUS
                  Network properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  delegatedSubnetResourceId:
                    type: string
                  privateDnsZoneArmResourceId:
                    type: string
                  publicNetworkAccess:
                    type: string
                type: object
              pointInTimeUTC:
                type: string
              privateEndpointConnections:
                items:
                  description: |-
                    Storage version of v1api20240801.PrivateEndpointConnection_STATUS
                    The private endpoint connection resource.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    id:
                      type: string
                  type: object
                type: array
              replica:
                description: |-
                  Storage version of v1api20240801.Replica_STATUS
                  Replica properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  capacity:
                    type: integer
                  promoteMode:
                    type: string
                  promoteOption:
                    type: string
                  replicationState:
                    type: string
                  role:
                    type: string
                type: object
              replicaCapacity:
                type: integer
              replicationRole:
                type: string
              sku:
                description: |-
                  Storage version of v1api20240801.Sku_STATUS
                  Compute information of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              sourceServerResourceId:
                type: string
              state:
                type: string
              storage:
                description: |-
                  Storage version of v1api20240801.Storage_STATUS
                  Storage properties of a flexible server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  autoGrow:
                    type: string
                  iops:
                    type: integer
                  storageSizeGB:
                    type: integer
                  throughput:
                    type: integer
                  tier:
                    type: string
                  type:
                    type: string
                type: object
              systemData:
                description: |-
                  Storage version of v1api20240801.SystemData_STATUS
                  Metadata pertaining to creation and last modification of the resource.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  createdAt:
                    type: string
                  createdBy:
                    type: string
                  createdByType:
                    type: string
                  lastModifiedAt:
                    type: string
                  lastModifiedBy:
                    type: string
                  lastModifiedByType:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              type:
                type: string
              version:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
