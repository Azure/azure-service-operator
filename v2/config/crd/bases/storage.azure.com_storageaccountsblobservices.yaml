---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: storageaccountsblobservices.storage.azure.com
spec:
  group: storage.azure.com
  names:
    kind: StorageAccountsBlobService
    listKind: StorageAccountsBlobServiceList
    plural: storageaccountsblobservices
    singular: storageaccountsblobservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20210401
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /storage/resource-manager/Microsoft.Storage/stable/2021-04-01/blob.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Storage/storageAccounts/{accountName}/blobServices/default
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              automaticSnapshotPolicyEnabled:
                description: 'AutomaticSnapshotPolicyEnabled: Deprecated in favor
                  of isVersioningEnabled property.'
                type: boolean
              changeFeed:
                description: 'ChangeFeed: The blob service properties for change feed
                  events.'
                properties:
                  enabled:
                    description: 'Enabled: Indicates whether change feed event logging
                      is enabled for the Blob service.'
                    type: boolean
                  retentionInDays:
                    description: |-
                      RetentionInDays: Indicates the duration of changeFeed retention in days. Minimum value is 1 day and maximum value is
                      146000 days (400 years). A null value indicates an infinite retention of the change feed.
                    maximum: 146000
                    minimum: 1
                    type: integer
                type: object
              containerDeleteRetentionPolicy:
                description: 'ContainerDeleteRetentionPolicy: The blob service properties
                  for container soft delete.'
                properties:
                  days:
                    description: |-
                      Days: Indicates the number of days that the deleted item should be retained. The minimum specified value can be 1 and
                      the maximum value can be 365.
                    maximum: 365
                    minimum: 1
                    type: integer
                  enabled:
                    description: 'Enabled: Indicates whether DeleteRetentionPolicy
                      is enabled.'
                    type: boolean
                type: object
              cors:
                description: |-
                  Cors: Specifies CORS rules for the Blob service. You can include up to five CorsRule elements in the request. If no
                  CorsRule elements are included in the request body, all CORS rules will be deleted, and CORS will be disabled for the
                  Blob service.
                properties:
                  corsRules:
                    description: 'CorsRules: The List of CORS rules. You can include
                      up to five CorsRule elements in the request.'
                    items:
                      description: Specifies a CORS rule for the Blob service.
                      properties:
                        allowedHeaders:
                          description: |-
                            AllowedHeaders: Required if CorsRule element is present. A list of headers allowed to be part of the cross-origin
                            request.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: |-
                            AllowedMethods: Required if CorsRule element is present. A list of HTTP methods that are allowed to be executed by the
                            origin.
                          items:
                            enum:
                            - DELETE
                            - GET
                            - HEAD
                            - MERGE
                            - OPTIONS
                            - POST
                            - PUT
                            type: string
                          type: array
                        allowedOrigins:
                          description: |-
                            AllowedOrigins: Required if CorsRule element is present. A list of origin domains that will be allowed via CORS, or "*"
                            to allow all domains
                          items:
                            type: string
                          type: array
                        exposedHeaders:
                          description: 'ExposedHeaders: Required if CorsRule element
                            is present. A list of response headers to expose to CORS
                            clients.'
                          items:
                            type: string
                          type: array
                        maxAgeInSeconds:
                          description: |-
                            MaxAgeInSeconds: Required if CorsRule element is present. The number of seconds that the client/browser should cache a
                            preflight response.
                          type: integer
                      required:
                      - allowedHeaders
                      - allowedMethods
                      - allowedOrigins
                      - exposedHeaders
                      - maxAgeInSeconds
                      type: object
                    type: array
                type: object
              defaultServiceVersion:
                description: |-
                  DefaultServiceVersion: DefaultServiceVersion indicates the default version to use for requests to the Blob service if an
                  incoming request’s version is not specified. Possible values include version 2008-10-27 and all more recent versions.
                type: string
              deleteRetentionPolicy:
                description: 'DeleteRetentionPolicy: The blob service properties for
                  blob soft delete.'
                properties:
                  days:
                    description: |-
                      Days: Indicates the number of days that the deleted item should be retained. The minimum specified value can be 1 and
                      the maximum value can be 365.
                    maximum: 365
                    minimum: 1
                    type: integer
                  enabled:
                    description: 'Enabled: Indicates whether DeleteRetentionPolicy
                      is enabled.'
                    type: boolean
                type: object
              isVersioningEnabled:
                description: 'IsVersioningEnabled: Versioning is enabled if set to
                  true.'
                type: boolean
              lastAccessTimeTrackingPolicy:
                description: 'LastAccessTimeTrackingPolicy: The blob service property
                  to configure last access time based tracking policy.'
                properties:
                  blobType:
                    description: |-
                      BlobType: An array of predefined supported blob types. Only blockBlob is the supported value. This field is currently
                      read only
                    items:
                      type: string
                    type: array
                  enable:
                    description: 'Enable: When set to true last access time based
                      tracking is enabled.'
                    type: boolean
                  name:
                    description: 'Name: Name of the policy. The valid value is AccessTimeTracking.
                      This field is currently read only'
                    enum:
                    - AccessTimeTracking
                    type: string
                  trackingGranularityInDays:
                    description: |-
                      TrackingGranularityInDays: The field specifies blob object tracking granularity in days, typically how often the blob
                      object should be tracked.This field is currently read only with value as 1
                    type: integer
                required:
                - enable
                type: object
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a storage.azure.com/StorageAccount resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              restorePolicy:
                description: 'RestorePolicy: The blob service properties for blob
                  restore policy.'
                properties:
                  days:
                    description: 'Days: how long this blob can be restored. It should
                      be great than zero and less than DeleteRetentionPolicy.days.'
                    maximum: 365
                    minimum: 1
                    type: integer
                  enabled:
                    description: 'Enabled: Blob restore is enabled if set to true.'
                    type: boolean
                required:
                - enabled
                type: object
            required:
            - owner
            type: object
          status:
            properties:
              automaticSnapshotPolicyEnabled:
                description: 'AutomaticSnapshotPolicyEnabled: Deprecated in favor
                  of isVersioningEnabled property.'
                type: boolean
              changeFeed:
                description: 'ChangeFeed: The blob service properties for change feed
                  events.'
                properties:
                  enabled:
                    description: 'Enabled: Indicates whether change feed event logging
                      is enabled for the Blob service.'
                    type: boolean
                  retentionInDays:
                    description: |-
                      RetentionInDays: Indicates the duration of changeFeed retention in days. Minimum value is 1 day and maximum value is
                      146000 days (400 years). A null value indicates an infinite retention of the change feed.
                    type: integer
                type: object
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              containerDeleteRetentionPolicy:
                description: 'ContainerDeleteRetentionPolicy: The blob service properties
                  for container soft delete.'
                properties:
                  days:
                    description: |-
                      Days: Indicates the number of days that the deleted item should be retained. The minimum specified value can be 1 and
                      the maximum value can be 365.
                    type: integer
                  enabled:
                    description: 'Enabled: Indicates whether DeleteRetentionPolicy
                      is enabled.'
                    type: boolean
                type: object
              cors:
                description: |-
                  Cors: Specifies CORS rules for the Blob service. You can include up to five CorsRule elements in the request. If no
                  CorsRule elements are included in the request body, all CORS rules will be deleted, and CORS will be disabled for the
                  Blob service.
                properties:
                  corsRules:
                    description: 'CorsRules: The List of CORS rules. You can include
                      up to five CorsRule elements in the request.'
                    items:
                      description: Specifies a CORS rule for the Blob service.
                      properties:
                        allowedHeaders:
                          description: |-
                            AllowedHeaders: Required if CorsRule element is present. A list of headers allowed to be part of the cross-origin
                            request.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: |-
                            AllowedMethods: Required if CorsRule element is present. A list of HTTP methods that are allowed to be executed by the
                            origin.
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          description: |-
                            AllowedOrigins: Required if CorsRule element is present. A list of origin domains that will be allowed via CORS, or "*"
                            to allow all domains
                          items:
                            type: string
                          type: array
                        exposedHeaders:
                          description: 'ExposedHeaders: Required if CorsRule element
                            is present. A list of response headers to expose to CORS
                            clients.'
                          items:
                            type: string
                          type: array
                        maxAgeInSeconds:
                          description: |-
                            MaxAgeInSeconds: Required if CorsRule element is present. The number of seconds that the client/browser should cache a
                            preflight response.
                          type: integer
                      type: object
                    type: array
                type: object
              defaultServiceVersion:
                description: |-
                  DefaultServiceVersion: DefaultServiceVersion indicates the default version to use for requests to the Blob service if an
                  incoming request’s version is not specified. Possible values include version 2008-10-27 and all more recent versions.
                type: string
              deleteRetentionPolicy:
                description: 'DeleteRetentionPolicy: The blob service properties for
                  blob soft delete.'
                properties:
                  days:
                    description: |-
                      Days: Indicates the number of days that the deleted item should be retained. The minimum specified value can be 1 and
                      the maximum value can be 365.
                    type: integer
                  enabled:
                    description: 'Enabled: Indicates whether DeleteRetentionPolicy
                      is enabled.'
                    type: boolean
                type: object
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. Ex -
                  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                type: string
              isVersioningEnabled:
                description: 'IsVersioningEnabled: Versioning is enabled if set to
                  true.'
                type: boolean
              lastAccessTimeTrackingPolicy:
                description: 'LastAccessTimeTrackingPolicy: The blob service property
                  to configure last access time based tracking policy.'
                properties:
                  blobType:
                    description: |-
                      BlobType: An array of predefined supported blob types. Only blockBlob is the supported value. This field is currently
                      read only
                    items:
                      type: string
                    type: array
                  enable:
                    description: 'Enable: When set to true last access time based
                      tracking is enabled.'
                    type: boolean
                  name:
                    description: 'Name: Name of the policy. The valid value is AccessTimeTracking.
                      This field is currently read only'
                    type: string
                  trackingGranularityInDays:
                    description: |-
                      TrackingGranularityInDays: The field specifies blob object tracking granularity in days, typically how often the blob
                      object should be tracked.This field is currently read only with value as 1
                    type: integer
                type: object
              name:
                description: 'Name: The name of the resource'
                type: string
              restorePolicy:
                description: 'RestorePolicy: The blob service properties for blob
                  restore policy.'
                properties:
                  days:
                    description: 'Days: how long this blob can be restored. It should
                      be great than zero and less than DeleteRetentionPolicy.days.'
                    type: integer
                  enabled:
                    description: 'Enabled: Blob restore is enabled if set to true.'
                    type: boolean
                  lastEnabledTime:
                    description: 'LastEnabledTime: Deprecated in favor of minRestoreTime
                      property.'
                    type: string
                  minRestoreTime:
                    description: 'MinRestoreTime: Returns the minimum date and time
                      that the restore can be started.'
                    type: string
                type: object
              sku:
                description: 'Sku: Sku name and tier.'
                properties:
                  name:
                    description: |-
                      Name: The SKU name. Required for account creation; optional for update. Note that in older versions, SKU name was called
                       accountType.
                    type: string
                  tier:
                    description: 'Tier: The SKU tier. This is based on the SKU name.'
                    type: string
                type: object
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20210401storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20210401.StorageAccountsBlobService
          Generator information:
          - Generated from: /storage/resource-manager/Microsoft.Storage/stable/2021-04-01/blob.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Storage/storageAccounts/{accountName}/blobServices/default
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20210401.StorageAccountsBlobService_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              automaticSnapshotPolicyEnabled:
                type: boolean
              changeFeed:
                description: |-
                  Storage version of v1api20210401.ChangeFeed
                  The blob service properties for change feed events.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  enabled:
                    type: boolean
                  retentionInDays:
                    type: integer
                type: object
              containerDeleteRetentionPolicy:
                description: |-
                  Storage version of v1api20210401.DeleteRetentionPolicy
                  The service properties for soft delete.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
              cors:
                description: |-
                  Storage version of v1api20210401.CorsRules
                  Sets the CORS rules. You can include up to five CorsRule elements in the request.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  corsRules:
                    items:
                      description: |-
                        Storage version of v1api20210401.CorsRule
                        Specifies a CORS rule for the Blob service.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        allowedHeaders:
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          items:
                            type: string
                          type: array
                        exposedHeaders:
                          items:
                            type: string
                          type: array
                        maxAgeInSeconds:
                          type: integer
                      type: object
                    type: array
                type: object
              defaultServiceVersion:
                type: string
              deleteRetentionPolicy:
                description: |-
                  Storage version of v1api20210401.DeleteRetentionPolicy
                  The service properties for soft delete.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
              isVersioningEnabled:
                type: boolean
              lastAccessTimeTrackingPolicy:
                description: |-
                  Storage version of v1api20210401.LastAccessTimeTrackingPolicy
                  The blob service properties for Last access time based tracking policy.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  blobType:
                    items:
                      type: string
                    type: array
                  enable:
                    type: boolean
                  name:
                    type: string
                  trackingGranularityInDays:
                    type: integer
                type: object
              operatorSpec:
                description: |-
                  Storage version of v1api20210401.StorageAccountsBlobServiceOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a storage.azure.com/StorageAccount resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              restorePolicy:
                description: |-
                  Storage version of v1api20210401.RestorePolicyProperties
                  The blob service properties for blob restore policy
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
            required:
            - owner
            type: object
          status:
            description: Storage version of v1api20210401.StorageAccountsBlobService_STATUS
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              automaticSnapshotPolicyEnabled:
                type: boolean
              changeFeed:
                description: |-
                  Storage version of v1api20210401.ChangeFeed_STATUS
                  The blob service properties for change feed events.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  enabled:
                    type: boolean
                  retentionInDays:
                    type: integer
                type: object
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              containerDeleteRetentionPolicy:
                description: |-
                  Storage version of v1api20210401.DeleteRetentionPolicy_STATUS
                  The service properties for soft delete.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
              cors:
                description: |-
                  Storage version of v1api20210401.CorsRules_STATUS
                  Sets the CORS rules. You can include up to five CorsRule elements in the request.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  corsRules:
                    items:
                      description: |-
                        Storage version of v1api20210401.CorsRule_STATUS
                        Specifies a CORS rule for the Blob service.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        allowedHeaders:
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          items:
                            type: string
                          type: array
                        exposedHeaders:
                          items:
                            type: string
                          type: array
                        maxAgeInSeconds:
                          type: integer
                      type: object
                    type: array
                type: object
              defaultServiceVersion:
                type: string
              deleteRetentionPolicy:
                description: |-
                  Storage version of v1api20210401.DeleteRetentionPolicy_STATUS
                  The service properties for soft delete.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
              id:
                type: string
              isVersioningEnabled:
                type: boolean
              lastAccessTimeTrackingPolicy:
                description: |-
                  Storage version of v1api20210401.LastAccessTimeTrackingPolicy_STATUS
                  The blob service properties for Last access time based tracking policy.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  blobType:
                    items:
                      type: string
                    type: array
                  enable:
                    type: boolean
                  name:
                    type: string
                  trackingGranularityInDays:
                    type: integer
                type: object
              name:
                type: string
              restorePolicy:
                description: |-
                  Storage version of v1api20210401.RestorePolicyProperties_STATUS
                  The blob service properties for blob restore policy
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  days:
                    type: integer
                  enabled:
                    type: boolean
                  lastEnabledTime:
                    type: string
                  minRestoreTime:
                    type: string
                type: object
              sku:
                description: |-
                  Storage version of v1api20210401.Sku_STATUS
                  The SKU of the storage account.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              type:
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20220901
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /storage/resource-manager/Microsoft.Storage/stable/2022-09-01/blob.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Storage/storageAccounts/{accountName}/blobServices/default
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              automaticSnapshotPolicyEnabled:
                description: 'AutomaticSnapshotPolicyEnabled: Deprecated in favor
                  of isVersioningEnabled property.'
                type: boolean
              changeFeed:
                description: 'ChangeFeed: The blob service properties for change feed
                  events.'
                properties:
                  enabled:
                    description: 'Enabled: Indicates whether change feed event logging
                      is enabled for the Blob service.'
                    type: boolean
                  retentionInDays:
                    description: |-
                      RetentionInDays: Indicates the duration of changeFeed retention in days. Minimum value is 1 day and maximum value is
                      146000 days (400 years). A null value indicates an infinite retention of the change feed.
                    maximum: 146000
                    minimum: 1
                    type: integer
                type: object
              containerDeleteRetentionPolicy:
                description: 'ContainerDeleteRetentionPolicy: The blob service properties
                  for container soft delete.'
                properties:
                  allowPermanentDelete:
                    description: |-
                      AllowPermanentDelete: This property when set to true allows deletion of the soft deleted blob versions and snapshots.
                      This property cannot be used blob restore policy. This property only applies to blob service and does not apply to
                      containers or file share.
                    type: boolean
                  days:
                    description: |-
                      Days: Indicates the number of days that the deleted item should be retained. The minimum specified value can be 1 and
                      the maximum value can be 365.
                    maximum: 365
                    minimum: 1
                    type: integer
                  enabled:
                    description: 'Enabled: Indicates whether DeleteRetentionPolicy
                      is enabled.'
                    type: boolean
                type: object
              cors:
                description: |-
                  Cors: Specifies CORS rules for the Blob service. You can include up to five CorsRule elements in the request. If no
                  CorsRule elements are included in the request body, all CORS rules will be deleted, and CORS will be disabled for the
                  Blob service.
                properties:
                  corsRules:
                    description: 'CorsRules: The List of CORS rules. You can include
                      up to five CorsRule elements in the request.'
                    items:
                      description: Specifies a CORS rule for the Blob service.
                      properties:
                        allowedHeaders:
                          description: |-
                            AllowedHeaders: Required if CorsRule element is present. A list of headers allowed to be part of the cross-origin
                            request.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: |-
                            AllowedMethods: Required if CorsRule element is present. A list of HTTP methods that are allowed to be executed by the
                            origin.
                          items:
                            enum:
                            - DELETE
                            - GET
                            - HEAD
                            - MERGE
                            - OPTIONS
                            - PATCH
                            - POST
                            - PUT
                            type: string
                          type: array
                        allowedOrigins:
                          description: |-
                            AllowedOrigins: Required if CorsRule element is present. A list of origin domains that will be allowed via CORS, or "*"
                            to allow all domains
                          items:
                            type: string
                          type: array
                        exposedHeaders:
                          description: 'ExposedHeaders: Required if CorsRule element
                            is present. A list of response headers to expose to CORS
                            clients.'
                          items:
                            type: string
                          type: array
                        maxAgeInSeconds:
                          description: |-
                            MaxAgeInSeconds: Required if CorsRule element is present. The number of seconds that the client/browser should cache a
                            preflight response.
                          type: integer
                      required:
                      - allowedHeaders
                      - allowedMethods
                      - allowedOrigins
                      - exposedHeaders
                      - maxAgeInSeconds
                      type: object
                    type: array
                type: object
              defaultServiceVersion:
                description: |-
                  DefaultServiceVersion: DefaultServiceVersion indicates the default version to use for requests to the Blob service if an
                  incoming request’s version is not specified. Possible values include version 2008-10-27 and all more recent versions.
                type: string
              deleteRetentionPolicy:
                description: 'DeleteRetentionPolicy: The blob service properties for
                  blob soft delete.'
                properties:
                  allowPermanentDelete:
                    description: |-
                      AllowPermanentDelete: This property when set to true allows deletion of the soft deleted blob versions and snapshots.
                      This property cannot be used blob restore policy. This property only applies to blob service and does not apply to
                      containers or file share.
                    type: boolean
                  days:
                    description: |-
                      Days: Indicates the number of days that the deleted item should be retained. The minimum specified value can be 1 and
                      the maximum value can be 365.
                    maximum: 365
                    minimum: 1
                    type: integer
                  enabled:
                    description: 'Enabled: Indicates whether DeleteRetentionPolicy
                      is enabled.'
                    type: boolean
                type: object
              isVersioningEnabled:
                description: 'IsVersioningEnabled: Versioning is enabled if set to
                  true.'
                type: boolean
              lastAccessTimeTrackingPolicy:
                description: 'LastAccessTimeTrackingPolicy: The blob service property
                  to configure last access time based tracking policy.'
                properties:
                  blobType:
                    description: |-
                      BlobType: An array of predefined supported blob types. Only blockBlob is the supported value. This field is currently
                      read only
                    items:
                      type: string
                    type: array
                  enable:
                    description: 'Enable: When set to true last access time based
                      tracking is enabled.'
                    type: boolean
                  name:
                    description: 'Name: Name of the policy. The valid value is AccessTimeTracking.
                      This field is currently read only'
                    enum:
                    - AccessTimeTracking
                    type: string
                  trackingGranularityInDays:
                    description: |-
                      TrackingGranularityInDays: The field specifies blob object tracking granularity in days, typically how often the blob
                      object should be tracked.This field is currently read only with value as 1
                    type: integer
                required:
                - enable
                type: object
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a storage.azure.com/StorageAccount resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              restorePolicy:
                description: 'RestorePolicy: The blob service properties for blob
                  restore policy.'
                properties:
                  days:
                    description: 'Days: how long this blob can be restored. It should
                      be great than zero and less than DeleteRetentionPolicy.days.'
                    maximum: 365
                    minimum: 1
                    type: integer
                  enabled:
                    description: 'Enabled: Blob restore is enabled if set to true.'
                    type: boolean
                required:
                - enabled
                type: object
            required:
            - owner
            type: object
          status:
            properties:
              automaticSnapshotPolicyEnabled:
                description: 'AutomaticSnapshotPolicyEnabled: Deprecated in favor
                  of isVersioningEnabled property.'
                type: boolean
              changeFeed:
                description: 'ChangeFeed: The blob service properties for change feed
                  events.'
                properties:
                  enabled:
                    description: 'Enabled: Indicates whether change feed event logging
                      is enabled for the Blob service.'
                    type: boolean
                  retentionInDays:
                    description: |-
                      RetentionInDays: Indicates the duration of changeFeed retention in days. Minimum value is 1 day and maximum value is
                      146000 days (400 years). A null value indicates an infinite retention of the change feed.
                    type: integer
                type: object
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              containerDeleteRetentionPolicy:
                description: 'ContainerDeleteRetentionPolicy: The blob service properties
                  for container soft delete.'
                properties:
                  allowPermanentDelete:
                    description: |-
                      AllowPermanentDelete: This property when set to true allows deletion of the soft deleted blob versions and snapshots.
                      This property cannot be used blob restore policy. This property only applies to blob service and does not apply to
                      containers or file share.
                    type: boolean
                  days:
                    description: |-
                      Days: Indicates the number of days that the deleted item should be retained. The minimum specified value can be 1 and
                      the maximum value can be 365.
                    type: integer
                  enabled:
                    description: 'Enabled: Indicates whether DeleteRetentionPolicy
                      is enabled.'
                    type: boolean
                type: object
              cors:
                description: |-
                  Cors: Specifies CORS rules for the Blob service. You can include up to five CorsRule elements in the request. If no
                  CorsRule elements are included in the request body, all CORS rules will be deleted, and CORS will be disabled for the
                  Blob service.
                properties:
                  corsRules:
                    description: 'CorsRules: The List of CORS rules. You can include
                      up to five CorsRule elements in the request.'
                    items:
                      description: Specifies a CORS rule for the Blob service.
                      properties:
                        allowedHeaders:
                          description: |-
                            AllowedHeaders: Required if CorsRule element is present. A list of headers allowed to be part of the cross-origin
                            request.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: |-
                            AllowedMethods: Required if CorsRule element is present. A list of HTTP methods that are allowed to be executed by the
                            origin.
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          description: |-
                            AllowedOrigins: Required if CorsRule element is present. A list of origin domains that will be allowed via CORS, or "*"
                            to allow all domains
                          items:
                            type: string
                          type: array
                        exposedHeaders:
                          description: 'ExposedHeaders: Required if CorsRule element
                            is present. A list of response headers to expose to CORS
                            clients.'
                          items:
                            type: string
                          type: array
                        maxAgeInSeconds:
                          description: |-
                            MaxAgeInSeconds: Required if CorsRule element is present. The number of seconds that the client/browser should cache a
                            preflight response.
                          type: integer
                      type: object
                    type: array
                type: object
              defaultServiceVersion:
                description: |-
                  DefaultServiceVersion: DefaultServiceVersion indicates the default version to use for requests to the Blob service if an
                  incoming request’s version is not specified. Possible values include version 2008-10-27 and all more recent versions.
                type: string
              deleteRetentionPolicy:
                description: 'DeleteRetentionPolicy: The blob service properties for
                  blob soft delete.'
                properties:
                  allowPermanentDelete:
                    description: |-
                      AllowPermanentDelete: This property when set to true allows deletion of the soft deleted blob versions and snapshots.
                      This property cannot be used blob restore policy. This property only applies to blob service and does not apply to
                      containers or file share.
                    type: boolean
                  days:
                    description: |-
                      Days: Indicates the number of days that the deleted item should be retained. The minimum specified value can be 1 and
                      the maximum value can be 365.
                    type: integer
                  enabled:
                    description: 'Enabled: Indicates whether DeleteRetentionPolicy
                      is enabled.'
                    type: boolean
                type: object
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. Ex -
                  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                type: string
              isVersioningEnabled:
                description: 'IsVersioningEnabled: Versioning is enabled if set to
                  true.'
                type: boolean
              lastAccessTimeTrackingPolicy:
                description: 'LastAccessTimeTrackingPolicy: The blob service property
                  to configure last access time based tracking policy.'
                properties:
                  blobType:
                    description: |-
                      BlobType: An array of predefined supported blob types. Only blockBlob is the supported value. This field is currently
                      read only
                    items:
                      type: string
                    type: array
                  enable:
                    description: 'Enable: When set to true last access time based
                      tracking is enabled.'
                    type: boolean
                  name:
                    description: 'Name: Name of the policy. The valid value is AccessTimeTracking.
                      This field is currently read only'
                    type: string
                  trackingGranularityInDays:
                    description: |-
                      TrackingGranularityInDays: The field specifies blob object tracking granularity in days, typically how often the blob
                      object should be tracked.This field is currently read only with value as 1
                    type: integer
                type: object
              name:
                description: 'Name: The name of the resource'
                type: string
              restorePolicy:
                description: 'RestorePolicy: The blob service properties for blob
                  restore policy.'
                properties:
                  days:
                    description: 'Days: how long this blob can be restored. It should
                      be great than zero and less than DeleteRetentionPolicy.days.'
                    type: integer
                  enabled:
                    description: 'Enabled: Blob restore is enabled if set to true.'
                    type: boolean
                  lastEnabledTime:
                    description: 'LastEnabledTime: Deprecated in favor of minRestoreTime
                      property.'
                    type: string
                  minRestoreTime:
                    description: 'MinRestoreTime: Returns the minimum date and time
                      that the restore can be started.'
                    type: string
                type: object
              sku:
                description: 'Sku: Sku name and tier.'
                properties:
                  name:
                    description: |-
                      Name: The SKU name. Required for account creation; optional for update. Note that in older versions, SKU name was called
                       accountType.
                    type: string
                  tier:
                    description: 'Tier: The SKU tier. This is based on the SKU name.'
                    type: string
                type: object
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20220901storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20220901.StorageAccountsBlobService
          Generator information:
          - Generated from: /storage/resource-manager/Microsoft.Storage/stable/2022-09-01/blob.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Storage/storageAccounts/{accountName}/blobServices/default
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20220901.StorageAccountsBlobService_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              automaticSnapshotPolicyEnabled:
                type: boolean
              changeFeed:
                description: |-
                  Storage version of v1api20220901.ChangeFeed
                  The blob service properties for change feed events.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  enabled:
                    type: boolean
                  retentionInDays:
                    type: integer
                type: object
              containerDeleteRetentionPolicy:
                description: |-
                  Storage version of v1api20220901.DeleteRetentionPolicy
                  The service properties for soft delete.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  allowPermanentDelete:
                    type: boolean
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
              cors:
                description: |-
                  Storage version of v1api20220901.CorsRules
                  Sets the CORS rules. You can include up to five CorsRule elements in the request.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  corsRules:
                    items:
                      description: |-
                        Storage version of v1api20220901.CorsRule
                        Specifies a CORS rule for the Blob service.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        allowedHeaders:
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          items:
                            type: string
                          type: array
                        exposedHeaders:
                          items:
                            type: string
                          type: array
                        maxAgeInSeconds:
                          type: integer
                      type: object
                    type: array
                type: object
              defaultServiceVersion:
                type: string
              deleteRetentionPolicy:
                description: |-
                  Storage version of v1api20220901.DeleteRetentionPolicy
                  The service properties for soft delete.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  allowPermanentDelete:
                    type: boolean
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
              isVersioningEnabled:
                type: boolean
              lastAccessTimeTrackingPolicy:
                description: |-
                  Storage version of v1api20220901.LastAccessTimeTrackingPolicy
                  The blob service properties for Last access time based tracking policy.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  blobType:
                    items:
                      type: string
                    type: array
                  enable:
                    type: boolean
                  name:
                    type: string
                  trackingGranularityInDays:
                    type: integer
                type: object
              operatorSpec:
                description: |-
                  Storage version of v1api20220901.StorageAccountsBlobServiceOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a storage.azure.com/StorageAccount resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              restorePolicy:
                description: |-
                  Storage version of v1api20220901.RestorePolicyProperties
                  The blob service properties for blob restore policy
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
            required:
            - owner
            type: object
          status:
            description: Storage version of v1api20220901.StorageAccountsBlobService_STATUS
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              automaticSnapshotPolicyEnabled:
                type: boolean
              changeFeed:
                description: |-
                  Storage version of v1api20220901.ChangeFeed_STATUS
                  The blob service properties for change feed events.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  enabled:
                    type: boolean
                  retentionInDays:
                    type: integer
                type: object
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              containerDeleteRetentionPolicy:
                description: |-
                  Storage version of v1api20220901.DeleteRetentionPolicy_STATUS
                  The service properties for soft delete.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  allowPermanentDelete:
                    type: boolean
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
              cors:
                description: |-
                  Storage version of v1api20220901.CorsRules_STATUS
                  Sets the CORS rules. You can include up to five CorsRule elements in the request.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  corsRules:
                    items:
                      description: |-
                        Storage version of v1api20220901.CorsRule_STATUS
                        Specifies a CORS rule for the Blob service.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        allowedHeaders:
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          items:
                            type: string
                          type: array
                        exposedHeaders:
                          items:
                            type: string
                          type: array
                        maxAgeInSeconds:
                          type: integer
                      type: object
                    type: array
                type: object
              defaultServiceVersion:
                type: string
              deleteRetentionPolicy:
                description: |-
                  Storage version of v1api20220901.DeleteRetentionPolicy_STATUS
                  The service properties for soft delete.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  allowPermanentDelete:
                    type: boolean
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
              id:
                type: string
              isVersioningEnabled:
                type: boolean
              lastAccessTimeTrackingPolicy:
                description: |-
                  Storage version of v1api20220901.LastAccessTimeTrackingPolicy_STATUS
                  The blob service properties for Last access time based tracking policy.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  blobType:
                    items:
                      type: string
                    type: array
                  enable:
                    type: boolean
                  name:
                    type: string
                  trackingGranularityInDays:
                    type: integer
                type: object
              name:
                type: string
              restorePolicy:
                description: |-
                  Storage version of v1api20220901.RestorePolicyProperties_STATUS
                  The blob service properties for blob restore policy
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  days:
                    type: integer
                  enabled:
                    type: boolean
                  lastEnabledTime:
                    type: string
                  minRestoreTime:
                    type: string
                type: object
              sku:
                description: |-
                  Storage version of v1api20220901.Sku_STATUS
                  The SKU of the storage account.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              type:
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20230101
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /storage/resource-manager/Microsoft.Storage/stable/2023-01-01/blob.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Storage/storageAccounts/{accountName}/blobServices/default
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              automaticSnapshotPolicyEnabled:
                description: 'AutomaticSnapshotPolicyEnabled: Deprecated in favor
                  of isVersioningEnabled property.'
                type: boolean
              changeFeed:
                description: 'ChangeFeed: The blob service properties for change feed
                  events.'
                properties:
                  enabled:
                    description: 'Enabled: Indicates whether change feed event logging
                      is enabled for the Blob service.'
                    type: boolean
                  retentionInDays:
                    description: |-
                      RetentionInDays: Indicates the duration of changeFeed retention in days. Minimum value is 1 day and maximum value is
                      146000 days (400 years). A null value indicates an infinite retention of the change feed.
                    maximum: 146000
                    minimum: 1
                    type: integer
                type: object
              containerDeleteRetentionPolicy:
                description: 'ContainerDeleteRetentionPolicy: The blob service properties
                  for container soft delete.'
                properties:
                  allowPermanentDelete:
                    description: |-
                      AllowPermanentDelete: This property when set to true allows deletion of the soft deleted blob versions and snapshots.
                      This property cannot be used blob restore policy. This property only applies to blob service and does not apply to
                      containers or file share.
                    type: boolean
                  days:
                    description: |-
                      Days: Indicates the number of days that the deleted item should be retained. The minimum specified value can be 1 and
                      the maximum value can be 365.
                    maximum: 365
                    minimum: 1
                    type: integer
                  enabled:
                    description: 'Enabled: Indicates whether DeleteRetentionPolicy
                      is enabled.'
                    type: boolean
                type: object
              cors:
                description: |-
                  Cors: Specifies CORS rules for the Blob service. You can include up to five CorsRule elements in the request. If no
                  CorsRule elements are included in the request body, all CORS rules will be deleted, and CORS will be disabled for the
                  Blob service.
                properties:
                  corsRules:
                    description: 'CorsRules: The List of CORS rules. You can include
                      up to five CorsRule elements in the request.'
                    items:
                      description: Specifies a CORS rule for the Blob service.
                      properties:
                        allowedHeaders:
                          description: |-
                            AllowedHeaders: Required if CorsRule element is present. A list of headers allowed to be part of the cross-origin
                            request.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: |-
                            AllowedMethods: Required if CorsRule element is present. A list of HTTP methods that are allowed to be executed by the
                            origin.
                          items:
                            enum:
                            - CONNECT
                            - DELETE
                            - GET
                            - HEAD
                            - MERGE
                            - OPTIONS
                            - PATCH
                            - POST
                            - PUT
                            - TRACE
                            type: string
                          type: array
                        allowedOrigins:
                          description: |-
                            AllowedOrigins: Required if CorsRule element is present. A list of origin domains that will be allowed via CORS, or "*"
                            to allow all domains
                          items:
                            type: string
                          type: array
                        exposedHeaders:
                          description: 'ExposedHeaders: Required if CorsRule element
                            is present. A list of response headers to expose to CORS
                            clients.'
                          items:
                            type: string
                          type: array
                        maxAgeInSeconds:
                          description: |-
                            MaxAgeInSeconds: Required if CorsRule element is present. The number of seconds that the client/browser should cache a
                            preflight response.
                          type: integer
                      required:
                      - allowedHeaders
                      - allowedMethods
                      - allowedOrigins
                      - exposedHeaders
                      - maxAgeInSeconds
                      type: object
                    type: array
                type: object
              defaultServiceVersion:
                description: |-
                  DefaultServiceVersion: DefaultServiceVersion indicates the default version to use for requests to the Blob service if an
                  incoming request’s version is not specified. Possible values include version 2008-10-27 and all more recent versions.
                type: string
              deleteRetentionPolicy:
                description: 'DeleteRetentionPolicy: The blob service properties for
                  blob soft delete.'
                properties:
                  allowPermanentDelete:
                    description: |-
                      AllowPermanentDelete: This property when set to true allows deletion of the soft deleted blob versions and snapshots.
                      This property cannot be used blob restore policy. This property only applies to blob service and does not apply to
                      containers or file share.
                    type: boolean
                  days:
                    description: |-
                      Days: Indicates the number of days that the deleted item should be retained. The minimum specified value can be 1 and
                      the maximum value can be 365.
                    maximum: 365
                    minimum: 1
                    type: integer
                  enabled:
                    description: 'Enabled: Indicates whether DeleteRetentionPolicy
                      is enabled.'
                    type: boolean
                type: object
              isVersioningEnabled:
                description: 'IsVersioningEnabled: Versioning is enabled if set to
                  true.'
                type: boolean
              lastAccessTimeTrackingPolicy:
                description: 'LastAccessTimeTrackingPolicy: The blob service property
                  to configure last access time based tracking policy.'
                properties:
                  blobType:
                    description: |-
                      BlobType: An array of predefined supported blob types. Only blockBlob is the supported value. This field is currently
                      read only
                    items:
                      type: string
                    type: array
                  enable:
                    description: 'Enable: When set to true last access time based
                      tracking is enabled.'
                    type: boolean
                  name:
                    description: 'Name: Name of the policy. The valid value is AccessTimeTracking.
                      This field is currently read only'
                    enum:
                    - AccessTimeTracking
                    type: string
                  trackingGranularityInDays:
                    description: |-
                      TrackingGranularityInDays: The field specifies blob object tracking granularity in days, typically how often the blob
                      object should be tracked.This field is currently read only with value as 1
                    type: integer
                required:
                - enable
                type: object
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a storage.azure.com/StorageAccount resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              restorePolicy:
                description: 'RestorePolicy: The blob service properties for blob
                  restore policy.'
                properties:
                  days:
                    description: 'Days: how long this blob can be restored. It should
                      be great than zero and less than DeleteRetentionPolicy.days.'
                    maximum: 365
                    minimum: 1
                    type: integer
                  enabled:
                    description: 'Enabled: Blob restore is enabled if set to true.'
                    type: boolean
                required:
                - enabled
                type: object
            required:
            - owner
            type: object
          status:
            properties:
              automaticSnapshotPolicyEnabled:
                description: 'AutomaticSnapshotPolicyEnabled: Deprecated in favor
                  of isVersioningEnabled property.'
                type: boolean
              changeFeed:
                description: 'ChangeFeed: The blob service properties for change feed
                  events.'
                properties:
                  enabled:
                    description: 'Enabled: Indicates whether change feed event logging
                      is enabled for the Blob service.'
                    type: boolean
                  retentionInDays:
                    description: |-
                      RetentionInDays: Indicates the duration of changeFeed retention in days. Minimum value is 1 day and maximum value is
                      146000 days (400 years). A null value indicates an infinite retention of the change feed.
                    type: integer
                type: object
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              containerDeleteRetentionPolicy:
                description: 'ContainerDeleteRetentionPolicy: The blob service properties
                  for container soft delete.'
                properties:
                  allowPermanentDelete:
                    description: |-
                      AllowPermanentDelete: This property when set to true allows deletion of the soft deleted blob versions and snapshots.
                      This property cannot be used blob restore policy. This property only applies to blob service and does not apply to
                      containers or file share.
                    type: boolean
                  days:
                    description: |-
                      Days: Indicates the number of days that the deleted item should be retained. The minimum specified value can be 1 and
                      the maximum value can be 365.
                    type: integer
                  enabled:
                    description: 'Enabled: Indicates whether DeleteRetentionPolicy
                      is enabled.'
                    type: boolean
                type: object
              cors:
                description: |-
                  Cors: Specifies CORS rules for the Blob service. You can include up to five CorsRule elements in the request. If no
                  CorsRule elements are included in the request body, all CORS rules will be deleted, and CORS will be disabled for the
                  Blob service.
                properties:
                  corsRules:
                    description: 'CorsRules: The List of CORS rules. You can include
                      up to five CorsRule elements in the request.'
                    items:
                      description: Specifies a CORS rule for the Blob service.
                      properties:
                        allowedHeaders:
                          description: |-
                            AllowedHeaders: Required if CorsRule element is present. A list of headers allowed to be part of the cross-origin
                            request.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: |-
                            AllowedMethods: Required if CorsRule element is present. A list of HTTP methods that are allowed to be executed by the
                            origin.
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          description: |-
                            AllowedOrigins: Required if CorsRule element is present. A list of origin domains that will be allowed via CORS, or "*"
                            to allow all domains
                          items:
                            type: string
                          type: array
                        exposedHeaders:
                          description: 'ExposedHeaders: Required if CorsRule element
                            is present. A list of response headers to expose to CORS
                            clients.'
                          items:
                            type: string
                          type: array
                        maxAgeInSeconds:
                          description: |-
                            MaxAgeInSeconds: Required if CorsRule element is present. The number of seconds that the client/browser should cache a
                            preflight response.
                          type: integer
                      type: object
                    type: array
                type: object
              defaultServiceVersion:
                description: |-
                  DefaultServiceVersion: DefaultServiceVersion indicates the default version to use for requests to the Blob service if an
                  incoming request’s version is not specified. Possible values include version 2008-10-27 and all more recent versions.
                type: string
              deleteRetentionPolicy:
                description: 'DeleteRetentionPolicy: The blob service properties for
                  blob soft delete.'
                properties:
                  allowPermanentDelete:
                    description: |-
                      AllowPermanentDelete: This property when set to true allows deletion of the soft deleted blob versions and snapshots.
                      This property cannot be used blob restore policy. This property only applies to blob service and does not apply to
                      containers or file share.
                    type: boolean
                  days:
                    description: |-
                      Days: Indicates the number of days that the deleted item should be retained. The minimum specified value can be 1 and
                      the maximum value can be 365.
                    type: integer
                  enabled:
                    description: 'Enabled: Indicates whether DeleteRetentionPolicy
                      is enabled.'
                    type: boolean
                type: object
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. Ex -
                  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                type: string
              isVersioningEnabled:
                description: 'IsVersioningEnabled: Versioning is enabled if set to
                  true.'
                type: boolean
              lastAccessTimeTrackingPolicy:
                description: 'LastAccessTimeTrackingPolicy: The blob service property
                  to configure last access time based tracking policy.'
                properties:
                  blobType:
                    description: |-
                      BlobType: An array of predefined supported blob types. Only blockBlob is the supported value. This field is currently
                      read only
                    items:
                      type: string
                    type: array
                  enable:
                    description: 'Enable: When set to true last access time based
                      tracking is enabled.'
                    type: boolean
                  name:
                    description: 'Name: Name of the policy. The valid value is AccessTimeTracking.
                      This field is currently read only'
                    type: string
                  trackingGranularityInDays:
                    description: |-
                      TrackingGranularityInDays: The field specifies blob object tracking granularity in days, typically how often the blob
                      object should be tracked.This field is currently read only with value as 1
                    type: integer
                type: object
              name:
                description: 'Name: The name of the resource'
                type: string
              restorePolicy:
                description: 'RestorePolicy: The blob service properties for blob
                  restore policy.'
                properties:
                  days:
                    description: 'Days: how long this blob can be restored. It should
                      be great than zero and less than DeleteRetentionPolicy.days.'
                    type: integer
                  enabled:
                    description: 'Enabled: Blob restore is enabled if set to true.'
                    type: boolean
                  lastEnabledTime:
                    description: 'LastEnabledTime: Deprecated in favor of minRestoreTime
                      property.'
                    type: string
                  minRestoreTime:
                    description: 'MinRestoreTime: Returns the minimum date and time
                      that the restore can be started.'
                    type: string
                type: object
              sku:
                description: 'Sku: Sku name and tier.'
                properties:
                  name:
                    description: |-
                      Name: The SKU name. Required for account creation; optional for update. Note that in older versions, SKU name was called
                       accountType.
                    type: string
                  tier:
                    description: 'Tier: The SKU tier. This is based on the SKU name.'
                    type: string
                type: object
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20230101storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20230101.StorageAccountsBlobService
          Generator information:
          - Generated from: /storage/resource-manager/Microsoft.Storage/stable/2023-01-01/blob.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Storage/storageAccounts/{accountName}/blobServices/default
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20230101.StorageAccountsBlobService_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              automaticSnapshotPolicyEnabled:
                type: boolean
              changeFeed:
                description: |-
                  Storage version of v1api20230101.ChangeFeed
                  The blob service properties for change feed events.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  enabled:
                    type: boolean
                  retentionInDays:
                    type: integer
                type: object
              containerDeleteRetentionPolicy:
                description: |-
                  Storage version of v1api20230101.DeleteRetentionPolicy
                  The service properties for soft delete.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  allowPermanentDelete:
                    type: boolean
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
              cors:
                description: |-
                  Storage version of v1api20230101.CorsRules
                  Sets the CORS rules. You can include up to five CorsRule elements in the request.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  corsRules:
                    items:
                      description: |-
                        Storage version of v1api20230101.CorsRule
                        Specifies a CORS rule for the Blob service.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        allowedHeaders:
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          items:
                            type: string
                          type: array
                        exposedHeaders:
                          items:
                            type: string
                          type: array
                        maxAgeInSeconds:
                          type: integer
                      type: object
                    type: array
                type: object
              defaultServiceVersion:
                type: string
              deleteRetentionPolicy:
                description: |-
                  Storage version of v1api20230101.DeleteRetentionPolicy
                  The service properties for soft delete.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  allowPermanentDelete:
                    type: boolean
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
              isVersioningEnabled:
                type: boolean
              lastAccessTimeTrackingPolicy:
                description: |-
                  Storage version of v1api20230101.LastAccessTimeTrackingPolicy
                  The blob service properties for Last access time based tracking policy.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  blobType:
                    items:
                      type: string
                    type: array
                  enable:
                    type: boolean
                  name:
                    type: string
                  trackingGranularityInDays:
                    type: integer
                type: object
              operatorSpec:
                description: |-
                  Storage version of v1api20230101.StorageAccountsBlobServiceOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a storage.azure.com/StorageAccount resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              restorePolicy:
                description: |-
                  Storage version of v1api20230101.RestorePolicyProperties
                  The blob service properties for blob restore policy
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
            required:
            - owner
            type: object
          status:
            description: Storage version of v1api20230101.StorageAccountsBlobService_STATUS
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              automaticSnapshotPolicyEnabled:
                type: boolean
              changeFeed:
                description: |-
                  Storage version of v1api20230101.ChangeFeed_STATUS
                  The blob service properties for change feed events.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  enabled:
                    type: boolean
                  retentionInDays:
                    type: integer
                type: object
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              containerDeleteRetentionPolicy:
                description: |-
                  Storage version of v1api20230101.DeleteRetentionPolicy_STATUS
                  The service properties for soft delete.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  allowPermanentDelete:
                    type: boolean
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
              cors:
                description: |-
                  Storage version of v1api20230101.CorsRules_STATUS
                  Sets the CORS rules. You can include up to five CorsRule elements in the request.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  corsRules:
                    items:
                      description: |-
                        Storage version of v1api20230101.CorsRule_STATUS
                        Specifies a CORS rule for the Blob service.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        allowedHeaders:
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          items:
                            type: string
                          type: array
                        exposedHeaders:
                          items:
                            type: string
                          type: array
                        maxAgeInSeconds:
                          type: integer
                      type: object
                    type: array
                type: object
              defaultServiceVersion:
                type: string
              deleteRetentionPolicy:
                description: |-
                  Storage version of v1api20230101.DeleteRetentionPolicy_STATUS
                  The service properties for soft delete.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  allowPermanentDelete:
                    type: boolean
                  days:
                    type: integer
                  enabled:
                    type: boolean
                type: object
              id:
                type: string
              isVersioningEnabled:
                type: boolean
              lastAccessTimeTrackingPolicy:
                description: |-
                  Storage version of v1api20230101.LastAccessTimeTrackingPolicy_STATUS
                  The blob service properties for Last access time based tracking policy.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  blobType:
                    items:
                      type: string
                    type: array
                  enable:
                    type: boolean
                  name:
                    type: string
                  trackingGranularityInDays:
                    type: integer
                type: object
              name:
                type: string
              restorePolicy:
                description: |-
                  Storage version of v1api20230101.RestorePolicyProperties_STATUS
                  The blob service properties for blob restore policy
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  days:
                    type: integer
                  enabled:
                    type: boolean
                  lastEnabledTime:
                    type: string
                  minRestoreTime:
                    type: string
                type: object
              sku:
                description: |-
                  Storage version of v1api20230101.Sku_STATUS
                  The SKU of the storage account.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  name:
                    type: string
                  tier:
                    type: string
                type: object
              type:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
