---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: workspaces.synapse.azure.com
spec:
  group: synapse.azure.com
  names:
    kind: Workspace
    listKind: WorkspaceList
    plural: workspaces
    singular: workspace
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20210601
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /synapse/resource-manager/Microsoft.Synapse/stable/2021-06-01/workspace.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Synapse/workspaces/{workspaceName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              azureADOnlyAuthentication:
                description: 'AzureADOnlyAuthentication: Enable or Disable AzureADOnlyAuthentication
                  on All Workspace subresource'
                type: boolean
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              cspWorkspaceAdminProperties:
                description: 'CspWorkspaceAdminProperties: Initial workspace AAD admin
                  properties for a CSP subscription'
                properties:
                  initialWorkspaceAdminObjectId:
                    description: 'InitialWorkspaceAdminObjectId: AAD object ID of
                      initial workspace admin'
                    type: string
                type: object
              defaultDataLakeStorage:
                description: 'DefaultDataLakeStorage: Workspace default data lake
                  storage account details'
                properties:
                  accountUrl:
                    description: 'AccountUrl: Account URL'
                    type: string
                  accountUrlFromConfig:
                    description: 'AccountUrlFromConfig: Account URL'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  createManagedPrivateEndpoint:
                    description: 'CreateManagedPrivateEndpoint: Create managed private
                      endpoint to this storage account or not'
                    type: boolean
                  filesystem:
                    description: 'Filesystem: Filesystem name'
                    type: string
                  resourceReference:
                    description: 'ResourceReference: ARM resource Id of this storage
                      account'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                type: object
              encryption:
                description: 'Encryption: The encryption details of the workspace'
                properties:
                  cmk:
                    description: 'Cmk: Customer Managed Key Details'
                    properties:
                      kekIdentity:
                        description: 'KekIdentity: Key encryption key'
                        properties:
                          useSystemAssignedIdentity:
                            description: 'UseSystemAssignedIdentity: Boolean specifying
                              whether to use system assigned identity or not'
                            x-kubernetes-preserve-unknown-fields: true
                          userAssignedIdentityReference:
                            description: 'UserAssignedIdentityReference: User assigned
                              identity resource Id'
                            properties:
                              armId:
                                description: |-
                                  ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                  The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                  ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                                pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                                type: string
                              group:
                                description: Group is the Kubernetes group of the
                                  resource.
                                type: string
                              kind:
                                description: Kind is the Kubernetes kind of the resource.
                                type: string
                              name:
                                description: Name is the Kubernetes name of the resource.
                                type: string
                            type: object
                        type: object
                      key:
                        description: 'Key: The key object of the workspace'
                        properties:
                          keyVaultUrl:
                            description: 'KeyVaultUrl: Workspace Key sub-resource
                              key vault url'
                            type: string
                          name:
                            description: 'Name: Workspace Key sub-resource name'
                            type: string
                        type: object
                    type: object
                type: object
              identity:
                description: 'Identity: Identity of the workspace'
                properties:
                  type:
                    description: 'Type: The type of managed identity for the workspace'
                    enum:
                    - None
                    - SystemAssigned
                    - SystemAssigned,UserAssigned
                    type: string
                  userAssignedIdentities:
                    description: 'UserAssignedIdentities: The user assigned managed
                      identities.'
                    items:
                      description: Information about the user assigned identity for
                        the resource
                      properties:
                        reference:
                          description: ResourceReference represents a resource reference,
                            either to a Kubernetes resource or directly to an Azure
                            resource via ARMID
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              managedResourceGroupName:
                description: |-
                  ManagedResourceGroupName: Workspace managed resource group. The resource group name uniquely identifies the resource
                  group within the user subscriptionId. The resource group name must be no longer than 90 characters long, and must be
                  alphanumeric characters (Char.IsLetterOrDigit()) and '-', '_', '(', ')' and'.'. Note that the name cannot end with '.'
                type: string
              managedVirtualNetwork:
                description: |-
                  ManagedVirtualNetwork: Setting this to 'default' will ensure that all compute for this workspace is in a virtual network
                  managed on behalf of the user.
                type: string
              managedVirtualNetworkSettings:
                description: 'ManagedVirtualNetworkSettings: Managed Virtual Network
                  Settings'
                properties:
                  allowedAadTenantIdsForLinking:
                    description: 'AllowedAadTenantIdsForLinking: Allowed Aad Tenant
                      Ids For Linking'
                    items:
                      type: string
                    type: array
                  linkedAccessCheckOnTargetResource:
                    description: 'LinkedAccessCheckOnTargetResource: Linked Access
                      Check On Target Resource'
                    type: boolean
                  preventDataExfiltration:
                    description: 'PreventDataExfiltration: Prevent Data Exfiltration'
                    type: boolean
                type: object
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              publicNetworkAccess:
                description: 'PublicNetworkAccess: Enable or Disable public network
                  access to workspace'
                enum:
                - Disabled
                - Enabled
                type: string
              purviewConfiguration:
                description: 'PurviewConfiguration: Purview Configuration'
                properties:
                  purviewResourceReference:
                    description: 'PurviewResourceReference: Purview Resource ID'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                type: object
              sqlAdministratorLogin:
                description: 'SqlAdministratorLogin: Login for workspace SQL active
                  directory administrator'
                type: string
              sqlAdministratorLoginPassword:
                description: 'SqlAdministratorLoginPassword: SQL administrator login
                  password'
                properties:
                  key:
                    description: Key is the key in the Kubernetes secret being referenced
                    type: string
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - key
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              trustedServiceBypassEnabled:
                description: 'TrustedServiceBypassEnabled: Is trustedServiceBypassEnabled
                  for the workspace'
                type: boolean
              virtualNetworkProfile:
                description: 'VirtualNetworkProfile: Virtual Network profile'
                properties:
                  computeSubnetId:
                    description: 'ComputeSubnetId: Subnet ID used for computes in
                      workspace'
                    type: string
                type: object
              workspaceRepositoryConfiguration:
                description: 'WorkspaceRepositoryConfiguration: Git integration settings'
                properties:
                  accountName:
                    description: 'AccountName: Account name'
                    type: string
                  collaborationBranch:
                    description: 'CollaborationBranch: Collaboration branch'
                    type: string
                  hostName:
                    description: 'HostName: GitHub Enterprise host name. For example:
                      `https://github.mydomain.com`'
                    type: string
                  lastCommitId:
                    description: 'LastCommitId: The last commit ID'
                    type: string
                  projectName:
                    description: 'ProjectName: VSTS project name'
                    type: string
                  repositoryName:
                    description: 'RepositoryName: Repository name'
                    type: string
                  rootFolder:
                    description: 'RootFolder: Root folder to use in the repository'
                    type: string
                  tenantId:
                    description: 'TenantId: The VSTS tenant ID'
                    pattern: ^[0-9a-fA-F]{8}(-[0-9a-fA-F]{4}){3}-[0-9a-fA-F]{12}$
                    type: string
                  type:
                    description: 'Type: Type of workspace repositoryID configuration.
                      Example WorkspaceVSTSConfiguration, WorkspaceGitHubConfiguration'
                    type: string
                type: object
            required:
            - location
            - owner
            type: object
          status:
            description: A workspace
            properties:
              adlaResourceId:
                description: 'AdlaResourceId: The ADLA resource ID.'
                type: string
              azureADOnlyAuthentication:
                description: 'AzureADOnlyAuthentication: Enable or Disable AzureADOnlyAuthentication
                  on All Workspace subresource'
                type: boolean
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectivityEndpoints:
                additionalProperties:
                  type: string
                description: 'ConnectivityEndpoints: Connectivity endpoints'
                type: object
              cspWorkspaceAdminProperties:
                description: 'CspWorkspaceAdminProperties: Initial workspace AAD admin
                  properties for a CSP subscription'
                properties:
                  initialWorkspaceAdminObjectId:
                    description: 'InitialWorkspaceAdminObjectId: AAD object ID of
                      initial workspace admin'
                    type: string
                type: object
              defaultDataLakeStorage:
                description: 'DefaultDataLakeStorage: Workspace default data lake
                  storage account details'
                properties:
                  accountUrl:
                    description: 'AccountUrl: Account URL'
                    type: string
                  createManagedPrivateEndpoint:
                    description: 'CreateManagedPrivateEndpoint: Create managed private
                      endpoint to this storage account or not'
                    type: boolean
                  filesystem:
                    description: 'Filesystem: Filesystem name'
                    type: string
                  resourceId:
                    description: 'ResourceId: ARM resource Id of this storage account'
                    type: string
                type: object
              encryption:
                description: 'Encryption: The encryption details of the workspace'
                properties:
                  cmk:
                    description: 'Cmk: Customer Managed Key Details'
                    properties:
                      kekIdentity:
                        description: 'KekIdentity: Key encryption key'
                        properties:
                          useSystemAssignedIdentity:
                            description: 'UseSystemAssignedIdentity: Boolean specifying
                              whether to use system assigned identity or not'
                            x-kubernetes-preserve-unknown-fields: true
                          userAssignedIdentity:
                            description: 'UserAssignedIdentity: User assigned identity
                              resource Id'
                            type: string
                        type: object
                      key:
                        description: 'Key: The key object of the workspace'
                        properties:
                          keyVaultUrl:
                            description: 'KeyVaultUrl: Workspace Key sub-resource
                              key vault url'
                            type: string
                          name:
                            description: 'Name: Workspace Key sub-resource name'
                            type: string
                        type: object
                      status:
                        description: 'Status: The customer managed key status on the
                          workspace'
                        type: string
                    type: object
                  doubleEncryptionEnabled:
                    description: 'DoubleEncryptionEnabled: Double Encryption enabled'
                    type: boolean
                type: object
              extraProperties:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                description: 'ExtraProperties: Workspace level configs and feature
                  flags'
                type: object
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. Ex -
                  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                type: string
              identity:
                description: 'Identity: Identity of the workspace'
                properties:
                  principalId:
                    description: 'PrincipalId: The principal ID of the workspace managed
                      identity'
                    type: string
                  tenantId:
                    description: 'TenantId: The tenant ID of the workspace managed
                      identity'
                    type: string
                  type:
                    description: 'Type: The type of managed identity for the workspace'
                    type: string
                  userAssignedIdentities:
                    additionalProperties:
                      description: User Assigned Managed Identity
                      properties:
                        clientId:
                          description: 'ClientId: The client ID.'
                          type: string
                        principalId:
                          description: 'PrincipalId: The principal ID.'
                          type: string
                      type: object
                    description: 'UserAssignedIdentities: The user assigned managed
                      identities.'
                    type: object
                type: object
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              managedResourceGroupName:
                description: |-
                  ManagedResourceGroupName: Workspace managed resource group. The resource group name uniquely identifies the resource
                  group within the user subscriptionId. The resource group name must be no longer than 90 characters long, and must be
                  alphanumeric characters (Char.IsLetterOrDigit()) and '-', '_', '(', ')' and'.'. Note that the name cannot end with '.'
                type: string
              managedVirtualNetwork:
                description: |-
                  ManagedVirtualNetwork: Setting this to 'default' will ensure that all compute for this workspace is in a virtual network
                  managed on behalf of the user.
                type: string
              managedVirtualNetworkSettings:
                description: 'ManagedVirtualNetworkSettings: Managed Virtual Network
                  Settings'
                properties:
                  allowedAadTenantIdsForLinking:
                    description: 'AllowedAadTenantIdsForLinking: Allowed Aad Tenant
                      Ids For Linking'
                    items:
                      type: string
                    type: array
                  linkedAccessCheckOnTargetResource:
                    description: 'LinkedAccessCheckOnTargetResource: Linked Access
                      Check On Target Resource'
                    type: boolean
                  preventDataExfiltration:
                    description: 'PreventDataExfiltration: Prevent Data Exfiltration'
                    type: boolean
                type: object
              name:
                description: 'Name: The name of the resource'
                type: string
              privateEndpointConnections:
                description: 'PrivateEndpointConnections: Private endpoint connections
                  to the workspace'
                items:
                  description: A private endpoint connection
                  properties:
                    id:
                      description: |-
                        Id: Fully qualified resource ID for the resource. Ex -
                        /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                      type: string
                  type: object
                type: array
              provisioningState:
                description: 'ProvisioningState: Resource provisioning state'
                type: string
              publicNetworkAccess:
                description: 'PublicNetworkAccess: Enable or Disable public network
                  access to workspace'
                type: string
              purviewConfiguration:
                description: 'PurviewConfiguration: Purview Configuration'
                properties:
                  purviewResourceId:
                    description: 'PurviewResourceId: Purview Resource ID'
                    type: string
                type: object
              settings:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                description: 'Settings: Workspace settings'
                type: object
              sqlAdministratorLogin:
                description: 'SqlAdministratorLogin: Login for workspace SQL active
                  directory administrator'
                type: string
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              trustedServiceBypassEnabled:
                description: 'TrustedServiceBypassEnabled: Is trustedServiceBypassEnabled
                  for the workspace'
                type: boolean
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
              virtualNetworkProfile:
                description: 'VirtualNetworkProfile: Virtual Network profile'
                properties:
                  computeSubnetId:
                    description: 'ComputeSubnetId: Subnet ID used for computes in
                      workspace'
                    type: string
                type: object
              workspaceRepositoryConfiguration:
                description: 'WorkspaceRepositoryConfiguration: Git integration settings'
                properties:
                  accountName:
                    description: 'AccountName: Account name'
                    type: string
                  collaborationBranch:
                    description: 'CollaborationBranch: Collaboration branch'
                    type: string
                  hostName:
                    description: 'HostName: GitHub Enterprise host name. For example:
                      `https://github.mydomain.com`'
                    type: string
                  lastCommitId:
                    description: 'LastCommitId: The last commit ID'
                    type: string
                  projectName:
                    description: 'ProjectName: VSTS project name'
                    type: string
                  repositoryName:
                    description: 'RepositoryName: Repository name'
                    type: string
                  rootFolder:
                    description: 'RootFolder: Root folder to use in the repository'
                    type: string
                  tenantId:
                    description: 'TenantId: The VSTS tenant ID'
                    type: string
                  type:
                    description: 'Type: Type of workspace repositoryID configuration.
                      Example WorkspaceVSTSConfiguration, WorkspaceGitHubConfiguration'
                    type: string
                type: object
              workspaceUID:
                description: 'WorkspaceUID: The workspace unique identifier'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20210601storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20210601.Workspace
          Generator information:
          - Generated from: /synapse/resource-manager/Microsoft.Synapse/stable/2021-06-01/workspace.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Synapse/workspaces/{workspaceName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20210601.Workspace_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              azureADOnlyAuthentication:
                type: boolean
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              cspWorkspaceAdminProperties:
                description: |-
                  Storage version of v1api20210601.CspWorkspaceAdminProperties
                  Initial workspace AAD admin properties for a CSP subscription
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  initialWorkspaceAdminObjectId:
                    type: string
                type: object
              defaultDataLakeStorage:
                description: |-
                  Storage version of v1api20210601.DataLakeStorageAccountDetails
                  Details of the data lake storage account associated with the workspace
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  accountUrl:
                    type: string
                  accountUrlFromConfig:
                    description: |-
                      ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  createManagedPrivateEndpoint:
                    type: boolean
                  filesystem:
                    type: string
                  resourceReference:
                    description: 'ResourceReference: ARM resource Id of this storage
                      account'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                type: object
              encryption:
                description: |-
                  Storage version of v1api20210601.EncryptionDetails
                  Details of the encryption associated with the workspace
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  cmk:
                    description: |-
                      Storage version of v1api20210601.CustomerManagedKeyDetails
                      Details of the customer managed key associated with the workspace
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      kekIdentity:
                        description: |-
                          Storage version of v1api20210601.KekIdentityProperties
                          Key encryption key properties
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          useSystemAssignedIdentity:
                            x-kubernetes-preserve-unknown-fields: true
                          userAssignedIdentityReference:
                            description: 'UserAssignedIdentityReference: User assigned
                              identity resource Id'
                            properties:
                              armId:
                                description: |-
                                  ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                  The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                  ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                                pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                                type: string
                              group:
                                description: Group is the Kubernetes group of the
                                  resource.
                                type: string
                              kind:
                                description: Kind is the Kubernetes kind of the resource.
                                type: string
                              name:
                                description: Name is the Kubernetes name of the resource.
                                type: string
                            type: object
                        type: object
                      key:
                        description: |-
                          Storage version of v1api20210601.WorkspaceKeyDetails
                          Details of the customer managed key associated with the workspace
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          keyVaultUrl:
                            type: string
                          name:
                            type: string
                        type: object
                    type: object
                type: object
              identity:
                description: |-
                  Storage version of v1api20210601.ManagedIdentity
                  The workspace managed identity
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  type:
                    type: string
                  userAssignedIdentities:
                    items:
                      description: |-
                        Storage version of v1api20210601.UserAssignedIdentityDetails
                        Information about the user assigned identity for the resource
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        reference:
                          description: ResourceReference represents a resource reference,
                            either to a Kubernetes resource or directly to an Azure
                            resource via ARMID
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              location:
                type: string
              managedResourceGroupName:
                type: string
              managedVirtualNetwork:
                type: string
              managedVirtualNetworkSettings:
                description: |-
                  Storage version of v1api20210601.ManagedVirtualNetworkSettings
                  Managed Virtual Network Settings
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  allowedAadTenantIdsForLinking:
                    items:
                      type: string
                    type: array
                  linkedAccessCheckOnTargetResource:
                    type: boolean
                  preventDataExfiltration:
                    type: boolean
                type: object
              operatorSpec:
                description: |-
                  Storage version of v1api20210601.WorkspaceOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              publicNetworkAccess:
                type: string
              purviewConfiguration:
                description: |-
                  Storage version of v1api20210601.PurviewConfiguration
                  Purview Configuration
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  purviewResourceReference:
                    description: 'PurviewResourceReference: Purview Resource ID'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                type: object
              sqlAdministratorLogin:
                type: string
              sqlAdministratorLoginPassword:
                description: |-
                  SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                  the resource it is on.
                properties:
                  key:
                    description: Key is the key in the Kubernetes secret being referenced
                    type: string
                  name:
                    description: |-
                      Name is the name of the Kubernetes secret being referenced.
                      The secret must be in the same namespace as the resource
                    type: string
                required:
                - key
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              trustedServiceBypassEnabled:
                type: boolean
              virtualNetworkProfile:
                description: |-
                  Storage version of v1api20210601.VirtualNetworkProfile
                  Virtual Network Profile
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  computeSubnetId:
                    type: string
                type: object
              workspaceRepositoryConfiguration:
                description: |-
                  Storage version of v1api20210601.WorkspaceRepositoryConfiguration
                  Git integration settings
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  accountName:
                    type: string
                  collaborationBranch:
                    type: string
                  hostName:
                    type: string
                  lastCommitId:
                    type: string
                  projectName:
                    type: string
                  repositoryName:
                    type: string
                  rootFolder:
                    type: string
                  tenantId:
                    type: string
                  type:
                    type: string
                type: object
            required:
            - owner
            type: object
          status:
            description: |-
              Storage version of v1api20210601.Workspace_STATUS
              A workspace
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              adlaResourceId:
                type: string
              azureADOnlyAuthentication:
                type: boolean
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectivityEndpoints:
                additionalProperties:
                  type: string
                type: object
              cspWorkspaceAdminProperties:
                description: |-
                  Storage version of v1api20210601.CspWorkspaceAdminProperties_STATUS
                  Initial workspace AAD admin properties for a CSP subscription
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  initialWorkspaceAdminObjectId:
                    type: string
                type: object
              defaultDataLakeStorage:
                description: |-
                  Storage version of v1api20210601.DataLakeStorageAccountDetails_STATUS
                  Details of the data lake storage account associated with the workspace
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  accountUrl:
                    type: string
                  createManagedPrivateEndpoint:
                    type: boolean
                  filesystem:
                    type: string
                  resourceId:
                    type: string
                type: object
              encryption:
                description: |-
                  Storage version of v1api20210601.EncryptionDetails_STATUS
                  Details of the encryption associated with the workspace
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  cmk:
                    description: |-
                      Storage version of v1api20210601.CustomerManagedKeyDetails_STATUS
                      Details of the customer managed key associated with the workspace
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      kekIdentity:
                        description: |-
                          Storage version of v1api20210601.KekIdentityProperties_STATUS
                          Key encryption key properties
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          useSystemAssignedIdentity:
                            x-kubernetes-preserve-unknown-fields: true
                          userAssignedIdentity:
                            type: string
                        type: object
                      key:
                        description: |-
                          Storage version of v1api20210601.WorkspaceKeyDetails_STATUS
                          Details of the customer managed key associated with the workspace
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          keyVaultUrl:
                            type: string
                          name:
                            type: string
                        type: object
                      status:
                        type: string
                    type: object
                  doubleEncryptionEnabled:
                    type: boolean
                type: object
              extraProperties:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                type: object
              id:
                type: string
              identity:
                description: |-
                  Storage version of v1api20210601.ManagedIdentity_STATUS
                  The workspace managed identity
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  principalId:
                    type: string
                  tenantId:
                    type: string
                  type:
                    type: string
                  userAssignedIdentities:
                    additionalProperties:
                      description: |-
                        Storage version of v1api20210601.UserAssignedManagedIdentity_STATUS
                        User Assigned Managed Identity
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        clientId:
                          type: string
                        principalId:
                          type: string
                      type: object
                    type: object
                type: object
              location:
                type: string
              managedResourceGroupName:
                type: string
              managedVirtualNetwork:
                type: string
              managedVirtualNetworkSettings:
                description: |-
                  Storage version of v1api20210601.ManagedVirtualNetworkSettings_STATUS
                  Managed Virtual Network Settings
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  allowedAadTenantIdsForLinking:
                    items:
                      type: string
                    type: array
                  linkedAccessCheckOnTargetResource:
                    type: boolean
                  preventDataExfiltration:
                    type: boolean
                type: object
              name:
                type: string
              privateEndpointConnections:
                items:
                  description: |-
                    Storage version of v1api20210601.PrivateEndpointConnection_STATUS
                    A private endpoint connection
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    id:
                      type: string
                  type: object
                type: array
              provisioningState:
                type: string
              publicNetworkAccess:
                type: string
              purviewConfiguration:
                description: |-
                  Storage version of v1api20210601.PurviewConfiguration_STATUS
                  Purview Configuration
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  purviewResourceId:
                    type: string
                type: object
              settings:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                type: object
              sqlAdministratorLogin:
                type: string
              tags:
                additionalProperties:
                  type: string
                type: object
              trustedServiceBypassEnabled:
                type: boolean
              type:
                type: string
              virtualNetworkProfile:
                description: |-
                  Storage version of v1api20210601.VirtualNetworkProfile_STATUS
                  Virtual Network Profile
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  computeSubnetId:
                    type: string
                type: object
              workspaceRepositoryConfiguration:
                description: |-
                  Storage version of v1api20210601.WorkspaceRepositoryConfiguration_STATUS
                  Git integration settings
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  accountName:
                    type: string
                  collaborationBranch:
                    type: string
                  hostName:
                    type: string
                  lastCommitId:
                    type: string
                  projectName:
                    type: string
                  repositoryName:
                    type: string
                  rootFolder:
                    type: string
                  tenantId:
                    type: string
                  type:
                    type: string
                type: object
              workspaceUID:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
