---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: servers.dbformariadb.azure.com
spec:
  group: dbformariadb.azure.com
  names:
    kind: Server
    listKind: ServerList
    plural: servers
    singular: server
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20180601
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /mariadb/resource-manager/Microsoft.DBforMariaDB/stable/2018-06-01/mariadb.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DBforMariaDB/servers/{serverName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              location:
                description: 'Location: The location the resource resides in.'
                type: string
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: 'Secrets: configures where to place Azure generated
                      secrets.'
                    properties:
                      fullyQualifiedDomainName:
                        description: |-
                          FullyQualifiedDomainName: indicates where the FullyQualifiedDomainName secret should be placed. If omitted, the secret
                          will not be retrieved from Azure.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              properties:
                description: 'Properties: Properties of the server.'
                properties:
                  default:
                    description: 'Default: Mutually exclusive with all other properties'
                    properties:
                      administratorLogin:
                        description: |-
                          AdministratorLogin: The administrator's login name of a server. Can only be specified when the server is being created
                          (and is required for creation).
                        type: string
                      administratorLoginPassword:
                        description: 'AdministratorLoginPassword: The password of
                          the administrator login.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      createMode:
                        description: 'CreateMode: The mode to create a new server.'
                        enum:
                        - Default
                        type: string
                      minimalTlsVersion:
                        description: 'MinimalTlsVersion: Enforce a minimal Tls version
                          for the server.'
                        enum:
                        - TLS1_0
                        - TLS1_1
                        - TLS1_2
                        - TLSEnforcementDisabled
                        type: string
                      publicNetworkAccess:
                        description: |-
                          PublicNetworkAccess: Whether or not public network access is allowed for this server. Value is optional but if passed
                          in, must be 'Enabled' or 'Disabled'
                        enum:
                        - Disabled
                        - Enabled
                        type: string
                      sslEnforcement:
                        description: 'SslEnforcement: Enable ssl enforcement or not
                          when connect to server.'
                        enum:
                        - Disabled
                        - Enabled
                        type: string
                      storageProfile:
                        description: 'StorageProfile: Storage profile of a server.'
                        properties:
                          backupRetentionDays:
                            description: 'BackupRetentionDays: Backup retention days
                              for the server.'
                            type: integer
                          geoRedundantBackup:
                            description: 'GeoRedundantBackup: Enable Geo-redundant
                              or not for server backup.'
                            enum:
                            - Disabled
                            - Enabled
                            type: string
                          storageAutogrow:
                            description: 'StorageAutogrow: Enable Storage Auto Grow.'
                            enum:
                            - Disabled
                            - Enabled
                            type: string
                          storageMB:
                            description: 'StorageMB: Max storage allowed for a server.'
                            type: integer
                        type: object
                      version:
                        description: 'Version: Server version.'
                        enum:
                        - "10.2"
                        - "10.3"
                        type: string
                    required:
                    - administratorLogin
                    - administratorLoginPassword
                    - createMode
                    type: object
                  geoRestore:
                    description: 'GeoRestore: Mutually exclusive with all other properties'
                    properties:
                      createMode:
                        description: 'CreateMode: The mode to create a new server.'
                        enum:
                        - GeoRestore
                        type: string
                      minimalTlsVersion:
                        description: 'MinimalTlsVersion: Enforce a minimal Tls version
                          for the server.'
                        enum:
                        - TLS1_0
                        - TLS1_1
                        - TLS1_2
                        - TLSEnforcementDisabled
                        type: string
                      publicNetworkAccess:
                        description: |-
                          PublicNetworkAccess: Whether or not public network access is allowed for this server. Value is optional but if passed
                          in, must be 'Enabled' or 'Disabled'
                        enum:
                        - Disabled
                        - Enabled
                        type: string
                      sourceServerId:
                        description: 'SourceServerId: The source server id to restore
                          from.'
                        type: string
                      sslEnforcement:
                        description: 'SslEnforcement: Enable ssl enforcement or not
                          when connect to server.'
                        enum:
                        - Disabled
                        - Enabled
                        type: string
                      storageProfile:
                        description: 'StorageProfile: Storage profile of a server.'
                        properties:
                          backupRetentionDays:
                            description: 'BackupRetentionDays: Backup retention days
                              for the server.'
                            type: integer
                          geoRedundantBackup:
                            description: 'GeoRedundantBackup: Enable Geo-redundant
                              or not for server backup.'
                            enum:
                            - Disabled
                            - Enabled
                            type: string
                          storageAutogrow:
                            description: 'StorageAutogrow: Enable Storage Auto Grow.'
                            enum:
                            - Disabled
                            - Enabled
                            type: string
                          storageMB:
                            description: 'StorageMB: Max storage allowed for a server.'
                            type: integer
                        type: object
                      version:
                        description: 'Version: Server version.'
                        enum:
                        - "10.2"
                        - "10.3"
                        type: string
                    required:
                    - createMode
                    - sourceServerId
                    type: object
                  pointInTimeRestore:
                    description: 'PointInTimeRestore: Mutually exclusive with all
                      other properties'
                    properties:
                      createMode:
                        description: 'CreateMode: The mode to create a new server.'
                        enum:
                        - PointInTimeRestore
                        type: string
                      minimalTlsVersion:
                        description: 'MinimalTlsVersion: Enforce a minimal Tls version
                          for the server.'
                        enum:
                        - TLS1_0
                        - TLS1_1
                        - TLS1_2
                        - TLSEnforcementDisabled
                        type: string
                      publicNetworkAccess:
                        description: |-
                          PublicNetworkAccess: Whether or not public network access is allowed for this server. Value is optional but if passed
                          in, must be 'Enabled' or 'Disabled'
                        enum:
                        - Disabled
                        - Enabled
                        type: string
                      restorePointInTime:
                        description: 'RestorePointInTime: Restore point creation time
                          (ISO8601 format), specifying the time to restore from.'
                        type: string
                      sourceServerId:
                        description: 'SourceServerId: The source server id to restore
                          from.'
                        type: string
                      sslEnforcement:
                        description: 'SslEnforcement: Enable ssl enforcement or not
                          when connect to server.'
                        enum:
                        - Disabled
                        - Enabled
                        type: string
                      storageProfile:
                        description: 'StorageProfile: Storage profile of a server.'
                        properties:
                          backupRetentionDays:
                            description: 'BackupRetentionDays: Backup retention days
                              for the server.'
                            type: integer
                          geoRedundantBackup:
                            description: 'GeoRedundantBackup: Enable Geo-redundant
                              or not for server backup.'
                            enum:
                            - Disabled
                            - Enabled
                            type: string
                          storageAutogrow:
                            description: 'StorageAutogrow: Enable Storage Auto Grow.'
                            enum:
                            - Disabled
                            - Enabled
                            type: string
                          storageMB:
                            description: 'StorageMB: Max storage allowed for a server.'
                            type: integer
                        type: object
                      version:
                        description: 'Version: Server version.'
                        enum:
                        - "10.2"
                        - "10.3"
                        type: string
                    required:
                    - createMode
                    - restorePointInTime
                    - sourceServerId
                    type: object
                  replica:
                    description: 'Replica: Mutually exclusive with all other properties'
                    properties:
                      createMode:
                        description: 'CreateMode: The mode to create a new server.'
                        enum:
                        - Replica
                        type: string
                      minimalTlsVersion:
                        description: 'MinimalTlsVersion: Enforce a minimal Tls version
                          for the server.'
                        enum:
                        - TLS1_0
                        - TLS1_1
                        - TLS1_2
                        - TLSEnforcementDisabled
                        type: string
                      publicNetworkAccess:
                        description: |-
                          PublicNetworkAccess: Whether or not public network access is allowed for this server. Value is optional but if passed
                          in, must be 'Enabled' or 'Disabled'
                        enum:
                        - Disabled
                        - Enabled
                        type: string
                      sourceServerId:
                        description: 'SourceServerId: The master server id to create
                          replica from.'
                        type: string
                      sslEnforcement:
                        description: 'SslEnforcement: Enable ssl enforcement or not
                          when connect to server.'
                        enum:
                        - Disabled
                        - Enabled
                        type: string
                      storageProfile:
                        description: 'StorageProfile: Storage profile of a server.'
                        properties:
                          backupRetentionDays:
                            description: 'BackupRetentionDays: Backup retention days
                              for the server.'
                            type: integer
                          geoRedundantBackup:
                            description: 'GeoRedundantBackup: Enable Geo-redundant
                              or not for server backup.'
                            enum:
                            - Disabled
                            - Enabled
                            type: string
                          storageAutogrow:
                            description: 'StorageAutogrow: Enable Storage Auto Grow.'
                            enum:
                            - Disabled
                            - Enabled
                            type: string
                          storageMB:
                            description: 'StorageMB: Max storage allowed for a server.'
                            type: integer
                        type: object
                      version:
                        description: 'Version: Server version.'
                        enum:
                        - "10.2"
                        - "10.3"
                        type: string
                    required:
                    - createMode
                    - sourceServerId
                    type: object
                type: object
              sku:
                description: 'Sku: The SKU (pricing tier) of the server.'
                properties:
                  capacity:
                    description: 'Capacity: The scale up/out capacity, representing
                      server''s compute units.'
                    minimum: 0
                    type: integer
                  family:
                    description: 'Family: The family of hardware.'
                    type: string
                  name:
                    description: 'Name: The name of the sku, typically, tier + family
                      + cores, e.g. B_Gen4_1, GP_Gen5_8.'
                    type: string
                  size:
                    description: 'Size: The size code, to be interpreted by resource
                      as appropriate.'
                    type: string
                  tier:
                    description: 'Tier: The tier of the particular SKU, e.g. Basic.'
                    enum:
                    - Basic
                    - GeneralPurpose
                    - MemoryOptimized
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Application-specific metadata in the form of key-value
                  pairs.'
                type: object
            required:
            - location
            - owner
            - properties
            type: object
          status:
            description: Represents a server.
            properties:
              administratorLogin:
                description: |-
                  AdministratorLogin: The administrator's login name of a server. Can only be specified when the server is being created
                  (and is required for creation).
                type: string
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              earliestRestoreDate:
                description: 'EarliestRestoreDate: Earliest restore point creation
                  time (ISO8601 format)'
                type: string
              fullyQualifiedDomainName:
                description: 'FullyQualifiedDomainName: The fully qualified domain
                  name of a server.'
                type: string
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. Ex -
                  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                type: string
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              masterServerId:
                description: 'MasterServerId: The master server id of a replica server.'
                type: string
              minimalTlsVersion:
                description: 'MinimalTlsVersion: Enforce a minimal Tls version for
                  the server.'
                type: string
              name:
                description: 'Name: The name of the resource'
                type: string
              privateEndpointConnections:
                description: 'PrivateEndpointConnections: List of private endpoint
                  connections on a server'
                items:
                  description: A private endpoint connection under a server
                  properties:
                    id:
                      description: 'Id: Resource Id of the private endpoint connection.'
                      type: string
                    properties:
                      description: 'Properties: Private endpoint connection properties'
                      properties:
                        privateEndpoint:
                          description: 'PrivateEndpoint: Private endpoint which the
                            connection belongs to.'
                          properties:
                            id:
                              description: 'Id: Resource id of the private endpoint.'
                              type: string
                          type: object
                        privateLinkServiceConnectionState:
                          description: 'PrivateLinkServiceConnectionState: Connection
                            state of the private endpoint connection.'
                          properties:
                            actionsRequired:
                              description: 'ActionsRequired: The actions required
                                for private link service connection.'
                              type: string
                            description:
                              description: 'Description: The private link service
                                connection description.'
                              type: string
                            status:
                              description: 'Status: The private link service connection
                                status.'
                              type: string
                          type: object
                        provisioningState:
                          description: 'ProvisioningState: State of the private endpoint
                            connection.'
                          type: string
                      type: object
                  type: object
                type: array
              publicNetworkAccess:
                description: |-
                  PublicNetworkAccess: Whether or not public network access is allowed for this server. Value is optional but if passed
                  in, must be 'Enabled' or 'Disabled'
                type: string
              replicaCapacity:
                description: 'ReplicaCapacity: The maximum number of replicas that
                  a master server can have.'
                type: integer
              replicationRole:
                description: 'ReplicationRole: The replication role of the server.'
                type: string
              sku:
                description: 'Sku: The SKU (pricing tier) of the server.'
                properties:
                  capacity:
                    description: 'Capacity: The scale up/out capacity, representing
                      server''s compute units.'
                    type: integer
                  family:
                    description: 'Family: The family of hardware.'
                    type: string
                  name:
                    description: 'Name: The name of the sku, typically, tier + family
                      + cores, e.g. B_Gen4_1, GP_Gen5_8.'
                    type: string
                  size:
                    description: 'Size: The size code, to be interpreted by resource
                      as appropriate.'
                    type: string
                  tier:
                    description: 'Tier: The tier of the particular SKU, e.g. Basic.'
                    type: string
                type: object
              sslEnforcement:
                description: 'SslEnforcement: Enable ssl enforcement or not when connect
                  to server.'
                type: string
              storageProfile:
                description: 'StorageProfile: Storage profile of a server.'
                properties:
                  backupRetentionDays:
                    description: 'BackupRetentionDays: Backup retention days for the
                      server.'
                    type: integer
                  geoRedundantBackup:
                    description: 'GeoRedundantBackup: Enable Geo-redundant or not
                      for server backup.'
                    type: string
                  storageAutogrow:
                    description: 'StorageAutogrow: Enable Storage Auto Grow.'
                    type: string
                  storageMB:
                    description: 'StorageMB: Max storage allowed for a server.'
                    type: integer
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
              userVisibleState:
                description: 'UserVisibleState: A state of a server that is visible
                  to user.'
                type: string
              version:
                description: 'Version: Server version.'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20180601storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20180601.Server
          Generator information:
          - Generated from: /mariadb/resource-manager/Microsoft.DBforMariaDB/stable/2018-06-01/mariadb.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DBforMariaDB/servers/{serverName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20180601.Server_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              location:
                type: string
              operatorSpec:
                description: |-
                  Storage version of v1api20180601.ServerOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: Storage version of v1api20180601.ServerOperatorSecrets
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      fullyQualifiedDomainName:
                        description: |-
                          SecretDestination describes the location to store a single secret value.
                          Note: This is similar to: ConfigMapDestination in configmaps.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              properties:
                description: Storage version of v1api20180601.ServerPropertiesForCreate
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  default:
                    description: Storage version of v1api20180601.ServerPropertiesForDefaultCreate
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      administratorLogin:
                        type: string
                      administratorLoginPassword:
                        description: |-
                          SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                          the resource it is on.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret being referenced.
                              The secret must be in the same namespace as the resource
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      createMode:
                        type: string
                      minimalTlsVersion:
                        type: string
                      publicNetworkAccess:
                        type: string
                      sslEnforcement:
                        type: string
                      storageProfile:
                        description: |-
                          Storage version of v1api20180601.StorageProfile
                          Storage Profile properties of a server
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          backupRetentionDays:
                            type: integer
                          geoRedundantBackup:
                            type: string
                          storageAutogrow:
                            type: string
                          storageMB:
                            type: integer
                        type: object
                      version:
                        type: string
                    type: object
                  geoRestore:
                    description: Storage version of v1api20180601.ServerPropertiesForGeoRestore
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      createMode:
                        type: string
                      minimalTlsVersion:
                        type: string
                      publicNetworkAccess:
                        type: string
                      sourceServerId:
                        type: string
                      sslEnforcement:
                        type: string
                      storageProfile:
                        description: |-
                          Storage version of v1api20180601.StorageProfile
                          Storage Profile properties of a server
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          backupRetentionDays:
                            type: integer
                          geoRedundantBackup:
                            type: string
                          storageAutogrow:
                            type: string
                          storageMB:
                            type: integer
                        type: object
                      version:
                        type: string
                    type: object
                  pointInTimeRestore:
                    description: Storage version of v1api20180601.ServerPropertiesForRestore
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      createMode:
                        type: string
                      minimalTlsVersion:
                        type: string
                      publicNetworkAccess:
                        type: string
                      restorePointInTime:
                        type: string
                      sourceServerId:
                        type: string
                      sslEnforcement:
                        type: string
                      storageProfile:
                        description: |-
                          Storage version of v1api20180601.StorageProfile
                          Storage Profile properties of a server
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          backupRetentionDays:
                            type: integer
                          geoRedundantBackup:
                            type: string
                          storageAutogrow:
                            type: string
                          storageMB:
                            type: integer
                        type: object
                      version:
                        type: string
                    type: object
                  replica:
                    description: Storage version of v1api20180601.ServerPropertiesForReplica
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      createMode:
                        type: string
                      minimalTlsVersion:
                        type: string
                      publicNetworkAccess:
                        type: string
                      sourceServerId:
                        type: string
                      sslEnforcement:
                        type: string
                      storageProfile:
                        description: |-
                          Storage version of v1api20180601.StorageProfile
                          Storage Profile properties of a server
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          backupRetentionDays:
                            type: integer
                          geoRedundantBackup:
                            type: string
                          storageAutogrow:
                            type: string
                          storageMB:
                            type: integer
                        type: object
                      version:
                        type: string
                    type: object
                type: object
              sku:
                description: |-
                  Storage version of v1api20180601.Sku
                  Billing information related properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  capacity:
                    type: integer
                  family:
                    type: string
                  name:
                    type: string
                  size:
                    type: string
                  tier:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - owner
            type: object
          status:
            description: |-
              Storage version of v1api20180601.Server_STATUS
              Represents a server.
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              administratorLogin:
                type: string
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              earliestRestoreDate:
                type: string
              fullyQualifiedDomainName:
                type: string
              id:
                type: string
              location:
                type: string
              masterServerId:
                type: string
              minimalTlsVersion:
                type: string
              name:
                type: string
              privateEndpointConnections:
                items:
                  description: |-
                    Storage version of v1api20180601.ServerPrivateEndpointConnection_STATUS
                    A private endpoint connection under a server
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    id:
                      type: string
                    properties:
                      description: |-
                        Storage version of v1api20180601.ServerPrivateEndpointConnectionProperties_STATUS
                        Properties of a private endpoint connection.
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        privateEndpoint:
                          description: Storage version of v1api20180601.PrivateEndpointProperty_STATUS
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            id:
                              type: string
                          type: object
                        privateLinkServiceConnectionState:
                          description: Storage version of v1api20180601.ServerPrivateLinkServiceConnectionStateProperty_STATUS
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            actionsRequired:
                              type: string
                            description:
                              type: string
                            status:
                              type: string
                          type: object
                        provisioningState:
                          type: string
                      type: object
                  type: object
                type: array
              publicNetworkAccess:
                type: string
              replicaCapacity:
                type: integer
              replicationRole:
                type: string
              sku:
                description: |-
                  Storage version of v1api20180601.Sku_STATUS
                  Billing information related properties of a server.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  capacity:
                    type: integer
                  family:
                    type: string
                  name:
                    type: string
                  size:
                    type: string
                  tier:
                    type: string
                type: object
              sslEnforcement:
                type: string
              storageProfile:
                description: |-
                  Storage version of v1api20180601.StorageProfile_STATUS
                  Storage Profile properties of a server
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  backupRetentionDays:
                    type: integer
                  geoRedundantBackup:
                    type: string
                  storageAutogrow:
                    type: string
                  storageMB:
                    type: integer
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              type:
                type: string
              userVisibleState:
                type: string
              version:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
