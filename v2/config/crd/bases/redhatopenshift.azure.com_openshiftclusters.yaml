---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: openshiftclusters.redhatopenshift.azure.com
spec:
  group: redhatopenshift.azure.com
  names:
    kind: OpenShiftCluster
    listKind: OpenShiftClusterList
    plural: openshiftclusters
    singular: openshiftcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20231122
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /redhatopenshift/resource-manager/Microsoft.RedHatOpenShift/openshiftclusters/stable/2023-11-22/redhatopenshift.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.RedHatOpenShift/openShiftClusters/{resourceName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              apiserverProfile:
                description: 'ApiserverProfile: The cluster API server profile.'
                properties:
                  visibility:
                    description: 'Visibility: API server visibility.'
                    enum:
                    - Private
                    - Public
                    type: string
                type: object
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              clusterProfile:
                description: 'ClusterProfile: The cluster profile.'
                properties:
                  domain:
                    description: 'Domain: The domain for the cluster.'
                    type: string
                  fipsValidatedModules:
                    description: 'FipsValidatedModules: If FIPS validated crypto modules
                      are used'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                  pullSecret:
                    description: 'PullSecret: The pull secret for the cluster.'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  resourceGroupId:
                    description: 'ResourceGroupId: The ID of the cluster resource
                      group.'
                    type: string
                  version:
                    description: 'Version: The version of the cluster.'
                    type: string
                type: object
              ingressProfiles:
                description: 'IngressProfiles: The cluster ingress profiles.'
                items:
                  description: IngressProfile represents an ingress profile.
                  properties:
                    name:
                      description: 'Name: The ingress profile name.'
                      type: string
                    visibility:
                      description: 'Visibility: Ingress visibility.'
                      enum:
                      - Private
                      - Public
                      type: string
                  type: object
                type: array
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              masterProfile:
                description: 'MasterProfile: The cluster master profile.'
                properties:
                  diskEncryptionSetReference:
                    description: 'DiskEncryptionSetReference: The resource ID of an
                      associated DiskEncryptionSet, if applicable.'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  encryptionAtHost:
                    description: 'EncryptionAtHost: Whether master virtual machines
                      are encrypted at host.'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                  subnetReference:
                    description: 'SubnetReference: The Azure resource ID of the master
                      subnet.'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  vmSize:
                    description: 'VmSize: The size of the master VMs.'
                    type: string
                type: object
              networkProfile:
                description: 'NetworkProfile: The cluster network profile.'
                properties:
                  loadBalancerProfile:
                    description: 'LoadBalancerProfile: The cluster load balancer profile.'
                    properties:
                      managedOutboundIps:
                        description: 'ManagedOutboundIps: The desired managed outbound
                          IPs for the cluster public load balancer.'
                        properties:
                          count:
                            description: |-
                              Count: Count represents the desired number of IPv4 outbound IPs created and managed by Azure for the cluster public load
                              balancer.  Allowed values are in the range of 1 - 20.  The default value is 1.
                            type: integer
                        type: object
                    type: object
                  outboundType:
                    description: 'OutboundType: The OutboundType used for egress traffic.'
                    enum:
                    - Loadbalancer
                    - UserDefinedRouting
                    type: string
                  podCidr:
                    description: 'PodCidr: The CIDR used for OpenShift/Kubernetes
                      Pods.'
                    type: string
                  preconfiguredNSG:
                    description: 'PreconfiguredNSG: Specifies whether subnets are
                      pre-attached with an NSG'
                    enum:
                    - Disabled
                    - Enabled
                    type: string
                  serviceCidr:
                    description: 'ServiceCidr: The CIDR used for OpenShift/Kubernetes
                      Services.'
                    type: string
                type: object
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: 'Secrets: configures where to place Azure generated
                      secrets.'
                    properties:
                      adminCredentials:
                        description: |-
                          AdminCredentials: indicates where the AdminCredentials secret should be placed. If omitted, the secret will not be
                          retrieved from Azure.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      password:
                        description: 'Password: indicates where the Password secret
                          should be placed. If omitted, the secret will not be retrieved
                          from Azure.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      username:
                        description: 'Username: indicates where the Username secret
                          should be placed. If omitted, the secret will not be retrieved
                          from Azure.'
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              servicePrincipalProfile:
                description: 'ServicePrincipalProfile: The cluster service principal
                  profile.'
                properties:
                  clientId:
                    description: 'ClientId: The client ID used for the cluster.'
                    type: string
                  clientIdFromConfig:
                    description: 'ClientIdFromConfig: The client ID used for the cluster.'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  clientSecret:
                    description: 'ClientSecret: The client secret used for the cluster.'
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              workerProfiles:
                description: 'WorkerProfiles: The cluster worker profiles.'
                items:
                  description: WorkerProfile represents a worker profile.
                  properties:
                    count:
                      description: 'Count: The number of worker VMs.'
                      type: integer
                    diskEncryptionSetReference:
                      description: 'DiskEncryptionSetReference: The resource ID of
                        an associated DiskEncryptionSet, if applicable.'
                      properties:
                        armId:
                          description: |-
                            ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                            The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                            ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                          pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                          type: string
                        group:
                          description: Group is the Kubernetes group of the resource.
                          type: string
                        kind:
                          description: Kind is the Kubernetes kind of the resource.
                          type: string
                        name:
                          description: Name is the Kubernetes name of the resource.
                          type: string
                      type: object
                    diskSizeGB:
                      description: 'DiskSizeGB: The disk size of the worker VMs.'
                      type: integer
                    encryptionAtHost:
                      description: 'EncryptionAtHost: Whether master virtual machines
                        are encrypted at host.'
                      enum:
                      - Disabled
                      - Enabled
                      type: string
                    name:
                      description: 'Name: The worker profile name.'
                      type: string
                    subnetReference:
                      description: 'SubnetReference: The Azure resource ID of the
                        worker subnet.'
                      properties:
                        armId:
                          description: |-
                            ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                            The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                            ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                          pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                          type: string
                        group:
                          description: Group is the Kubernetes group of the resource.
                          type: string
                        kind:
                          description: Kind is the Kubernetes kind of the resource.
                          type: string
                        name:
                          description: Name is the Kubernetes name of the resource.
                          type: string
                      type: object
                    vmSize:
                      description: 'VmSize: The size of the worker VMs.'
                      type: string
                  type: object
                type: array
            required:
            - location
            - owner
            type: object
          status:
            description: OpenShiftCluster represents an Azure Red Hat OpenShift cluster.
            properties:
              apiserverProfile:
                description: 'ApiserverProfile: The cluster API server profile.'
                properties:
                  ip:
                    description: 'Ip: The IP of the cluster API server.'
                    type: string
                  url:
                    description: 'Url: The URL to access the cluster API server.'
                    type: string
                  visibility:
                    description: 'Visibility: API server visibility.'
                    type: string
                type: object
              clusterProfile:
                description: 'ClusterProfile: The cluster profile.'
                properties:
                  domain:
                    description: 'Domain: The domain for the cluster.'
                    type: string
                  fipsValidatedModules:
                    description: 'FipsValidatedModules: If FIPS validated crypto modules
                      are used'
                    type: string
                  resourceGroupId:
                    description: 'ResourceGroupId: The ID of the cluster resource
                      group.'
                    type: string
                  version:
                    description: 'Version: The version of the cluster.'
                    type: string
                type: object
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              consoleProfile:
                description: 'ConsoleProfile: The console profile.'
                properties:
                  url:
                    description: 'Url: The URL to access the cluster console.'
                    type: string
                type: object
              id:
                description: |-
                  Id: Fully qualified resource ID for the resource. Ex -
                  /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
                type: string
              ingressProfiles:
                description: 'IngressProfiles: The cluster ingress profiles.'
                items:
                  description: IngressProfile represents an ingress profile.
                  properties:
                    ip:
                      description: 'Ip: The IP of the ingress.'
                      type: string
                    name:
                      description: 'Name: The ingress profile name.'
                      type: string
                    visibility:
                      description: 'Visibility: Ingress visibility.'
                      type: string
                  type: object
                type: array
              location:
                description: 'Location: The geo-location where the resource lives'
                type: string
              masterProfile:
                description: 'MasterProfile: The cluster master profile.'
                properties:
                  diskEncryptionSetId:
                    description: 'DiskEncryptionSetId: The resource ID of an associated
                      DiskEncryptionSet, if applicable.'
                    type: string
                  encryptionAtHost:
                    description: 'EncryptionAtHost: Whether master virtual machines
                      are encrypted at host.'
                    type: string
                  subnetId:
                    description: 'SubnetId: The Azure resource ID of the master subnet.'
                    type: string
                  vmSize:
                    description: 'VmSize: The size of the master VMs.'
                    type: string
                type: object
              name:
                description: 'Name: The name of the resource'
                type: string
              networkProfile:
                description: 'NetworkProfile: The cluster network profile.'
                properties:
                  loadBalancerProfile:
                    description: 'LoadBalancerProfile: The cluster load balancer profile.'
                    properties:
                      effectiveOutboundIps:
                        description: 'EffectiveOutboundIps: The list of effective
                          outbound IP addresses of the public load balancer.'
                        items:
                          description: EffectiveOutboundIP represents an effective
                            outbound IP resource of the cluster public load balancer.
                          properties:
                            id:
                              description: 'Id: The fully qualified Azure resource
                                id of an IP address resource.'
                              type: string
                          type: object
                        type: array
                      managedOutboundIps:
                        description: 'ManagedOutboundIps: The desired managed outbound
                          IPs for the cluster public load balancer.'
                        properties:
                          count:
                            description: |-
                              Count: Count represents the desired number of IPv4 outbound IPs created and managed by Azure for the cluster public load
                              balancer.  Allowed values are in the range of 1 - 20.  The default value is 1.
                            type: integer
                        type: object
                    type: object
                  outboundType:
                    description: 'OutboundType: The OutboundType used for egress traffic.'
                    type: string
                  podCidr:
                    description: 'PodCidr: The CIDR used for OpenShift/Kubernetes
                      Pods.'
                    type: string
                  preconfiguredNSG:
                    description: 'PreconfiguredNSG: Specifies whether subnets are
                      pre-attached with an NSG'
                    type: string
                  serviceCidr:
                    description: 'ServiceCidr: The CIDR used for OpenShift/Kubernetes
                      Services.'
                    type: string
                type: object
              provisioningState:
                description: 'ProvisioningState: The cluster provisioning state.'
                type: string
              servicePrincipalProfile:
                description: 'ServicePrincipalProfile: The cluster service principal
                  profile.'
                properties:
                  clientId:
                    description: 'ClientId: The client ID used for the cluster.'
                    type: string
                type: object
              systemData:
                description: 'SystemData: Azure Resource Manager metadata containing
                  createdBy and modifiedBy information.'
                properties:
                  createdAt:
                    description: 'CreatedAt: The timestamp of resource creation (UTC).'
                    type: string
                  createdBy:
                    description: 'CreatedBy: The identity that created the resource.'
                    type: string
                  createdByType:
                    description: 'CreatedByType: The type of identity that created
                      the resource.'
                    type: string
                  lastModifiedAt:
                    description: 'LastModifiedAt: The timestamp of resource last modification
                      (UTC)'
                    type: string
                  lastModifiedBy:
                    description: 'LastModifiedBy: The identity that last modified
                      the resource.'
                    type: string
                  lastModifiedByType:
                    description: 'LastModifiedByType: The type of identity that last
                      modified the resource.'
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              type:
                description: 'Type: The type of the resource. E.g. "Microsoft.Compute/virtualMachines"
                  or "Microsoft.Storage/storageAccounts"'
                type: string
              workerProfiles:
                description: 'WorkerProfiles: The cluster worker profiles.'
                items:
                  description: WorkerProfile represents a worker profile.
                  properties:
                    count:
                      description: 'Count: The number of worker VMs.'
                      type: integer
                    diskEncryptionSetId:
                      description: 'DiskEncryptionSetId: The resource ID of an associated
                        DiskEncryptionSet, if applicable.'
                      type: string
                    diskSizeGB:
                      description: 'DiskSizeGB: The disk size of the worker VMs.'
                      type: integer
                    encryptionAtHost:
                      description: 'EncryptionAtHost: Whether master virtual machines
                        are encrypted at host.'
                      type: string
                    name:
                      description: 'Name: The worker profile name.'
                      type: string
                    subnetId:
                      description: 'SubnetId: The Azure resource ID of the worker
                        subnet.'
                      type: string
                    vmSize:
                      description: 'VmSize: The size of the worker VMs.'
                      type: string
                  type: object
                type: array
              workerProfilesStatus:
                description: 'WorkerProfilesStatus: The cluster worker profiles status.'
                items:
                  description: WorkerProfile represents a worker profile.
                  properties:
                    count:
                      description: 'Count: The number of worker VMs.'
                      type: integer
                    diskEncryptionSetId:
                      description: 'DiskEncryptionSetId: The resource ID of an associated
                        DiskEncryptionSet, if applicable.'
                      type: string
                    diskSizeGB:
                      description: 'DiskSizeGB: The disk size of the worker VMs.'
                      type: integer
                    encryptionAtHost:
                      description: 'EncryptionAtHost: Whether master virtual machines
                        are encrypted at host.'
                      type: string
                    name:
                      description: 'Name: The worker profile name.'
                      type: string
                    subnetId:
                      description: 'SubnetId: The Azure resource ID of the worker
                        subnet.'
                      type: string
                    vmSize:
                      description: 'VmSize: The size of the worker VMs.'
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20231122storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20231122.OpenShiftCluster
          Generator information:
          - Generated from: /redhatopenshift/resource-manager/Microsoft.RedHatOpenShift/openshiftclusters/stable/2023-11-22/redhatopenshift.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.RedHatOpenShift/openShiftClusters/{resourceName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20231122.OpenShiftCluster_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              apiserverProfile:
                description: |-
                  Storage version of v1api20231122.APIServerProfile
                  APIServerProfile represents an API server profile.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  visibility:
                    type: string
                type: object
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              clusterProfile:
                description: |-
                  Storage version of v1api20231122.ClusterProfile
                  ClusterProfile represents a cluster profile.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  domain:
                    type: string
                  fipsValidatedModules:
                    type: string
                  pullSecret:
                    description: |-
                      SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  resourceGroupId:
                    type: string
                  version:
                    type: string
                type: object
              ingressProfiles:
                items:
                  description: |-
                    Storage version of v1api20231122.IngressProfile
                    IngressProfile represents an ingress profile.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    name:
                      type: string
                    visibility:
                      type: string
                  type: object
                type: array
              location:
                type: string
              masterProfile:
                description: |-
                  Storage version of v1api20231122.MasterProfile
                  MasterProfile represents a master profile.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  diskEncryptionSetReference:
                    description: 'DiskEncryptionSetReference: The resource ID of an
                      associated DiskEncryptionSet, if applicable.'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  encryptionAtHost:
                    type: string
                  subnetReference:
                    description: 'SubnetReference: The Azure resource ID of the master
                      subnet.'
                    properties:
                      armId:
                        description: |-
                          ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                          The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                          ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                        pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                        type: string
                      group:
                        description: Group is the Kubernetes group of the resource.
                        type: string
                      kind:
                        description: Kind is the Kubernetes kind of the resource.
                        type: string
                      name:
                        description: Name is the Kubernetes name of the resource.
                        type: string
                    type: object
                  vmSize:
                    type: string
                type: object
              networkProfile:
                description: |-
                  Storage version of v1api20231122.NetworkProfile
                  NetworkProfile represents a network profile.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  loadBalancerProfile:
                    description: |-
                      Storage version of v1api20231122.LoadBalancerProfile
                      LoadBalancerProfile represents the profile of the cluster public load balancer.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      managedOutboundIps:
                        description: |-
                          Storage version of v1api20231122.ManagedOutboundIPs
                          ManagedOutboundIPs represents the desired managed outbound IPs for the cluster public load balancer.
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          count:
                            type: integer
                        type: object
                    type: object
                  outboundType:
                    type: string
                  podCidr:
                    type: string
                  preconfiguredNSG:
                    type: string
                  serviceCidr:
                    type: string
                type: object
              operatorSpec:
                description: |-
                  Storage version of v1api20231122.OpenShiftClusterOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  secrets:
                    description: Storage version of v1api20231122.OpenShiftClusterOperatorSecrets
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      adminCredentials:
                        description: |-
                          SecretDestination describes the location to store a single secret value.
                          Note: This is similar to: ConfigMapDestination in configmaps.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      password:
                        description: |-
                          SecretDestination describes the location to store a single secret value.
                          Note: This is similar to: ConfigMapDestination in configmaps.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      username:
                        description: |-
                          SecretDestination describes the location to store a single secret value.
                          Note: This is similar to: ConfigMapDestination in configmaps.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the Kubernetes secret being
                              referenced.
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes secret to write to.
                              The secret will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              servicePrincipalProfile:
                description: |-
                  Storage version of v1api20231122.ServicePrincipalProfile
                  ServicePrincipalProfile represents a service principal profile.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  clientId:
                    type: string
                  clientIdFromConfig:
                    description: |-
                      ConfigMapReference is a reference to a Kubernetes configmap and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes configmap being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes configmap being referenced.
                          The configmap must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  clientSecret:
                    description: |-
                      SecretReference is a reference to a Kubernetes secret and key in the same namespace as
                      the resource it is on.
                    properties:
                      key:
                        description: Key is the key in the Kubernetes secret being
                          referenced
                        type: string
                      name:
                        description: |-
                          Name is the name of the Kubernetes secret being referenced.
                          The secret must be in the same namespace as the resource
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              workerProfiles:
                items:
                  description: |-
                    Storage version of v1api20231122.WorkerProfile
                    WorkerProfile represents a worker profile.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    count:
                      type: integer
                    diskEncryptionSetReference:
                      description: 'DiskEncryptionSetReference: The resource ID of
                        an associated DiskEncryptionSet, if applicable.'
                      properties:
                        armId:
                          description: |-
                            ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                            The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                            ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                          pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                          type: string
                        group:
                          description: Group is the Kubernetes group of the resource.
                          type: string
                        kind:
                          description: Kind is the Kubernetes kind of the resource.
                          type: string
                        name:
                          description: Name is the Kubernetes name of the resource.
                          type: string
                      type: object
                    diskSizeGB:
                      type: integer
                    encryptionAtHost:
                      type: string
                    name:
                      type: string
                    subnetReference:
                      description: 'SubnetReference: The Azure resource ID of the
                        worker subnet.'
                      properties:
                        armId:
                          description: |-
                            ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                            The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                            ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                          pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                          type: string
                        group:
                          description: Group is the Kubernetes group of the resource.
                          type: string
                        kind:
                          description: Kind is the Kubernetes kind of the resource.
                          type: string
                        name:
                          description: Name is the Kubernetes name of the resource.
                          type: string
                      type: object
                    vmSize:
                      type: string
                  type: object
                type: array
            required:
            - owner
            type: object
          status:
            description: |-
              Storage version of v1api20231122.OpenShiftCluster_STATUS
              OpenShiftCluster represents an Azure Red Hat OpenShift cluster.
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              apiserverProfile:
                description: |-
                  Storage version of v1api20231122.APIServerProfile_STATUS
                  APIServerProfile represents an API server profile.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  ip:
                    type: string
                  url:
                    type: string
                  visibility:
                    type: string
                type: object
              clusterProfile:
                description: |-
                  Storage version of v1api20231122.ClusterProfile_STATUS
                  ClusterProfile represents a cluster profile.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  domain:
                    type: string
                  fipsValidatedModules:
                    type: string
                  resourceGroupId:
                    type: string
                  version:
                    type: string
                type: object
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              consoleProfile:
                description: |-
                  Storage version of v1api20231122.ConsoleProfile_STATUS
                  ConsoleProfile represents a console profile.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  url:
                    type: string
                type: object
              id:
                type: string
              ingressProfiles:
                items:
                  description: |-
                    Storage version of v1api20231122.IngressProfile_STATUS
                    IngressProfile represents an ingress profile.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    ip:
                      type: string
                    name:
                      type: string
                    visibility:
                      type: string
                  type: object
                type: array
              location:
                type: string
              masterProfile:
                description: |-
                  Storage version of v1api20231122.MasterProfile_STATUS
                  MasterProfile represents a master profile.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  diskEncryptionSetId:
                    type: string
                  encryptionAtHost:
                    type: string
                  subnetId:
                    type: string
                  vmSize:
                    type: string
                type: object
              name:
                type: string
              networkProfile:
                description: |-
                  Storage version of v1api20231122.NetworkProfile_STATUS
                  NetworkProfile represents a network profile.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  loadBalancerProfile:
                    description: |-
                      Storage version of v1api20231122.LoadBalancerProfile_STATUS
                      LoadBalancerProfile represents the profile of the cluster public load balancer.
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      effectiveOutboundIps:
                        items:
                          description: |-
                            Storage version of v1api20231122.EffectiveOutboundIP_STATUS
                            EffectiveOutboundIP represents an effective outbound IP resource of the cluster public load balancer.
                          properties:
                            $propertyBag:
                              additionalProperties:
                                type: string
                              description: |-
                                PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                                resources, allowing for full fidelity round trip conversions
                              type: object
                            id:
                              type: string
                          type: object
                        type: array
                      managedOutboundIps:
                        description: |-
                          Storage version of v1api20231122.ManagedOutboundIPs_STATUS
                          ManagedOutboundIPs represents the desired managed outbound IPs for the cluster public load balancer.
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          count:
                            type: integer
                        type: object
                    type: object
                  outboundType:
                    type: string
                  podCidr:
                    type: string
                  preconfiguredNSG:
                    type: string
                  serviceCidr:
                    type: string
                type: object
              provisioningState:
                type: string
              servicePrincipalProfile:
                description: |-
                  Storage version of v1api20231122.ServicePrincipalProfile_STATUS
                  ServicePrincipalProfile represents a service principal profile.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  clientId:
                    type: string
                type: object
              systemData:
                description: |-
                  Storage version of v1api20231122.SystemData_STATUS
                  Metadata pertaining to creation and last modification of the resource.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  createdAt:
                    type: string
                  createdBy:
                    type: string
                  createdByType:
                    type: string
                  lastModifiedAt:
                    type: string
                  lastModifiedBy:
                    type: string
                  lastModifiedByType:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              type:
                type: string
              workerProfiles:
                items:
                  description: |-
                    Storage version of v1api20231122.WorkerProfile_STATUS
                    WorkerProfile represents a worker profile.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    count:
                      type: integer
                    diskEncryptionSetId:
                      type: string
                    diskSizeGB:
                      type: integer
                    encryptionAtHost:
                      type: string
                    name:
                      type: string
                    subnetId:
                      type: string
                    vmSize:
                      type: string
                  type: object
                type: array
              workerProfilesStatus:
                items:
                  description: |-
                    Storage version of v1api20231122.WorkerProfile_STATUS
                    WorkerProfile represents a worker profile.
                  properties:
                    $propertyBag:
                      additionalProperties:
                        type: string
                      description: |-
                        PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                        resources, allowing for full fidelity round trip conversions
                      type: object
                    count:
                      type: integer
                    diskEncryptionSetId:
                      type: string
                    diskSizeGB:
                      type: integer
                    encryptionAtHost:
                      type: string
                    name:
                      type: string
                    subnetId:
                      type: string
                    vmSize:
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
