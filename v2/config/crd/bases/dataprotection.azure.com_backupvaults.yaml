---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: backupvaults.dataprotection.azure.com
spec:
  group: dataprotection.azure.com
  names:
    kind: BackupVault
    listKind: BackupVaultList
    plural: backupvaults
    singular: backupvault
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20230101
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /dataprotection/resource-manager/Microsoft.DataProtection/stable/2023-01-01/dataprotection.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DataProtection/backupVaults/{vaultName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              identity:
                description: 'Identity: Input Managed Identity Details'
                properties:
                  type:
                    description: 'Type: The identityType which can be either SystemAssigned
                      or None'
                    type: string
                type: object
              location:
                description: 'Location: Resource location.'
                type: string
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  configMaps:
                    description: 'ConfigMaps: configures where to place operator written
                      ConfigMaps.'
                    properties:
                      principalId:
                        description: 'PrincipalId: indicates where the PrincipalId
                          config map should be placed. If omitted, no config map will
                          be created.'
                        properties:
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              properties:
                description: 'Properties: BackupVaultResource properties'
                properties:
                  featureSettings:
                    description: 'FeatureSettings: Feature Settings'
                    properties:
                      crossSubscriptionRestoreSettings:
                        description: 'CrossSubscriptionRestoreSettings: CrossSubscriptionRestore
                          Settings'
                        properties:
                          state:
                            description: 'State: CrossSubscriptionRestore state'
                            enum:
                            - Disabled
                            - Enabled
                            - PermanentlyDisabled
                            type: string
                        type: object
                    type: object
                  monitoringSettings:
                    description: 'MonitoringSettings: Monitoring Settings'
                    properties:
                      azureMonitorAlertSettings:
                        description: 'AzureMonitorAlertSettings: Settings for Azure
                          Monitor based alerts'
                        properties:
                          alertsForAllJobFailures:
                            enum:
                            - Disabled
                            - Enabled
                            type: string
                        type: object
                    type: object
                  securitySettings:
                    description: 'SecuritySettings: Security Settings'
                    properties:
                      immutabilitySettings:
                        description: 'ImmutabilitySettings: Immutability Settings
                          at vault level'
                        properties:
                          state:
                            description: 'State: Immutability state'
                            enum:
                            - Disabled
                            - Locked
                            - Unlocked
                            type: string
                        type: object
                      softDeleteSettings:
                        description: 'SoftDeleteSettings: Soft delete related settings'
                        properties:
                          retentionDurationInDays:
                            description: 'RetentionDurationInDays: Soft delete retention
                              duration'
                            type: number
                          state:
                            description: 'State: State of soft delete'
                            enum:
                            - AlwaysOn
                            - "Off"
                            - "On"
                            type: string
                        type: object
                    type: object
                  storageSettings:
                    description: 'StorageSettings: Storage Settings'
                    items:
                      description: Storage setting
                      properties:
                        datastoreType:
                          description: 'DatastoreType: Gets or sets the type of the
                            datastore.'
                          enum:
                          - ArchiveStore
                          - OperationalStore
                          - VaultStore
                          type: string
                        type:
                          description: 'Type: Gets or sets the type.'
                          enum:
                          - GeoRedundant
                          - LocallyRedundant
                          - ZoneRedundant
                          type: string
                      type: object
                    type: array
                required:
                - storageSettings
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
            required:
            - owner
            - properties
            type: object
          status:
            description: Backup Vault Resource
            properties:
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              eTag:
                description: 'ETag: Optional ETag.'
                type: string
              id:
                description: 'Id: Resource Id represents the complete path to the
                  resource.'
                type: string
              identity:
                description: 'Identity: Input Managed Identity Details'
                properties:
                  principalId:
                    description: |-
                      PrincipalId: The object ID of the service principal object for the managed identity that is used to grant role-based
                      access to an Azure resource.
                    type: string
                  tenantId:
                    description: 'TenantId: A Globally Unique Identifier (GUID) that
                      represents the Azure AD tenant where the resource is now a member.'
                    type: string
                  type:
                    description: 'Type: The identityType which can be either SystemAssigned
                      or None'
                    type: string
                type: object
              location:
                description: 'Location: Resource location.'
                type: string
              name:
                description: 'Name: Resource name associated with the resource.'
                type: string
              properties:
                description: 'Properties: BackupVaultResource properties'
                properties:
                  featureSettings:
                    description: 'FeatureSettings: Feature Settings'
                    properties:
                      crossSubscriptionRestoreSettings:
                        description: 'CrossSubscriptionRestoreSettings: CrossSubscriptionRestore
                          Settings'
                        properties:
                          state:
                            description: 'State: CrossSubscriptionRestore state'
                            type: string
                        type: object
                    type: object
                  isVaultProtectedByResourceGuard:
                    description: 'IsVaultProtectedByResourceGuard: Is vault protected
                      by resource guard'
                    type: boolean
                  monitoringSettings:
                    description: 'MonitoringSettings: Monitoring Settings'
                    properties:
                      azureMonitorAlertSettings:
                        description: 'AzureMonitorAlertSettings: Settings for Azure
                          Monitor based alerts'
                        properties:
                          alertsForAllJobFailures:
                            type: string
                        type: object
                    type: object
                  provisioningState:
                    description: 'ProvisioningState: Provisioning state of the BackupVault
                      resource'
                    type: string
                  resourceMoveDetails:
                    description: 'ResourceMoveDetails: Resource move details for backup
                      vault'
                    properties:
                      completionTimeUtc:
                        description: 'CompletionTimeUtc: Completion time in UTC of
                          latest ResourceMove operation attempted. ISO 8601 format.'
                        type: string
                      operationId:
                        description: 'OperationId: CorrelationId of latest ResourceMove
                          operation attempted'
                        type: string
                      sourceResourcePath:
                        description: 'SourceResourcePath: ARM resource path of source
                          resource'
                        type: string
                      startTimeUtc:
                        description: 'StartTimeUtc: Start time in UTC of latest ResourceMove
                          operation attempted. ISO 8601 format.'
                        type: string
                      targetResourcePath:
                        description: 'TargetResourcePath: ARM resource path of target
                          resource used in latest ResourceMove operation'
                        type: string
                    type: object
                  resourceMoveState:
                    description: 'ResourceMoveState: Resource move state for backup
                      vault'
                    type: string
                  securitySettings:
                    description: 'SecuritySettings: Security Settings'
                    properties:
                      immutabilitySettings:
                        description: 'ImmutabilitySettings: Immutability Settings
                          at vault level'
                        properties:
                          state:
                            description: 'State: Immutability state'
                            type: string
                        type: object
                      softDeleteSettings:
                        description: 'SoftDeleteSettings: Soft delete related settings'
                        properties:
                          retentionDurationInDays:
                            description: 'RetentionDurationInDays: Soft delete retention
                              duration'
                            type: number
                          state:
                            description: 'State: State of soft delete'
                            type: string
                        type: object
                    type: object
                  storageSettings:
                    description: 'StorageSettings: Storage Settings'
                    items:
                      description: Storage setting
                      properties:
                        datastoreType:
                          description: 'DatastoreType: Gets or sets the type of the
                            datastore.'
                          type: string
                        type:
                          description: 'Type: Gets or sets the type.'
                          type: string
                      type: object
                    type: array
                type: object
              systemData:
                description: 'SystemData: Metadata pertaining to creation and last
                  modification of the resource.'
                properties:
                  createdAt:
                    description: 'CreatedAt: The timestamp of resource creation (UTC).'
                    type: string
                  createdBy:
                    description: 'CreatedBy: The identity that created the resource.'
                    type: string
                  createdByType:
                    description: 'CreatedByType: The type of identity that created
                      the resource.'
                    type: string
                  lastModifiedAt:
                    description: 'LastModifiedAt: The timestamp of resource last modification
                      (UTC)'
                    type: string
                  lastModifiedBy:
                    description: 'LastModifiedBy: The identity that last modified
                      the resource.'
                    type: string
                  lastModifiedByType:
                    description: 'LastModifiedByType: The type of identity that last
                      modified the resource.'
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              type:
                description: 'Type: Resource type represents the complete path of
                  the form Namespace/ResourceType/ResourceType/...'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20230101storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20230101.BackupVault
          Generator information:
          - Generated from: /dataprotection/resource-manager/Microsoft.DataProtection/stable/2023-01-01/dataprotection.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DataProtection/backupVaults/{vaultName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20230101.BackupVault_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              identity:
                description: |-
                  Storage version of v1api20230101.DppIdentityDetails
                  Identity details
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  type:
                    type: string
                type: object
              location:
                type: string
              operatorSpec:
                description: |-
                  Storage version of v1api20230101.BackupVaultOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  configMaps:
                    description: Storage version of v1api20230101.BackupVaultOperatorConfigMaps
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      principalId:
                        description: |-
                          ConfigMapDestination describes the location to store a single configmap value
                          Note: This is similar to: SecretDestination in secrets.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              properties:
                description: |-
                  Storage version of v1api20230101.BackupVaultSpec
                  Backup Vault
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  featureSettings:
                    description: |-
                      Storage version of v1api20230101.FeatureSettings
                      Class containing feature settings of vault
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      crossSubscriptionRestoreSettings:
                        description: |-
                          Storage version of v1api20230101.CrossSubscriptionRestoreSettings
                          CrossSubscriptionRestore Settings
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          state:
                            type: string
                        type: object
                    type: object
                  monitoringSettings:
                    description: |-
                      Storage version of v1api20230101.MonitoringSettings
                      Monitoring Settings
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      azureMonitorAlertSettings:
                        description: |-
                          Storage version of v1api20230101.AzureMonitorAlertSettings
                          Settings for Azure Monitor based alerts
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          alertsForAllJobFailures:
                            type: string
                        type: object
                    type: object
                  securitySettings:
                    description: |-
                      Storage version of v1api20230101.SecuritySettings
                      Class containing security settings of vault
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      immutabilitySettings:
                        description: |-
                          Storage version of v1api20230101.ImmutabilitySettings
                          Immutability Settings at vault level
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          state:
                            type: string
                        type: object
                      softDeleteSettings:
                        description: |-
                          Storage version of v1api20230101.SoftDeleteSettings
                          Soft delete related settings
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          retentionDurationInDays:
                            type: number
                          state:
                            type: string
                        type: object
                    type: object
                  storageSettings:
                    items:
                      description: |-
                        Storage version of v1api20230101.StorageSetting
                        Storage setting
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        datastoreType:
                          type: string
                        type:
                          type: string
                      type: object
                    type: array
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - owner
            type: object
          status:
            description: |-
              Storage version of v1api20230101.BackupVaultResource_STATUS
              Backup Vault Resource
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              eTag:
                type: string
              id:
                type: string
              identity:
                description: |-
                  Storage version of v1api20230101.DppIdentityDetails_STATUS
                  Identity details
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  principalId:
                    type: string
                  tenantId:
                    type: string
                  type:
                    type: string
                type: object
              location:
                type: string
              name:
                type: string
              properties:
                description: |-
                  Storage version of v1api20230101.BackupVault_STATUS
                  Backup Vault
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  featureSettings:
                    description: |-
                      Storage version of v1api20230101.FeatureSettings_STATUS
                      Class containing feature settings of vault
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      crossSubscriptionRestoreSettings:
                        description: |-
                          Storage version of v1api20230101.CrossSubscriptionRestoreSettings_STATUS
                          CrossSubscriptionRestore Settings
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          state:
                            type: string
                        type: object
                    type: object
                  isVaultProtectedByResourceGuard:
                    type: boolean
                  monitoringSettings:
                    description: |-
                      Storage version of v1api20230101.MonitoringSettings_STATUS
                      Monitoring Settings
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      azureMonitorAlertSettings:
                        description: |-
                          Storage version of v1api20230101.AzureMonitorAlertSettings_STATUS
                          Settings for Azure Monitor based alerts
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          alertsForAllJobFailures:
                            type: string
                        type: object
                    type: object
                  provisioningState:
                    type: string
                  resourceMoveDetails:
                    description: |-
                      Storage version of v1api20230101.ResourceMoveDetails_STATUS
                      ResourceMoveDetails will be returned in response to GetResource call from ARM
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      completionTimeUtc:
                        type: string
                      operationId:
                        type: string
                      sourceResourcePath:
                        type: string
                      startTimeUtc:
                        type: string
                      targetResourcePath:
                        type: string
                    type: object
                  resourceMoveState:
                    type: string
                  securitySettings:
                    description: |-
                      Storage version of v1api20230101.SecuritySettings_STATUS
                      Class containing security settings of vault
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      immutabilitySettings:
                        description: |-
                          Storage version of v1api20230101.ImmutabilitySettings_STATUS
                          Immutability Settings at vault level
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          state:
                            type: string
                        type: object
                      softDeleteSettings:
                        description: |-
                          Storage version of v1api20230101.SoftDeleteSettings_STATUS
                          Soft delete related settings
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          retentionDurationInDays:
                            type: number
                          state:
                            type: string
                        type: object
                    type: object
                  storageSettings:
                    items:
                      description: |-
                        Storage version of v1api20230101.StorageSetting_STATUS
                        Storage setting
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        datastoreType:
                          type: string
                        type:
                          type: string
                      type: object
                    type: array
                type: object
              systemData:
                description: |-
                  Storage version of v1api20230101.SystemData_STATUS
                  Metadata pertaining to creation and last modification of the resource.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  createdAt:
                    type: string
                  createdBy:
                    type: string
                  createdByType:
                    type: string
                  lastModifiedAt:
                    type: string
                  lastModifiedBy:
                    type: string
                  lastModifiedByType:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              type:
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20231101
    schema:
      openAPIV3Schema:
        description: |-
          Generator information:
          - Generated from: /dataprotection/resource-manager/Microsoft.DataProtection/stable/2023-11-01/dataprotection.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DataProtection/backupVaults/{vaultName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              identity:
                description: 'Identity: Input Managed Identity Details'
                properties:
                  type:
                    description: 'Type: The identityType which can be either SystemAssigned,
                      UserAssigned, ''SystemAssigned,UserAssigned'' or None'
                    type: string
                  userAssignedIdentities:
                    description: 'UserAssignedIdentities: Gets or sets the user assigned
                      identities.'
                    items:
                      description: Information about the user assigned identity for
                        the resource
                      properties:
                        reference:
                          description: ResourceReference represents a resource reference,
                            either to a Kubernetes resource or directly to an Azure
                            resource via ARMID
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              location:
                description: 'Location: Resource location.'
                type: string
              operatorSpec:
                description: |-
                  OperatorSpec: The specification for configuring operator behavior. This field is interpreted by the operator and not
                  passed directly to Azure
                properties:
                  configMapExpressions:
                    description: 'ConfigMapExpressions: configures where to place
                      operator written dynamic ConfigMaps (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  configMaps:
                    description: 'ConfigMaps: configures where to place operator written
                      ConfigMaps.'
                    properties:
                      principalId:
                        description: 'PrincipalId: indicates where the PrincipalId
                          config map should be placed. If omitted, no config map will
                          be created.'
                        properties:
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  secretExpressions:
                    description: 'SecretExpressions: configures where to place operator
                      written dynamic secrets (created with CEL expressions).'
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              properties:
                description: 'Properties: BackupVaultResource properties'
                properties:
                  featureSettings:
                    description: 'FeatureSettings: Feature Settings'
                    properties:
                      crossRegionRestoreSettings:
                        properties:
                          state:
                            description: 'State: CrossRegionRestore state'
                            enum:
                            - Disabled
                            - Enabled
                            type: string
                        type: object
                      crossSubscriptionRestoreSettings:
                        description: 'CrossSubscriptionRestoreSettings: CrossSubscriptionRestore
                          Settings'
                        properties:
                          state:
                            description: 'State: CrossSubscriptionRestore state'
                            enum:
                            - Disabled
                            - Enabled
                            - PermanentlyDisabled
                            type: string
                        type: object
                    type: object
                  monitoringSettings:
                    description: 'MonitoringSettings: Monitoring Settings'
                    properties:
                      azureMonitorAlertSettings:
                        description: 'AzureMonitorAlertSettings: Settings for Azure
                          Monitor based alerts'
                        properties:
                          alertsForAllJobFailures:
                            enum:
                            - Disabled
                            - Enabled
                            type: string
                        type: object
                    type: object
                  replicatedRegions:
                    description: 'ReplicatedRegions: List of replicated regions for
                      Backup Vault'
                    items:
                      type: string
                    type: array
                  securitySettings:
                    description: 'SecuritySettings: Security Settings'
                    properties:
                      immutabilitySettings:
                        description: 'ImmutabilitySettings: Immutability Settings
                          at vault level'
                        properties:
                          state:
                            description: 'State: Immutability state'
                            enum:
                            - Disabled
                            - Locked
                            - Unlocked
                            type: string
                        type: object
                      softDeleteSettings:
                        description: 'SoftDeleteSettings: Soft delete related settings'
                        properties:
                          retentionDurationInDays:
                            description: 'RetentionDurationInDays: Soft delete retention
                              duration'
                            type: number
                          state:
                            description: 'State: State of soft delete'
                            enum:
                            - AlwaysOn
                            - "Off"
                            - "On"
                            type: string
                        type: object
                    type: object
                  storageSettings:
                    description: 'StorageSettings: Storage Settings'
                    items:
                      description: Storage setting
                      properties:
                        datastoreType:
                          description: 'DatastoreType: Gets or sets the type of the
                            datastore.'
                          enum:
                          - ArchiveStore
                          - OperationalStore
                          - VaultStore
                          type: string
                        type:
                          description: 'Type: Gets or sets the type.'
                          enum:
                          - GeoRedundant
                          - LocallyRedundant
                          - ZoneRedundant
                          type: string
                      type: object
                    type: array
                required:
                - storageSettings
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
            required:
            - owner
            - properties
            type: object
          status:
            description: Backup Vault Resource
            properties:
              conditions:
                description: 'Conditions: The observed state of the resource'
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              eTag:
                description: 'ETag: Optional ETag.'
                type: string
              id:
                description: 'Id: Resource Id represents the complete path to the
                  resource.'
                type: string
              identity:
                description: 'Identity: Input Managed Identity Details'
                properties:
                  principalId:
                    description: |-
                      PrincipalId: The object ID of the service principal object for the managed identity that is used to grant role-based
                      access to an Azure resource.
                    type: string
                  tenantId:
                    description: 'TenantId: A Globally Unique Identifier (GUID) that
                      represents the Azure AD tenant where the resource is now a member.'
                    type: string
                  type:
                    description: 'Type: The identityType which can be either SystemAssigned,
                      UserAssigned, ''SystemAssigned,UserAssigned'' or None'
                    type: string
                  userAssignedIdentities:
                    additionalProperties:
                      description: User assigned identity properties
                      properties:
                        clientId:
                          description: 'ClientId: The client ID of the assigned identity.'
                          type: string
                        principalId:
                          description: 'PrincipalId: The principal ID of the assigned
                            identity.'
                          type: string
                      type: object
                    description: 'UserAssignedIdentities: Gets or sets the user assigned
                      identities.'
                    type: object
                type: object
              location:
                description: 'Location: Resource location.'
                type: string
              name:
                description: 'Name: Resource name associated with the resource.'
                type: string
              properties:
                description: 'Properties: BackupVaultResource properties'
                properties:
                  featureSettings:
                    description: 'FeatureSettings: Feature Settings'
                    properties:
                      crossRegionRestoreSettings:
                        properties:
                          state:
                            description: 'State: CrossRegionRestore state'
                            type: string
                        type: object
                      crossSubscriptionRestoreSettings:
                        description: 'CrossSubscriptionRestoreSettings: CrossSubscriptionRestore
                          Settings'
                        properties:
                          state:
                            description: 'State: CrossSubscriptionRestore state'
                            type: string
                        type: object
                    type: object
                  isVaultProtectedByResourceGuard:
                    description: 'IsVaultProtectedByResourceGuard: Is vault protected
                      by resource guard'
                    type: boolean
                  monitoringSettings:
                    description: 'MonitoringSettings: Monitoring Settings'
                    properties:
                      azureMonitorAlertSettings:
                        description: 'AzureMonitorAlertSettings: Settings for Azure
                          Monitor based alerts'
                        properties:
                          alertsForAllJobFailures:
                            type: string
                        type: object
                    type: object
                  provisioningState:
                    description: 'ProvisioningState: Provisioning state of the BackupVault
                      resource'
                    type: string
                  replicatedRegions:
                    description: 'ReplicatedRegions: List of replicated regions for
                      Backup Vault'
                    items:
                      type: string
                    type: array
                  resourceMoveDetails:
                    description: 'ResourceMoveDetails: Resource move details for backup
                      vault'
                    properties:
                      completionTimeUtc:
                        description: 'CompletionTimeUtc: Completion time in UTC of
                          latest ResourceMove operation attempted. ISO 8601 format.'
                        type: string
                      operationId:
                        description: 'OperationId: CorrelationId of latest ResourceMove
                          operation attempted'
                        type: string
                      sourceResourcePath:
                        description: 'SourceResourcePath: ARM resource path of source
                          resource'
                        type: string
                      startTimeUtc:
                        description: 'StartTimeUtc: Start time in UTC of latest ResourceMove
                          operation attempted. ISO 8601 format.'
                        type: string
                      targetResourcePath:
                        description: 'TargetResourcePath: ARM resource path of target
                          resource used in latest ResourceMove operation'
                        type: string
                    type: object
                  resourceMoveState:
                    description: 'ResourceMoveState: Resource move state for backup
                      vault'
                    type: string
                  secureScore:
                    description: 'SecureScore: Secure Score of Backup Vault'
                    type: string
                  securitySettings:
                    description: 'SecuritySettings: Security Settings'
                    properties:
                      immutabilitySettings:
                        description: 'ImmutabilitySettings: Immutability Settings
                          at vault level'
                        properties:
                          state:
                            description: 'State: Immutability state'
                            type: string
                        type: object
                      softDeleteSettings:
                        description: 'SoftDeleteSettings: Soft delete related settings'
                        properties:
                          retentionDurationInDays:
                            description: 'RetentionDurationInDays: Soft delete retention
                              duration'
                            type: number
                          state:
                            description: 'State: State of soft delete'
                            type: string
                        type: object
                    type: object
                  storageSettings:
                    description: 'StorageSettings: Storage Settings'
                    items:
                      description: Storage setting
                      properties:
                        datastoreType:
                          description: 'DatastoreType: Gets or sets the type of the
                            datastore.'
                          type: string
                        type:
                          description: 'Type: Gets or sets the type.'
                          type: string
                      type: object
                    type: array
                type: object
              systemData:
                description: 'SystemData: Metadata pertaining to creation and last
                  modification of the resource.'
                properties:
                  createdAt:
                    description: 'CreatedAt: The timestamp of resource creation (UTC).'
                    type: string
                  createdBy:
                    description: 'CreatedBy: The identity that created the resource.'
                    type: string
                  createdByType:
                    description: 'CreatedByType: The type of identity that created
                      the resource.'
                    type: string
                  lastModifiedAt:
                    description: 'LastModifiedAt: The timestamp of resource last modification
                      (UTC)'
                    type: string
                  lastModifiedBy:
                    description: 'LastModifiedBy: The identity that last modified
                      the resource.'
                    type: string
                  lastModifiedByType:
                    description: 'LastModifiedByType: The type of identity that last
                      modified the resource.'
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Tags: Resource tags.'
                type: object
              type:
                description: 'Type: Resource type represents the complete path of
                  the form Namespace/ResourceType/ResourceType/...'
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: Message
      type: string
    name: v1api20231101storage
    schema:
      openAPIV3Schema:
        description: |-
          Storage version of v1api20231101.BackupVault
          Generator information:
          - Generated from: /dataprotection/resource-manager/Microsoft.DataProtection/stable/2023-11-01/dataprotection.json
          - ARM URI: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DataProtection/backupVaults/{vaultName}
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Storage version of v1api20231101.BackupVault_Spec
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              azureName:
                description: |-
                  AzureName: The name of the resource in Azure. This is often the same as the name of the resource in Kubernetes but it
                  doesn't have to be.
                type: string
              identity:
                description: |-
                  Storage version of v1api20231101.DppIdentityDetails
                  Identity details
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  type:
                    type: string
                  userAssignedIdentities:
                    items:
                      description: |-
                        Storage version of v1api20231101.UserAssignedIdentityDetails
                        Information about the user assigned identity for the resource
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        reference:
                          description: ResourceReference represents a resource reference,
                            either to a Kubernetes resource or directly to an Azure
                            resource via ARMID
                          properties:
                            armId:
                              description: |-
                                ARMID is a string of the form /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}.
                                The /resourcegroups/{resourceGroupName} bit is optional as some resources are scoped at the subscription level
                                ARMID is mutually exclusive with Group, Kind, Namespace and Name.
                              pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                              type: string
                            group:
                              description: Group is the Kubernetes group of the resource.
                              type: string
                            kind:
                              description: Kind is the Kubernetes kind of the resource.
                              type: string
                            name:
                              description: Name is the Kubernetes name of the resource.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              location:
                type: string
              operatorSpec:
                description: |-
                  Storage version of v1api20231101.BackupVaultOperatorSpec
                  Details for configuring operator behavior. Fields in this struct are interpreted by the operator directly rather than being passed to Azure
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  configMapExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  configMaps:
                    description: Storage version of v1api20231101.BackupVaultOperatorConfigMaps
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      principalId:
                        description: |-
                          ConfigMapDestination describes the location to store a single configmap value
                          Note: This is similar to: SecretDestination in secrets.go.
                          Changes to one may need to be made to the others as well.
                        properties:
                          key:
                            description: Key is the key in the ConfigMap being referenced
                            type: string
                          name:
                            description: |-
                              Name is the name of the Kubernetes ConfigMap to write to.
                              The ConfigMap will be created in the same namespace as the resource.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  secretExpressions:
                    items:
                      description: |-
                        DestinationExpression is a CEL expression and a destination to store the result in. The destination may
                        be a secret or a configmap. The value of the expression is stored at the specified location in
                        the destination.
                      properties:
                        key:
                          description: |-
                            Key is the key in the ConfigMap or Secret being written to. If the CEL expression in Value returns a string
                            this is required to identify what key to write to. If the CEL expression in Value returns a map[string]string
                            Key must not be set, instead the keys written will be determined dynamically based on the keys of the resulting
                            map[string]string.
                          type: string
                        name:
                          description: |-
                            Name is the name of the Kubernetes configmap or secret to write to.
                            The configmap or secret will be created in the same namespace as the resource.
                          type: string
                        value:
                          description: |-
                            Value is a CEL expression. The CEL expression may return a string or a map[string]string. For more information
                            on CEL in ASO see https://azure.github.io/azure-service-operator/guide/expressions/
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              originalVersion:
                type: string
              owner:
                description: |-
                  Owner: The owner of the resource. The owner controls where the resource goes when it is deployed. The owner also
                  controls the resources lifecycle. When the owner is deleted the resource will also be deleted. Owner is expected to be a
                  reference to a resources.azure.com/ResourceGroup resource
                properties:
                  armId:
                    pattern: (?i)(^(/subscriptions/([^/]+)(/resourcegroups/([^/]+))?)?/providers/([^/]+)/([^/]+/[^/]+)(/([^/]+/[^/]+))*$|^/subscriptions/([^/]+)(/resourcegroups/([^/]+))?$)
                    type: string
                  name:
                    description: This is the name of the Kubernetes resource to reference.
                    type: string
                type: object
              properties:
                description: |-
                  Storage version of v1api20231101.BackupVaultSpec
                  Backup Vault
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  featureSettings:
                    description: |-
                      Storage version of v1api20231101.FeatureSettings
                      Class containing feature settings of vault
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      crossRegionRestoreSettings:
                        description: Storage version of v1api20231101.CrossRegionRestoreSettings
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          state:
                            type: string
                        type: object
                      crossSubscriptionRestoreSettings:
                        description: |-
                          Storage version of v1api20231101.CrossSubscriptionRestoreSettings
                          CrossSubscriptionRestore Settings
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          state:
                            type: string
                        type: object
                    type: object
                  monitoringSettings:
                    description: |-
                      Storage version of v1api20231101.MonitoringSettings
                      Monitoring Settings
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      azureMonitorAlertSettings:
                        description: |-
                          Storage version of v1api20231101.AzureMonitorAlertSettings
                          Settings for Azure Monitor based alerts
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          alertsForAllJobFailures:
                            type: string
                        type: object
                    type: object
                  replicatedRegions:
                    items:
                      type: string
                    type: array
                  securitySettings:
                    description: |-
                      Storage version of v1api20231101.SecuritySettings
                      Class containing security settings of vault
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      immutabilitySettings:
                        description: |-
                          Storage version of v1api20231101.ImmutabilitySettings
                          Immutability Settings at vault level
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          state:
                            type: string
                        type: object
                      softDeleteSettings:
                        description: |-
                          Storage version of v1api20231101.SoftDeleteSettings
                          Soft delete related settings
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          retentionDurationInDays:
                            type: number
                          state:
                            type: string
                        type: object
                    type: object
                  storageSettings:
                    items:
                      description: |-
                        Storage version of v1api20231101.StorageSetting
                        Storage setting
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        datastoreType:
                          type: string
                        type:
                          type: string
                      type: object
                    type: array
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - owner
            type: object
          status:
            description: |-
              Storage version of v1api20231101.BackupVaultResource_STATUS
              Backup Vault Resource
            properties:
              $propertyBag:
                additionalProperties:
                  type: string
                description: |-
                  PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                  resources, allowing for full fidelity round trip conversions
                type: object
              conditions:
                items:
                  description: Condition defines an extension to status (an observation)
                    of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable message indicating
                        details about the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration is the .metadata.generation that the condition was set based upon. For instance, if
                        .metadata.generation is currently 12, but the .status.condition[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: |-
                        Reason for the condition's last transition.
                        Reasons are upper CamelCase (PascalCase) with no spaces. A reason is always provided, this field will not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity with which to treat failures of this type of condition.
                        For conditions which have positive polarity (Status == True is their normal/healthy state), this will be omitted when Status == True
                        For conditions which have negative polarity (Status == False is their normal/healthy state), this will be omitted when Status == False.
                        This is omitted in all cases when Status == Unknown
                      type: string
                    status:
                      description: Status of the condition, one of True, False, or
                        Unknown.
                      type: string
                    type:
                      description: Type of condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              eTag:
                type: string
              id:
                type: string
              identity:
                description: |-
                  Storage version of v1api20231101.DppIdentityDetails_STATUS
                  Identity details
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  principalId:
                    type: string
                  tenantId:
                    type: string
                  type:
                    type: string
                  userAssignedIdentities:
                    additionalProperties:
                      description: |-
                        Storage version of v1api20231101.UserAssignedIdentity_STATUS
                        User assigned identity properties
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        clientId:
                          type: string
                        principalId:
                          type: string
                      type: object
                    type: object
                type: object
              location:
                type: string
              name:
                type: string
              properties:
                description: |-
                  Storage version of v1api20231101.BackupVault_STATUS
                  Backup Vault
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  featureSettings:
                    description: |-
                      Storage version of v1api20231101.FeatureSettings_STATUS
                      Class containing feature settings of vault
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      crossRegionRestoreSettings:
                        description: Storage version of v1api20231101.CrossRegionRestoreSettings_STATUS
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          state:
                            type: string
                        type: object
                      crossSubscriptionRestoreSettings:
                        description: |-
                          Storage version of v1api20231101.CrossSubscriptionRestoreSettings_STATUS
                          CrossSubscriptionRestore Settings
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          state:
                            type: string
                        type: object
                    type: object
                  isVaultProtectedByResourceGuard:
                    type: boolean
                  monitoringSettings:
                    description: |-
                      Storage version of v1api20231101.MonitoringSettings_STATUS
                      Monitoring Settings
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      azureMonitorAlertSettings:
                        description: |-
                          Storage version of v1api20231101.AzureMonitorAlertSettings_STATUS
                          Settings for Azure Monitor based alerts
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          alertsForAllJobFailures:
                            type: string
                        type: object
                    type: object
                  provisioningState:
                    type: string
                  replicatedRegions:
                    items:
                      type: string
                    type: array
                  resourceMoveDetails:
                    description: |-
                      Storage version of v1api20231101.ResourceMoveDetails_STATUS
                      ResourceMoveDetails will be returned in response to GetResource call from ARM
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      completionTimeUtc:
                        type: string
                      operationId:
                        type: string
                      sourceResourcePath:
                        type: string
                      startTimeUtc:
                        type: string
                      targetResourcePath:
                        type: string
                    type: object
                  resourceMoveState:
                    type: string
                  secureScore:
                    type: string
                  securitySettings:
                    description: |-
                      Storage version of v1api20231101.SecuritySettings_STATUS
                      Class containing security settings of vault
                    properties:
                      $propertyBag:
                        additionalProperties:
                          type: string
                        description: |-
                          PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                          resources, allowing for full fidelity round trip conversions
                        type: object
                      immutabilitySettings:
                        description: |-
                          Storage version of v1api20231101.ImmutabilitySettings_STATUS
                          Immutability Settings at vault level
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          state:
                            type: string
                        type: object
                      softDeleteSettings:
                        description: |-
                          Storage version of v1api20231101.SoftDeleteSettings_STATUS
                          Soft delete related settings
                        properties:
                          $propertyBag:
                            additionalProperties:
                              type: string
                            description: |-
                              PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                              resources, allowing for full fidelity round trip conversions
                            type: object
                          retentionDurationInDays:
                            type: number
                          state:
                            type: string
                        type: object
                    type: object
                  storageSettings:
                    items:
                      description: |-
                        Storage version of v1api20231101.StorageSetting_STATUS
                        Storage setting
                      properties:
                        $propertyBag:
                          additionalProperties:
                            type: string
                          description: |-
                            PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                            resources, allowing for full fidelity round trip conversions
                          type: object
                        datastoreType:
                          type: string
                        type:
                          type: string
                      type: object
                    type: array
                type: object
              systemData:
                description: |-
                  Storage version of v1api20231101.SystemData_STATUS
                  Metadata pertaining to creation and last modification of the resource.
                properties:
                  $propertyBag:
                    additionalProperties:
                      type: string
                    description: |-
                      PropertyBag is an unordered set of stashed information that used for properties not directly supported by storage
                      resources, allowing for full fidelity round trip conversions
                    type: object
                  createdAt:
                    type: string
                  createdBy:
                    type: string
                  createdByType:
                    type: string
                  lastModifiedAt:
                    type: string
                  lastModifiedBy:
                    type: string
                  lastModifiedByType:
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              type:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
