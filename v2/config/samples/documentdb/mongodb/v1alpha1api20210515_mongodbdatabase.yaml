apiVersion: documentdb.azure.com/v1alpha1api20210515
kind: MongodbDatabase
metadata:
  name: sample-db-mongodb-alpha
  namespace: default
spec:
  location: westcentralus
  owner:
    name: sample-db-account
  options:
    autoscaleSettings:
      maxThroughput: 4000
  resource:
    id: sample-db-mongodb-alpha
