# Adapted from CosmosDB SQL Bicep examples:
# https://docs.microsoft.com/en-us/azure/cosmos-db/sql/manage-with-bicep#create-sproc
apiVersion: documentdb.azure.com/v1beta20210515
kind: SqlDatabaseContainerStoredProcedure
metadata:
  name: sample-sql-stored-procedure-beta
  namespace: default
spec:
  location: westcentralus
  owner:
    name: sample-sql-container-beta
  resource:
    id: sample-sql-stored-procedure-beta
    body: |
      function () {
          var context = getContext();
          var response = context.getResponse();
          response.setBody('Hello, World');
      }
