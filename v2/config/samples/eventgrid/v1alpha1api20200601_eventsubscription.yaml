apiVersion: eventgrid.azure.com/v1alpha1api20200601
kind: EventSubscription
metadata:
  name: sample-subscription
  namespace: default
spec:
  owner:
    name: sample-domain-topic
  destination:
    storageQueue:
      endpointType: StorageQueue
      properties: 
        resourceReference: 
          ARMID: "account-ARM-ID #TODO"
        queueName: samplequeue
