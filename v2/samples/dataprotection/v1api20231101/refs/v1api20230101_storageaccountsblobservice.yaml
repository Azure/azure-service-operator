apiVersion: storage.azure.com/v1api20230101
kind: StorageAccountsBlobService
metadata:
  name: asodpsamplekubestorageservice
  namespace: default
  annotations: 
    # Can't be directly deleted in Azure, you must delete it's parent
    # We can delete it from the cluster by applying this annotation, but this won't change anything in Azure.
    serviceoperator.azure.com/reconcile-policy: detach-on-delete
spec:
  owner:
    name: asodpsamplekubestorage
