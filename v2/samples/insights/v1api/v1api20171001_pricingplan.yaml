apiVersion: insights.azure.com/v1api20171001
kind: PricingPlan
metadata:
  name: aso-sample-pricingplan
  namespace: default
  annotations: 
    # Can't be directly deleted in Azure, only by deleting the parent component
    # We can delete it from the cluster by applying this annotation, but this won't change anything in Azure.
    serviceoperator.azure.com/reconcile-policy: detach-on-delete
spec:
  cap: 1.0
  owner:
    name: sampleappinsights
  planType: "Basic"
  stopSendNotificationWhenHitCap: false
