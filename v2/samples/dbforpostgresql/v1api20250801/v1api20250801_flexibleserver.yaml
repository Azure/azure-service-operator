apiVersion: dbforpostgresql.azure.com/v1api20250801
kind: FlexibleServer
metadata:
  name: asosampleserver20250801
  namespace: default
spec:
  location: uksouth
  owner:
    name: aso-sample-rg
  version: "18"
  sku:
    name: Standard_D4s_v3
    tier: GeneralPurpose
  administratorLogin: myAdmin
  administratorLoginPassword: # This is the name/key of a Kubernetes secret in the same namespace
    name: server-admin-pw
    key: password
  storage:
    storageSizeGB: 128
  # Needed if you want to use FlexibleServers Administrator with AAD
  # authConfig:
  #   activeDirectoryAuth: Enabled
  #   passwordAuth: Enabled
  #   tenantId: <your tenant here>
  #   tenantIdFromConfig:
  #     name: admin-settings
  #     key: tenantId
