apiVersion: creator.telstra.k8.io/v1
kind: EventhubNamespace
metadata:
  name: eventhubnamespace-sample
spec:    
    location: "westus" 
    sku:
      name: "Standard"
      tier: "Standard"
      capacity: 1      
    properties:
      isautoinflateenabled: false
      maximumthroughputunits: 0
      kafkaenabled: false
