apiVersion: azure.microsoft.com/v1
kind: AdlsGen2
metadata:
  name: adls-test
spec:
  # Add fields here
  filesystem: test-filesystem-clm
  # xmsproperties: 
  # xmsclientrequestid:
  # timeout:
  # xmsdate:
