
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: cosmosdbs.service.azure
spec:
  group: service.azure
  names:
    kind: CosmosDB
    plural: cosmosdbs
  scope: ""
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: CosmosDB is the Schema for the cosmosdbs API
      properties:
        additionalResources:
          description: CosmosDBAdditionalResources holds the additional resources
          properties:
            secrets:
              items:
                type: string
              type: array
          type: object
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        output:
          properties:
            cosmosDBName:
              type: string
            primaryMasterKey:
              type: string
          type: object
        spec:
          description: CosmosDBSpec defines the desired state of CosmosDB
          properties:
            kind:
              description: CosmosDBKind enumerates the values for kind. Only one of
                the following kinds may be specified. If none of the following kinds
                is specified, the default one is GlobalDocumentDBKind.
              enum:
              - GlobalDocumentDB
              - MongoDB
              type: string
            location:
              type: string
            properties:
              description: CosmosDBProperties the CosmosDBProperties of CosmosDB.
              properties:
                databaseAccountOfferType:
                  description: CosmosDBDatabaseAccountOfferType - The offer type for
                    the Cosmos DB database account.
                  enum:
                  - Standard
                  type: string
              type: object
          type: object
        status:
          description: CosmosDBStatus defines the observed state of CosmosDB
          properties:
            deploymentName:
              description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                of cluster Important: Run "make" to regenerate code after modifying
                this file'
              type: string
            provisioningState:
              type: string
          type: object
      type: object
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
