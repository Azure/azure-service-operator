
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: storages.service.azure
spec:
  group: service.azure
  names:
    kind: Storage
    plural: storages
  scope: ""
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: Storage is the Schema for the storages API
      properties:
        additionalResources:
          description: StorageAdditionalResources holds the additional resources
          properties:
            secrets:
              items:
                type: string
              type: array
          type: object
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        output:
          properties:
            connectionString1:
              type: string
            connectionString2:
              type: string
            key1:
              type: string
            key2:
              type: string
            storageAccountName:
              type: string
          type: object
        spec:
          description: StorageSpec defines the desired state of Storage
          properties:
            accessTier:
              description: AccessTier enumerates the values for access tier. Only
                one of the following access tiers may be specified. If none of the
                following access tiers is specified, the default one is Hot.
              enum:
              - Cool
              - Hot
              type: string
            kind:
              description: StorageKind enumerates the values for kind. Only one of
                the following kinds may be specified. If none of the following kinds
                is specified, the default one is StorageV2.
              enum:
              - BlobStorage
              - BlockBlobStorage
              - FileStorage
              - Storage
              - StorageV2
              type: string
            location:
              type: string
            sku:
              description: Sku the SKU of the storage account.
              properties:
                name:
                  description: 'Name - The SKU name. Required for account creation;
                    optional for update. Possible values include: ''StandardLRS'',
                    ''StandardGRS'', ''StandardRAGRS'', ''StandardZRS'', ''PremiumLRS'',
                    ''PremiumZRS'', ''StandardGZRS'', ''StandardRAGZRS'''
                  enum:
                  - Premium_LRS
                  - Premium_ZRS
                  - Standard_GRS
                  - Standard_GZRS
                  - Standard_LRS
                  - Standard_RAGRS
                  - Standard_RAGZRS
                  - Standard_ZRS
                  type: string
              type: object
            supportsHttpsTrafficOnly:
              type: boolean
          type: object
        status:
          description: StorageStatus defines the observed state of Storage
          properties:
            deploymentName:
              type: string
            generation:
              format: int64
              type: integer
            provisioningState:
              type: string
          type: object
      type: object
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
